---
module: ml-code
---
### [1] X — массив NumPy формы (50, 4). Что вернёт `X.sum(axis=1)`? @ml-numpy
- [ ] Массив из 4 сумм по столбцам
- [x] Массив из 50 сумм по строкам
- [ ] Одно число
- [ ] Массив формы (50, 4)
> Ось, по которой суммируем, исчезает: остаётся ось объектов.

### [1] Какой оператор NumPy выполняет матричное умножение? @ml-numpy
- [ ] `*`
- [x] `@`
- [ ] `**`
- [ ] `&`
> `*` умножает поэлементно.

### [1] Зачем в градиентном спуске learning rate? @ml-from-scratch
- [ ] Чтобы задать число итераций
- [x] Он определяет длину шага против градиента: слишком большой — расходимость, слишком маленький — медленная сходимость
- [ ] Чтобы регуляризовать модель
- [ ] Он нужен только для SGD
> Шаг w ← w − η·∇L.

### [1] Допуск ответа в задаче — 10⁻⁶. Как лучше печатать число? @ml-contest-tasks
- [ ] `print(round(x, 6))`
- [x] С запасом знаков, например `f"{x:.9f}"`
- [ ] `print(int(x))`
- [ ] `print(x)` без форматирования — всегда достаточно
> Округление до 6 знаков само вносит ошибку до 5·10⁻⁷.

### [2] y имеет форму (n,), p — (n, 1). Что вернёт `np.mean((y - p) ** 2)`? @ml-numpy
- [ ] Правильный MSE
- [x] Неверное число: разность растянется до матрицы (n, n)
- [ ] Ошибку несовпадения форм
- [ ] nan
> Broadcasting молча превращает (n,) − (n, 1) в (n, n). Выравнивайте формы через `ravel()`.

### [2] Как устойчиво вычислить softmax для логитов порядка 1000? @ml-from-scratch
- [ ] Поделить логиты на 1000
- [x] Вычесть из всех логитов максимальный — результат тот же, exp не переполняется
- [ ] Взять логарифм логитов
- [ ] Ограничить логиты сверху единицей
> Softmax инвариантен к сдвигу всех логитов на одну константу.

### [2] Условие требует одновременного обновления весов. В коде `for j in range(d): w[j] -= lr * grad_j(w)`. В чём ошибка? @ml-from-scratch
- [ ] Ошибки нет
- [x] Градиенты следующих координат считаются по уже обновлённым весам — это покоординатный спуск
- [ ] Нужно прибавлять, а не вычитать
- [ ] Нельзя использовать цикл
> Сначала весь вектор градиента по старым весам, потом обновление.

### [2] Как найти среднее значение признака по каждому классу без цикла по объектам? @ml-numpy
- [ ] `X.mean()`
- [x] `np.bincount(y, weights=x) / np.bincount(y)`
- [ ] `np.sort(x)[y]`
- [ ] `x[y].mean()`
> `bincount` с весами суммирует значения по группам.

### [3] Как проверить аналитический градиент функции потерь? @ml-from-scratch
- [ ] Посмотреть, что потеря уменьшается на первом шаге
- [x] Сравнить с центральной разностью (L(w + ε·eⱼ) − L(w − ε·eⱼ)) / 2ε по каждой координате
- [ ] Сравнить знаки градиента и весов
- [ ] Обучить модель и посмотреть на accuracy
> Относительное расхождение порядка 1e-6 и меньше — градиент верен.

### [3] Почему для системы XᵀXw = Xᵀy используют `solve` или `lstsq`, а не `inv(XᵀX) @ Xᵀy`? @ml-from-scratch
- [ ] `inv` не существует в NumPy
- [x] Решение системы дешевле и численно устойчивее, а `lstsq` работает и при вырожденной матрице
- [ ] `inv` работает только для целых чисел
- [ ] Ответы будут разные даже для хорошо обусловленной матрицы
> Явное обращение добавляет ошибки округления и лишнюю работу.

### [3] Задача «обучи модель и выведи предсказания», засчитывается при метрике выше порога. Что делать до сложной модели? @ml-contest-tasks
- [ ] Отправлять разные модели, пока не пройдёт
- [x] Сдать бейзлайн, отложить валидацию и мерить на ней метрику из условия
- [ ] Сразу настраивать гиперпараметры
- [ ] Обучиться на всех данных без валидации
> Бейзлайн показывает масштаб, валидация — избавляет от отправок вслепую.

### [3] Как найти наибольшее собственное значение ковариационной матрицы без библиотек? @ml-from-scratch
- [ ] Взять максимальный элемент диагонали
- [x] Степенным методом: v ← Cv / ‖Cv‖, затем λ = vᵀCv
- [ ] Посчитать след матрицы
- [ ] Посчитать определитель
> След — сумма всех собственных значений, а диагональ — дисперсии исходных признаков.
