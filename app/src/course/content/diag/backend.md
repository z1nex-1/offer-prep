---
module: backend
---
### [1] Какой протокол гарантирует доставку и порядок байтов? @networks
- [x] TCP
- [ ] UDP
- [ ] IP
- [ ] DNS
> UDP отправляет датаграммы без подтверждений; IP — уровнем ниже и тоже без гарантий.

### [2] Сколько сообщений в тройном рукопожатии TCP и какие? @networks
- [ ] Два: SYN и ACK
- [x] Три: SYN, SYN-ACK, ACK
- [ ] Четыре: SYN, ACK, FIN, ACK
- [ ] Одно: SYN
> Закрытие соединения — отдельный обмен FIN и ACK.

### [3] Клиент дважды отправил по TCP по 100 байт. Что гарантированно получит сервер? @networks
- [ ] Два отдельных сообщения
- [x] 200 байт в исходном порядке, границы сообщений могут не сохраниться
- [ ] Байты в произвольном порядке
- [ ] Одно сообщение в 200 байт
> TCP — поток байтов; границы задаёт прикладной протокол.

### [1] Какой HTTP-метод не идемпотентен? @http-api
- [ ] GET
- [ ] PUT
- [ ] DELETE
- [x] POST
> Повтор POST может создать второй ресурс.

### [2] Чем 401 отличается от 403? @http-api
- [ ] Ничем
- [x] 401 — не аутентифицирован, 403 — аутентифицирован, но нет прав
- [ ] 401 — нет ресурса, 403 — ошибка сервера
- [ ] 403 — неверный пароль
> 404 — нет ресурса, 5xx — ошибки сервера.

### [3] Чем gRPC отличается от REST? @http-api
- [ ] gRPC работает только в браузере
- [x] Контракт в .proto, бинарный Protobuf поверх HTTP/2, генерация клиента и стриминг
- [ ] gRPC — текстовый JSON-протокол
- [ ] REST быстрее всегда
> REST — стиль поверх HTTP с ресурсами в URL, чаще с JSON.

### [1] Чем HAVING отличается от WHERE? @sql-basics
- [ ] Ничем
- [x] WHERE фильтрует строки до группировки, HAVING — группы после неё
- [ ] HAVING только для JOIN
- [ ] WHERE только для чисел
> Агрегаты в WHERE использовать нельзя.

### [2] Как найти пользователей без заказов? @sql-basics
- [ ] INNER JOIN и WHERE orders.id IS NULL
- [x] LEFT JOIN orders и WHERE orders.id IS NULL
- [ ] WHERE orders = NULL
- [ ] CROSS JOIN
> LEFT JOIN сохраняет пользователей без пары.

### [2] Есть индекс по (user_id, created_at). Поможет ли он запросу `WHERE created_at > ...`? @db-internals
- [ ] Да, всегда
- [x] Обычно нет: составной индекс работает по левому префиксу
- [ ] Только для чисел
- [ ] Да, если таблица маленькая
> Нужен отдельный индекс по created_at или другой порядок столбцов.

### [3] Какую аномалию предотвращает Read Committed, а какие допускает? @db-internals
- [ ] Предотвращает все
- [x] Запрещает грязное чтение, допускает неповторяющееся чтение и фантомы
- [ ] Допускает грязное чтение
- [ ] Запрещает фантомы, допускает грязное чтение
> Выше — Repeatable Read и Serializable.

### [1] Что общего у потоков одного процесса? @os-linux
- [ ] Стек
- [x] Память кучи и открытые файлы
- [ ] Регистры
- [ ] Ничего
> Свой у каждого потока — стек и регистры.

### [2] Какой сигнал нельзя перехватить? @os-linux
- [ ] SIGTERM
- [ ] SIGINT
- [x] SIGKILL
- [ ] SIGHUP
> Для корректного завершения сначала шлют SIGTERM.

### [2] Зачем бэкенд-приложение за балансировщиком делают stateless? @backend-arch
- [ ] Чтобы не нужна была база
- [x] Чтобы любой запрос могла обработать любая копия и копии можно было добавлять
- [ ] Так требует HTTP
- [ ] Чтобы ускорить Python
> Состояние выносят в базу, кэш или токен.

### [3] Почему обработчики сообщений из очереди делают идемпотентными? @backend-arch
- [ ] Чтобы быстрее работали
- [x] При гарантии «хотя бы один раз» сообщение может прийти повторно
- [ ] Иначе очередь не примет сообщение
- [ ] Чтобы сообщения шли по порядку
> Повторная обработка не должна менять результат.
