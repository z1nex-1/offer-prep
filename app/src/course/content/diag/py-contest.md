---
module: py-contest
---
### [1] Что значит вердикт WA? @py-contest
- [ ] Превышено время
- [x] Неправильный ответ на одном из тестов
- [ ] Ошибка компиляции
- [ ] Программа упала
> Wrong Answer — вывод не совпал с правильным.

### [1] Можно ли на контесте Яндекса пользоваться numpy? @py-contest
- [ ] Да, любые библиотеки
- [x] Нет, только стандартная библиотека
- [ ] Только numpy
- [ ] Только на Python 2
> Сторонние библиотеки на отборе запрещены.

### [2] Какой способ чтения лучше для 10⁶ чисел? @py-contest
- [ ] `input()` в цикле
- [x] `sys.stdin.buffer.read().split()`
- [ ] `eval(input())`
- [ ] `open("input.txt")`
> Одно чтение всего ввода — в разы быстрее построчного.

### [2] n ≤ 10⁵. Какая асимптотика нужна на Python? @py-contest
- [ ] O(n²) пройдёт
- [x] O(n log n) или O(n)
- [ ] O(2ⁿ)
- [ ] O(n³)
> 10¹⁰ операций не уложатся ни в какой лимит.

### [2] Какая сложность `deque.popleft()` и `list.pop(0)`? @py-stdlib
- [x] O(1) и O(n)
- [ ] O(1) и O(1)
- [ ] O(n) и O(n)
- [ ] O(log n) и O(n)
> У списка удаление из начала сдвигает все элементы.

### [2] Что выведет код? @py-stdlib
```python
import heapq
h = []
for x in [4, 1, 7]:
    heapq.heappush(h, -x)
print(-heapq.heappop(h))
```
- [ ] `1`
- [x] `7`
- [ ] `4`
- [ ] `-7`
> Отрицание превращает min-кучу в max-кучу.

### [2] Что выведет код? @py-stdlib
```python
import bisect
a = [1, 3, 3, 3, 8]
print(bisect.bisect_right(a, 3) - bisect.bisect_left(a, 3))
```
- [ ] `2`
- [x] `3`
- [ ] `4`
- [ ] `1`
> Разность правой и левой границ — число вхождений.

### [3] Зачем в решении с глубокой рекурсией запускают поток с увеличенным `threading.stack_size`? @py-contest
- [ ] Чтобы решение шло параллельно
- [x] `setrecursionlimit` снимает только проверку интерпретатора, а стек основного потока может закончиться
- [ ] Так требует Яндекс Контест
- [ ] Чтобы ускорить ввод
> Без большого стека программа упадёт с RE на глубине в десятки тысяч.

### [3] Что выведет код? @py-stdlib
```python
from itertools import combinations, permutations
print(len(list(combinations(range(5), 3))), len(list(permutations(range(4), 2))))
```
- [ ] `60 6`
- [x] `10 12`
- [ ] `10 6`
- [ ] `20 12`
> C(5, 3) = 10, размещения A(4, 2) = 4 · 3 = 12.

### [3] Решение получает WA на 23-м тесте, примеры проходят. Что эффективнее всего? @py-contest
- [ ] Отправлять с мелкими правками наугад
- [x] Написать перебор и стресс-тест на маленьких случайных тестах
- [ ] Написать в поддержку
- [ ] Поднять лимит рекурсии
> Первый же несовпавший маленький тест покажет ошибку.
