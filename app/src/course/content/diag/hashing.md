---
module: hashing
---
### [1] Как быстро проверить, встречалось ли уже число, при проходе массива? @hash-patterns
- [ ] Список и `in`
- [x] Множество и `in`
- [ ] Отсортировать массив на каждом шаге
- [ ] Строка с числами
> Проверка во множестве — O(1) в среднем.

### [1] Две строки — анаграммы. Какая проверка правильная? @hash-patterns
- [ ] `set(a) == set(b)`
- [x] `Counter(a) == Counter(b)`
- [ ] `len(a) == len(b)`
- [ ] Все символы `a` есть в `b`
> Нужны частоты, а не только наличие символов.

### [2] Что выведет код? @hash-patterns
```python
a = [2, 7, 11, 15]
seen = {}
for i, x in enumerate(a):
    if 9 - x in seen:
        print(seen[9 - x], i)
    seen[x] = i
```
- [ ] `1 0`
- [x] `0 1`
- [ ] `0 3`
- [ ] Ничего
> Классика two sum за один проход.

### [2] Как сгруппировать слова-анаграммы? @hash-patterns
- [ ] По длине
- [x] Словарь с ключом `"".join(sorted(word))`
- [ ] По первой букве
- [ ] Множеством слов
> У всех анаграмм одинаковый отсортированный вид.

### [2] Какая сложность поиска в хеш-таблице в худшем случае? @hash-tables
- [ ] O(1)
- [x] O(n)
- [ ] O(log n)
- [ ] O(n log n)
> Если все ключи попали в одну корзину.

### [2] Почему `list` нельзя сделать ключом словаря? @hash-tables
- [ ] Слишком большой
- [x] Изменяемый и нехешируемый: хеш мог бы поменяться
- [ ] Ключи — только строки
- [ ] Можно
> Кортеж можно, если его элементы хешируемы.

### [3] Как посчитать количество подмассивов с суммой k (числа любого знака)? @hash-patterns
- [ ] Скользящее окно
- [x] Префиксные суммы и словарь «сумма префикса → сколько раз»
- [ ] Сортировка и два указателя
- [ ] Только перебор O(n²)
> Окно не работает с отрицательными числами.

### [3] Что выведет код? @hash-patterns
```python
cnt = {0: 1}
cur = res = 0
for x in [1, 2, 3]:
    cur += x
    res += cnt.get(cur - 3, 0)
    cnt[cur] = cnt.get(cur, 0) + 1
print(res)
```
- [ ] `1`
- [x] `2`
- [ ] `3`
- [ ] `0`
> Подмассивы с суммой 3: `[1, 2]` и `[3]`.

### [3] Какой способ разрешения коллизий в dict CPython? @hash-tables
- [ ] Цепочки
- [x] Открытая адресация
- [ ] Деревья в корзинах
- [ ] Коллизий нет
> Записи лежат в одном массиве, при коллизии ищется другая ячейка.
