import type { Topic } from '../../types'

const JS = 'JavaScript'
const WEB = 'Веб и браузер'
const R = 'React и TypeScript'

export const frontend: Topic[] = [
  {
    id: 'web-html-css',
    title: 'HTML и CSS: семантика, блочная модель, flex и grid',
    area: WEB,
    level: 'base',
    minutes: 90,
    summary: 'Семантическая вёрстка, специфичность, позиционирование, адаптивность.',
    body: `### Семантика и доступность
Теги несут смысл: \`header\`, \`nav\`, \`main\`, \`article\`, \`section\`, \`aside\`, \`footer\`, \`button\` (а не \`div\` с onClick). Семантика помогает скринридерам, SEO и навигации с клавиатуры. Картинкам — \`alt\`, полям — \`label\`, интерактивным элементам — фокус и видимый outline.

### Блочная модель
Размер элемента = content + padding + border (+ margin снаружи). \`box-sizing: border-box\` включает padding и border в \`width\` — удобнее. Вертикальные margin соседних блоков **схлопываются** (берётся больший).

### Отображение и позиционирование
- \`display\`: block, inline (ширина и высота не задаются), inline-block, flex, grid, none.
- \`position\`: static, relative (смещение от своего места), absolute (от ближайшего позиционированного предка), fixed (от окна), sticky (прилипает при прокрутке).
- \`z-index\` работает в пределах **контекста наложения** (его создают position с z-index, opacity < 1, transform и др.).

### Специфичность
Считается как (inline, id, классы/атрибуты/псевдоклассы, теги): \`#nav .item a\` = (0,1,1,1). Побеждает большая специфичность, при равенстве — последнее правило. \`!important\` перебивает всё — признак проблем в архитектуре CSS.

### Flexbox и Grid
- **Flex** — одномерная раскладка: \`justify-content\` (главная ось), \`align-items\` (поперечная), \`flex: 1 1 0\` (grow, shrink, basis), \`gap\`, \`flex-wrap\`.
- **Grid** — двумерная: \`grid-template-columns: repeat(auto-fill, minmax(200px, 1fr))\`, области, \`gap\`.
Центрирование: \`display: grid; place-items: center\` или flex с \`justify-content\` и \`align-items: center\`.

### Адаптивность
Медиазапросы, относительные единицы (\`rem\`, \`%\`, \`vw\`, \`clamp()\`), mobile-first, \`<meta name="viewport">\`, адаптивные изображения (\`srcset\`, \`picture\`). Контейнерные запросы (\`@container\`) — адаптация к размеру родителя.

### Методологии
БЭМ (блок__элемент--модификатор, придуман в Яндексе), CSS Modules, CSS-in-JS, утилитарные классы (Tailwind).`,
    questions: [
      {
        q: 'Как работает box-sizing: border-box?',
        a: 'По умолчанию (content-box) width задаёт ширину содержимого, а padding и border прибавляются сверху — элемент с width: 100% и padding вылезает за родителя. border-box включает padding и border в width, и элемент занимает ровно заданную ширину. Поэтому его обычно задают глобально.',
      },
      {
        q: 'Как вычисляется специфичность селектора?',
        a: 'Как кортеж: инлайн-стиль, количество id, количество классов, атрибутов и псевдоклассов, количество тегов и псевдоэлементов. Сравнение идёт слева направо: один id сильнее любого числа классов. При равной специфичности применяется правило, объявленное позже. !important поднимает объявление над обычными.',
      },
      {
        q: 'Чем flexbox отличается от grid?',
        a: 'Flexbox раскладывает элементы вдоль одной оси (строка или столбец), размеры определяются содержимым и свойствами flex — хорош для компонентов: панелей, списков, выравнивания. Grid задаёт двумерную сетку строк и столбцов, в которую размещаются элементы, — хорош для раскладки страниц и карточных сеток. Часто используются вместе.',
      },
      {
        q: 'Как отцентрировать элемент по вертикали и горизонтали?',
        a: 'У родителя display: grid; place-items: center. Или display: flex; justify-content: center; align-items: center. Для абсолютно позиционированного элемента — top: 50%; left: 50%; transform: translate(-50%, -50%) или inset: 0; margin: auto при заданных размерах.',
      },
      {
        q: 'Чем position: absolute отличается от fixed и sticky?',
        a: 'absolute позиционирует элемент относительно ближайшего предка с position не static и вынимает его из потока. fixed — относительно окна просмотра (или предка с transform), не прокручивается. sticky ведёт себя как relative, пока не достигнет порога прокрутки внутри родителя, затем прилипает как fixed.',
      },
      {
        q: 'Почему важна семантическая вёрстка?',
        a: 'Скринридеры и другие вспомогательные технологии используют семантику для навигации (заголовки, ориентиры main и nav, кнопки). Нативные элементы вроде button и a из коробки фокусируются и работают с клавиатуры. Поисковики лучше понимают структуру. Код читабельнее. div с onClick вместо button ломает доступность.',
      },
    ],
    resources: [
      { title: 'MDN: CSS', url: 'https://developer.mozilla.org/ru/docs/Web/CSS', kind: 'docs' },
      { title: 'Flexbox Froggy', url: 'https://flexboxfroggy.com/#ru', kind: 'practice' },
    ],
  },
  {
    id: 'js-types',
    title: 'Типы и приведение в JavaScript',
    area: JS,
    level: 'base',
    minutes: 60,
    summary: 'Примитивы и объекты, == и ===, приведение, NaN, typeof.',
    body: `### Типы
7 примитивов: \`string\`, \`number\`, \`bigint\`, \`boolean\`, \`undefined\`, \`symbol\`, \`null\` — и \`object\` (включая массивы, функции, даты). Примитивы неизменяемы и передаются по значению, объекты — по ссылке.

### typeof
\`typeof null === 'object'\` (историческая ошибка), \`typeof [] === 'object'\` (проверяйте \`Array.isArray\`), \`typeof function(){} === 'function'\`, \`typeof NaN === 'number'\`.

### Приведение
- **К строке**: \`+\` со строкой конкатенирует: \`1 + '2' === '12'\`.
- **К числу**: остальные арифметические операции: \`'3' - 1 === 2\`, \`+'  42 ' === 42\`, \`Number('') === 0\`, \`Number('abc')\` — NaN, \`Number(null) === 0\`, \`Number(undefined)\` — NaN.
- **К логическому**: ложные значения — \`false, 0, -0, 0n, '', null, undefined, NaN\`. Всё остальное истинно, включая \`'0'\`, \`[]\`, \`{}\`.
- Объекты приводятся к примитиву через \`Symbol.toPrimitive\`, \`valueOf\`, \`toString\`: \`[] + [] === ''\`, \`[] + {} === '[object Object]'\`.

### == и ===
\`===\` сравнивает без приведения типов. \`==\` приводит: \`0 == ''\`, \`'1' == 1\`, \`null == undefined\` — true, но \`null == 0\` — false. Правило: всегда \`===\`, кроме проверки \`x == null\` (null или undefined).

### Числа
Все \`number\` — 64-битные с плавающей точкой: \`0.1 + 0.2 !== 0.3\`, целые точны до \`Number.MAX_SAFE_INTEGER\` (2⁵³ − 1). Для больших целых — \`BigInt\`. \`NaN !== NaN\` — проверка \`Number.isNaN\`. \`Object.is(NaN, NaN) === true\`, \`Object.is(0, -0) === false\`.

### Nullish
\`??\` возвращает правый операнд только для \`null\`/\`undefined\` (в отличие от \`||\`, который срабатывает на любое ложное значение: \`0 || 10 === 10\`, \`0 ?? 10 === 0\`). Опциональная цепочка \`a?.b?.()\`.`,
    questions: [
      {
        q: 'Какие типы данных есть в JavaScript?',
        a: 'Семь примитивов — string, number, bigint, boolean, undefined, null, symbol — и object. Массивы, функции, даты, Map и Set — разновидности объектов. Примитивы неизменяемы и сравниваются по значению, объекты — по ссылке.',
      },
      {
        q: 'Чем == отличается от ===?',
        a: '=== сравнивает значения без приведения типов: разные типы — false. == перед сравнением приводит операнды по сложным правилам: строки и логические значения к числам, объекты к примитивам; null равен только undefined. Из-за неочевидных результатов (0 == \'\', \'\\n\' == 0) используют ===, а == допустим только для x == null.',
      },
      {
        q: 'Какие значения являются ложными?',
        a: 'false, 0, -0, 0n, пустая строка, null, undefined и NaN. Всё остальное истинно, в том числе строка "0", "false", пустые массив и объект. Это важно для условий: if (arr.length) — правильно, if (arr) — всегда true для массива.',
      },
      {
        q: 'Почему 0.1 + 0.2 !== 0.3 и как сравнивать дробные числа?',
        a: 'Числа хранятся в двоичном формате IEEE 754, где 0.1 и 0.2 не представимы точно, и сумма получается 0.30000000000000004. Сравнивают с допуском: Math.abs(a - b) < Number.EPSILON (или разумный eps), а деньги хранят в целых копейках.',
      },
      {
        q: 'Чем ?? отличается от ||?',
        a: '|| возвращает правый операнд, если левый — любое ложное значение (0, пустая строка, false, NaN). ?? — только если левый null или undefined. Для значений по умолчанию, где 0 или пустая строка допустимы, нужен ??.',
      },
    ],
  },
  {
    id: 'js-scope',
    title: 'Области видимости, замыкания и this',
    area: JS,
    level: 'base',
    minutes: 90,
    summary: 'var, let, const, hoisting, TDZ, замыкания, правила this, call/apply/bind.',
    body: `### var, let, const
- \`var\` — функциональная область видимости, всплывает со значением \`undefined\`, можно переобъявить.
- \`let\`, \`const\` — блочная область видимости, до объявления — **временная мёртвая зона** (TDZ, ReferenceError). \`const\` запрещает переприсваивание, но не изменение объекта.
- Function declaration всплывает целиком, function expression и стрелки — нет.

### Замыкания
Функция сохраняет доступ к лексическому окружению, в котором была создана, даже после выхода из него. Применения: приватные переменные, фабрики функций, мемоизация, каррирование, debounce/throttle.
\`\`\`js
function debounce(fn, ms) {
  let t
  return function (...args) {
    clearTimeout(t)
    t = setTimeout(() => fn.apply(this, args), ms)
  }
}
\`\`\`

### this
Определяется **способом вызова**:
1. \`new F()\` — новый объект.
2. \`call\`/\`apply\`/\`bind\` — явно переданный.
3. \`obj.method()\` — объект слева от точки.
4. Обычный вызов \`f()\` — \`undefined\` в строгом режиме (в нестрогом — глобальный объект).
5. **Стрелочные функции** не имеют своего \`this\` — берут из внешней области в момент создания; \`call\`/\`bind\` его не меняют.

Потеря контекста: \`setTimeout(obj.method, 0)\`, передача метода как колбэка, деструктуризация метода. Лечится \`bind\`, стрелкой-обёрткой или полем-стрелкой в классе.

### call, apply, bind
\`f.call(ctx, a, b)\`, \`f.apply(ctx, [a, b])\` — вызов с контекстом; \`f.bind(ctx, a)\` — новая функция с привязанным контекстом (и частично применёнными аргументами).

### Реализуйте bind
\`\`\`js
Function.prototype.myBind = function (ctx, ...bound) {
  const fn = this
  return function (...args) {
    return fn.apply(ctx, [...bound, ...args])
  }
}
\`\`\``,
    questions: [
      {
        q: 'Чем отличаются var, let и const?',
        a: 'var имеет функциональную область видимости, поднимается со значением undefined и допускает повторное объявление. let и const — блочные, до строки объявления находятся во временной мёртвой зоне (обращение — ReferenceError), повторное объявление в той же области запрещено. const нельзя переприсвоить, но объект, на который он указывает, изменять можно.',
      },
      {
        q: 'Что такое замыкание? Приведите практический пример.',
        a: 'Функция вместе с лексическим окружением, в котором она создана: она продолжает видеть переменные внешней функции после её завершения. Примеры: счётчик с приватной переменной, debounce, который хранит id таймера между вызовами, мемоизация с кэшем внутри замыкания, обработчики событий в цикле с let.',
      },
      {
        q: 'Как определяется this?',
        a: 'По способу вызова: при new — новый объект; при call/apply/bind — переданный контекст; при вызове как метода obj.f() — obj; при простом вызове f() — undefined в строгом режиме. Стрелочные функции своего this не имеют и берут его из окружающей области. Отсюда потеря контекста при передаче метода как колбэка.',
      },
      {
        q: 'Чем call отличается от apply и bind?',
        a: 'call и apply сразу вызывают функцию с указанным this; call принимает аргументы списком, apply — массивом. bind не вызывает, а возвращает новую функцию с навсегда привязанным this и, возможно, частью аргументов. Повторный bind уже привязанной функции контекст не меняет.',
      },
      {
        q: 'Реализуйте debounce и объясните, чем он отличается от throttle.',
        a: 'debounce откладывает вызов, пока события продолжаются: каждый вызов сбрасывает таймер, функция выполнится через ms после последнего события — для поиска по вводу, ресайза. throttle гарантирует не больше одного вызова за интервал — для прокрутки и движения мыши. Реализация debounce: замыкание с переменной таймера, clearTimeout и новый setTimeout, сохранение this и аргументов через apply.',
      },
      {
        q: 'Что такое hoisting?',
        a: 'Поведение, при котором объявления обрабатываются до выполнения кода области видимости. Function declaration доступна целиком до своей строки. var доступна, но равна undefined. let, const и class тоже регистрируются заранее, но до объявления находятся во временной мёртвой зоне, и обращение к ним бросает ReferenceError.',
      },
    ],
  },
  {
    id: 'js-proto',
    title: 'Прототипы, классы и объекты',
    area: JS,
    level: 'core',
    minutes: 60,
    summary: 'Цепочка прототипов, new, классы как сахар, копирование объектов.',
    body: `### Прототипное наследование
У каждого объекта есть скрытая ссылка \`[[Prototype]]\` (доступ — \`Object.getPrototypeOf\`, устаревший \`__proto__\`). При чтении свойства, которого нет у объекта, поиск идёт по цепочке прототипов до \`Object.prototype\`, затем \`null\`. Запись всегда создаёт собственное свойство.

У функций есть свойство \`prototype\` — оно станет прототипом объектов, созданных через \`new F()\`.

### Что делает new
1. Создаёт пустой объект с прототипом \`F.prototype\`.
2. Вызывает \`F\` с \`this\` = новый объект.
3. Возвращает этот объект (если функция не вернула другой объект).

### Классы
\`class\` — синтаксис над прототипами: методы кладутся в \`prototype\`, \`extends\` настраивает цепочку, \`super\` вызывает родителя. Отличия от функций-конструкторов: тело в строгом режиме, класс нельзя вызвать без \`new\`, не всплывает. Приватные поля \`#field\`, статические члены, геттеры и сеттеры.

### Работа с объектами
- Копирование: \`{...obj}\`, \`Object.assign\` — поверхностные; \`structuredClone\` — глубокое (без функций).
- \`Object.keys/values/entries\`, \`Object.freeze\` (поверхностная заморозка), \`Object.defineProperty\` (дескрипторы: writable, enumerable, configurable).
- Проверка собственного свойства — \`Object.hasOwn(obj, key)\`.
- \`Map\` вместо объекта-словаря, когда ключи не строки, важен порядок вставки, частые добавления и удаления.`,
    questions: [
      {
        q: 'Как работает прототипное наследование?',
        a: 'Каждый объект ссылается на прототип. Если свойства нет у объекта, движок ищет его у прототипа, затем у прототипа прототипа и так до null. Методы обычно лежат в прототипе и разделяются всеми экземплярами. Классы в JS — синтаксический сахар над этой моделью.',
      },
      {
        q: 'Что происходит при вызове new?',
        a: 'Создаётся новый объект, его прототипом становится Constructor.prototype; функция-конструктор выполняется с this, указывающим на новый объект; если конструктор явно вернул объект, результатом будет он, иначе — созданный объект.',
      },
      {
        q: 'Как сделать глубокую копию объекта?',
        a: 'structuredClone(obj) — встроенное глубокое копирование, поддерживает Map, Set, Date и циклические ссылки, но не функции и не DOM-узлы. JSON.parse(JSON.stringify(obj)) теряет undefined, функции, Date превращает в строки и падает на циклах. Для сложных случаев — рекурсивная функция или библиотека.',
      },
      {
        q: 'Когда использовать Map вместо обычного объекта?',
        a: 'Когда ключи — не строки (объекты, числа без приведения), когда важен порядок вставки и размер (size), при частом добавлении и удалении ключей, когда нужно избежать коллизий с унаследованными свойствами вроде toString. Объект удобнее для фиксированных структур и сериализации в JSON.',
      },
    ],
  },
  {
    id: 'js-async',
    title: 'Асинхронность: event loop, промисы, async/await',
    area: JS,
    level: 'base',
    minutes: 120,
    summary: 'Главная тема JS-секций: порядок выполнения, микро- и макрозадачи, Promise API.',
    body: `### Event loop
JavaScript выполняет код в одном потоке. Окружение (браузер или Node.js) даёт асинхронные API, результаты которых попадают в очереди.

Цикл:
1. Выполнить весь синхронный код (стек вызовов пуст).
2. Выполнить **все микрозадачи**: колбэки промисов (\`then/catch/finally\`), продолжения после \`await\`, \`queueMicrotask\`, \`MutationObserver\`. Микрозадачи, добавленные в процессе, тоже выполняются сейчас.
3. Браузер может отрисовать кадр (\`requestAnimationFrame\` — перед отрисовкой).
4. Взять **одну макрозадачу**: \`setTimeout\`, \`setInterval\`, события ввода, сообщения, I/O — и повторить.

Следствия: промис всегда раньше таймера с 0 мс; бесконечная цепочка микрозадач блокирует отрисовку; тяжёлый синхронный код замораживает страницу.

### Промисы
Состояния: pending → fulfilled или rejected (однократно). \`then\` возвращает новый промис — цепочки. Исключение в \`then\` превращается в отклонение. Необработанное отклонение — событие \`unhandledrejection\`.

| Метод | Результат |
|---|---|
| \`Promise.all\` | все успешны → массив в порядке входа; первая ошибка отклоняет всё |
| \`Promise.allSettled\` | ждёт всех, возвращает статусы |
| \`Promise.race\` | первый завершившийся (успех или ошибка) |
| \`Promise.any\` | первый успешный; все ошибки → AggregateError |

### async/await
\`async\` функция всегда возвращает промис. \`await\` приостанавливает функцию до разрешения промиса, остальной код продолжает работать. Ошибки ловятся \`try/catch\`. Последовательные \`await\` в цикле выполняются по очереди — для параллельности \`await Promise.all(items.map(fetchItem))\`.

### Реализуйте Promise.all (частая задача)
\`\`\`js
function promiseAll(items) {
  return new Promise((resolve, reject) => {
    const results = []
    let left = items.length
    if (left === 0) return resolve(results)
    items.forEach((item, i) => {
      Promise.resolve(item).then((value) => {
        results[i] = value
        if (--left === 0) resolve(results)
      }, reject)
    })
  })
}
\`\`\`

### Отмена
\`AbortController\`: \`fetch(url, { signal })\`, \`controller.abort()\` — отмена устаревших запросов (поиск по вводу, смена страницы).`,
    questions: [
      {
        q: 'Объясните, как работает event loop.',
        a: 'Движок выполняет синхронный код до опустошения стека вызовов. Затем выполняются все микрозадачи (колбэки промисов, продолжения await, queueMicrotask), включая добавленные по ходу. Потом браузер может отрисовать кадр и берёт одну макрозадачу (таймер, событие, сообщение), после которой снова опустошается очередь микрозадач. Поэтому Promise.then выполняется раньше setTimeout(fn, 0).',
      },
      {
        q: 'Чем микрозадачи отличаются от макрозадач?',
        a: 'Макрозадачи — таймеры, события, сетевые колбэки, сообщения; за одну итерацию цикла выполняется одна. Микрозадачи — колбэки промисов, await, queueMicrotask; выполняются все сразу после каждой макрозадачи, до отрисовки. Бесконечно порождая микрозадачи, можно «повесить» страницу, хотя цикл формально не блокирован.',
      },
      {
        q: 'Реализуйте Promise.all.',
        a: 'Возвращаем новый промис. Для каждого элемента вызываем Promise.resolve(item).then: сохраняем значение по индексу (порядок результата — порядок входа, а не завершения), уменьшаем счётчик оставшихся и при нуле делаем resolve(results). Любая ошибка — reject. Отдельно обрабатываем пустой массив — сразу resolve([]).',
      },
      {
        q: 'Чем Promise.all отличается от allSettled, race и any?',
        a: 'all ждёт успеха всех и отклоняется при первой ошибке. allSettled ждёт завершения всех и возвращает массив объектов со статусом и значением или причиной. race завершается вместе с первым завершившимся промисом, неважно успешным или нет. any — с первым успешным, а если все упали — AggregateError.',
      },
      {
        q: 'Как выполнить асинхронные операции параллельно, а не последовательно?',
        a: 'Сначала запустить все операции, получив массив промисов, затем дождаться их вместе: await Promise.all(ids.map(id => fetchUser(id))). Последовательные await в цикле for…of ждут каждую операцию по очереди. Для ограничения параллелизма — пул: запускать не больше N одновременно.',
      },
      {
        q: 'Как отменить устаревший fetch-запрос?',
        a: 'Создать AbortController, передать controller.signal в fetch и вызвать controller.abort(), когда запрос больше не нужен (новый ввод, размонтирование компонента). fetch отклонится с AbortError, который нужно отличать от настоящих ошибок. Это же решает гонку, когда старый ответ приходит позже нового.',
      },
    ],
    resources: [{ title: 'learn.javascript.ru: событийный цикл', url: 'https://learn.javascript.ru/event-loop', kind: 'article' }],
  },
  {
    id: 'js-es',
    title: 'Современный JavaScript: ES2015+ и методы массивов',
    area: JS,
    level: 'base',
    minutes: 60,
    summary: 'Деструктуризация, spread, модули, итераторы, методы массивов, Map/Set.',
    body: `### Синтаксис
- Деструктуризация: \`const { a, b: renamed = 1, ...rest } = obj\`, \`const [x, , z] = arr\`.
- Spread и rest: \`[...a, ...b]\`, \`{ ...defaults, ...options }\`, \`function f(...args)\`.
- Шаблонные строки, стрелочные функции, параметры по умолчанию, сокращённые свойства.
- Опциональная цепочка \`?.\` и \`??\`.
- Модули: \`import\`/\`export\` — статические, выполняются один раз, в строгом режиме; динамический \`import()\` для ленивой загрузки.

### Методы массивов
| Метод | Возвращает | Меняет массив |
|---|---|---|
| \`map\`, \`filter\`, \`slice\`, \`concat\` | новый массив | нет |
| \`reduce\` | одно значение | нет |
| \`find\`, \`findIndex\`, \`some\`, \`every\`, \`includes\` | элемент / индекс / boolean | нет |
| \`push\`, \`pop\`, \`shift\`, \`unshift\`, \`splice\`, \`sort\`, \`reverse\` | разное | **да** |
| \`toSorted\`, \`toReversed\`, \`with\`, \`toSpliced\` (ES2023) | новый массив | нет |
| \`flat\`, \`flatMap\`, \`at\`, \`Array.from\` | новый | нет |

\`sort\` без компаратора сравнивает строки: \`[10, 9, 1].sort()\` → \`[1, 10, 9]\`.

### Итераторы и генераторы
Объект итерируем, если у него есть \`[Symbol.iterator]\`. \`for...of\`, spread и деструктуризация работают с любым итерируемым. Генераторы \`function*\` с \`yield\`.

### Коллекции
\`Map\`, \`Set\`, \`WeakMap\`, \`WeakSet\` (ключи — объекты, не мешают сборке мусора: хранение метаданных о DOM-узлах).

### Частые задачи на реализацию
\`flatten\`, \`deepEqual\`, \`groupBy\`, \`memoize\`, \`curry\`, \`compose\`, \`EventEmitter\`, \`once\`, \`throttle\`, глубокое копирование.`,
    questions: [
      {
        q: 'Чем map отличается от forEach?',
        a: 'map возвращает новый массив из результатов колбэка той же длины — для преобразования данных. forEach ничего не возвращает (undefined) и используется ради побочных эффектов. Ни один из них нельзя прервать break; для раннего выхода — for…of, some, find.',
      },
      {
        q: 'Какие методы массива изменяют исходный массив?',
        a: 'push, pop, shift, unshift, splice, sort, reverse, fill, copyWithin. Это важно в React: мутация состояния не вызовет перерисовку. Неизменяющие аналоги — spread, slice, toSorted, toReversed, toSpliced, with.',
      },
      {
        q: 'Как реализовать groupBy?',
        a: 'Через reduce: для каждого элемента вычисляем ключ функцией и добавляем элемент в массив acc[key], создавая его при первом появлении. Возвращаем acc. В современных движках есть встроенный Object.groupBy(items, fn) и Map.groupBy.',
      },
      {
        q: 'Как реализовать EventEmitter с методами on, off, emit и once?',
        a: 'Хранить Map событие → массив (или Set) обработчиков. on добавляет обработчик и возвращает this для цепочек; off удаляет конкретный; emit копирует список (чтобы изменения во время обхода не ломали итерацию) и вызывает обработчики с аргументами; once оборачивает обработчик функцией, которая вызывает его и сразу отписывается. Задача встречалась в Т-Банке.',
        companies: ['tbank'],
      },
      {
        q: 'Чем ES-модули отличаются от CommonJS?',
        a: 'ES-модули (import/export) статические: зависимости известны до выполнения, это даёт tree shaking; импорты — живые привязки; выполняются в строгом режиме; поддерживают top-level await и асинхронную загрузку. CommonJS (require/module.exports) — синхронная загрузка во время выполнения, экспортируется копия значения, исторически используется в Node.js.',
      },
    ],
  },
  {
    id: 'web-browser',
    title: 'Браузер: DOM, события, рендеринг, хранилища, CORS',
    area: WEB,
    level: 'core',
    minutes: 90,
    summary: 'Критический путь рендеринга, всплытие событий, делегирование, cookie и storage, CORS.',
    body: `### Путь рендеринга
HTML → **DOM**, CSS → **CSSOM** → **render tree** (только видимые элементы) → **layout** (размеры и позиции) → **paint** (пиксели слоёв) → **composite** (сборка слоёв на GPU).
- **Reflow** (перерасчёт раскладки) — дорогой: вызывается изменением размеров, добавлением элементов, чтением \`offsetHeight\` после записи стилей (принудительная синхронная раскладка).
- **Repaint** — перерисовка без изменения геометрии (цвет).
- Дешёвые анимации — \`transform\` и \`opacity\` (только композиция).
- \`<script>\` блокирует разбор HTML; \`defer\` — загрузка параллельно, выполнение после разбора в порядке; \`async\` — выполнение сразу по загрузке. CSS блокирует отрисовку.

### События
Фазы: **погружение** (capture) от window к цели → цель → **всплытие** (bubble) обратно. \`event.target\` — где произошло, \`currentTarget\` — на ком обработчик. \`stopPropagation\` останавливает распространение, \`preventDefault\` отменяет действие браузера.

**Делегирование**: один обработчик на родителе вместо тысяч на элементах — проверяем \`event.target.closest('.item')\`. Работает и для динамически добавленных элементов.

### Хранилища
| | Объём | Живёт | Отправляется на сервер |
|---|---|---|---|
| Cookie | ~4 КБ | до expires | да, с каждым запросом |
| localStorage | ~5 МБ | бессрочно | нет |
| sessionStorage | ~5 МБ | вкладка | нет |
| IndexedDB | сотни МБ | бессрочно | нет, асинхронная |
Флаги cookie: \`HttpOnly\` (недоступна из JS — защита от кражи через XSS), \`Secure\`, \`SameSite\` (защита от CSRF). Токены в localStorage уязвимы для XSS.

### Same-Origin Policy и CORS
Скрипт может читать ответы только своего источника (схема + домен + порт). **CORS** позволяет серверу разрешить другие источники заголовком \`Access-Control-Allow-Origin\`. Для «непростых» запросов (PUT, JSON, свои заголовки) браузер сначала отправляет **preflight** \`OPTIONS\`. CORS — механизм браузера, от серверных запросов не защищает.

### Web API
\`fetch\`, \`IntersectionObserver\` (ленивая загрузка, бесконечная прокрутка), \`ResizeObserver\`, \`requestAnimationFrame\`, Web Workers (тяжёлые вычисления вне главного потока), Service Worker (офлайн, кэш, пуши).`,
    questions: [
      {
        q: 'Что происходит после получения HTML браузером?',
        a: 'Парсер строит DOM, загружая по пути ресурсы; CSS разбирается в CSSOM. Скрипты без defer/async останавливают парсинг. Из DOM и CSSOM строится дерево отрисовки, затем layout вычисляет геометрию, paint рисует слои, composite собирает их на экране. Изменения стилей и DOM запускают эти этапы повторно частично или полностью.',
      },
      {
        q: 'Что такое reflow и как его избегать?',
        a: 'Перерасчёт раскладки при изменении геометрии: размеры, шрифты, добавление элементов. Дорог, особенно на больших страницах. Избегают: не чередовать чтение размеров и запись стилей в цикле (layout thrashing), группировать изменения DOM, анимировать transform и opacity, использовать requestAnimationFrame для визуальных изменений.',
      },
      {
        q: 'Что такое всплытие событий и делегирование?',
        a: 'Событие после срабатывания на целевом элементе поднимается по цепочке предков, вызывая их обработчики. Делегирование использует это: вешаем один обработчик на общего родителя и определяем элемент через event.target.closest(селектор). Экономит память и работает для элементов, добавленных позже.',
      },
      {
        q: 'Где хранить токен авторизации?',
        a: 'Надёжнее всего — в cookie с флагами HttpOnly, Secure и SameSite: JavaScript её не прочитает, значит, XSS не украдёт токен, а SameSite снижает риск CSRF. localStorage удобен, но доступен любому скрипту на странице. Короткоживущий access-токен в памяти + refresh-токен в HttpOnly-cookie — распространённый компромисс.',
      },
      {
        q: 'Что такое CORS и когда отправляется preflight?',
        a: 'Механизм, с помощью которого сервер разрешает браузеру отдавать скриптам других источников свои ответы: заголовки Access-Control-Allow-Origin, -Methods, -Headers, -Credentials. Preflight OPTIONS отправляется для непростых запросов: методы кроме GET/HEAD/POST, Content-Type кроме form-urlencoded/multipart/text-plain (например, application/json), нестандартные заголовки. CORS защищает пользователя в браузере, а не сервер.',
      },
      {
        q: 'Чем defer отличается от async у тега script?',
        a: 'Оба загружают скрипт параллельно с разбором HTML. defer выполняет скрипты после завершения разбора документа, строго в порядке следования, до DOMContentLoaded. async выполняет скрипт сразу после загрузки, прерывая разбор, порядок не гарантирован — подходит для независимых скриптов вроде аналитики.',
      },
    ],
  },
  {
    id: 'react-core',
    title: 'React: компоненты, состояние, рендеринг',
    area: R,
    level: 'core',
    minutes: 90,
    summary: 'Виртуальный DOM и reconciliation, ключи, подъём состояния, когда происходит ререндер.',
    body: `### Модель React
UI = f(state). Компоненты — функции, возвращающие JSX. При изменении состояния React вызывает компонент заново, строит новое дерево элементов, сравнивает с предыдущим (**reconciliation**) и применяет к DOM минимальные изменения.

### Когда компонент перерисовывается
1. Изменилось его состояние (\`setState\` с новым значением — сравнение через \`Object.is\`).
2. Перерисовался родитель — **все дети перерисовываются по умолчанию**, даже если пропсы не изменились.
3. Изменился контекст, который он читает.
\`React.memo\` пропускает перерисовку при поверхностно равных пропсах — но новый объект или функция в пропсах на каждом рендере сводит это на нет (нужны \`useMemo\`/\`useCallback\`).

### Ключи
\`key\` помогает сопоставить элементы списка между рендерами. Индекс в качестве ключа ломает состояние при вставке, удалении и сортировке (поля ввода «переезжают» к другим элементам). Ключ должен быть стабильным и уникальным среди соседей. Смена \`key\` пересоздаёт компонент — способ сбросить состояние.

### Состояние
- Неизменяемые обновления: \`setItems([...items, x])\`, а не \`items.push(x)\`.
- Функциональное обновление \`setCount(c => c + 1)\`, когда новое значение зависит от старого.
- Обновления батчатся — \`setState\` не меняет значение переменной сразу.
- **Подъём состояния** к общему родителю, если оно нужно нескольким компонентам. Не храните в состоянии то, что можно вычислить.
- Глобальное состояние: Context (для редко меняющихся данных), Redux Toolkit, Zustand, MobX; серверное состояние — TanStack Query.

### Управляемые компоненты
\`<input value={v} onChange={e => setV(e.target.value)} />\` — значение хранит React. Неуправляемые — через \`ref\`.

### JSX
\`className\`, \`htmlFor\`, \`disabled\` (а не \`disable\`), \`onClick\` в camelCase, выражения в \`{}\`. Условный рендер \`cond && <X/>\` — осторожно с \`0 && <X/>\` (выведет 0).

### Современный React
Concurrent-рендеринг (\`startTransition\`, \`useDeferredValue\`), Suspense, серверные компоненты (Next.js), React 19 (\`use\`, actions, компилятор React для автоматической мемоизации).`,
    questions: [
      {
        q: 'Когда компонент React перерисовывается?',
        a: 'Когда меняется его состояние, когда перерисовывается родитель (по умолчанию все дочерние компоненты рендерятся заново независимо от пропсов) и когда меняется значение используемого контекста. Изменение пропсов само по себе — следствие ререндера родителя. React.memo позволяет пропустить рендер при поверхностно равных пропсах.',
      },
      {
        q: 'Зачем нужен key и почему нельзя использовать индекс?',
        a: 'key позволяет React сопоставить элементы списка между рендерами и понять, какие добавлены, удалены или переставлены. С индексом при вставке в начало или сортировке ключи сдвигаются, и React переиспользует не те компоненты: их состояние (введённый текст, фокус) оказывается у другого элемента, плюс лишние обновления DOM. Нужен стабильный уникальный идентификатор из данных.',
      },
      {
        q: 'Почему нельзя изменять состояние напрямую?',
        a: 'React определяет изменения сравнением ссылок (Object.is). Мутация массива или объекта сохраняет ту же ссылку, поэтому ререндер может не произойти, а memo и эффекты с зависимостями будут работать неправильно. Нужно создавать новый объект: spread, map, filter, toSorted.',
      },
      {
        q: 'Что такое виртуальный DOM и reconciliation?',
        a: 'Представление интерфейса в виде дерева JS-объектов. При обновлении React строит новое дерево и сравнивает с предыдущим эвристическим алгоритмом за O(n): элементы разных типов пересоздаются целиком, списки сопоставляются по key. Затем в реальный DOM применяются только необходимые изменения.',
      },
      {
        q: 'Что выведет {count && <Badge />}, если count = 0?',
        a: 'Выведет 0: выражение 0 && ... возвращает 0, а React рендерит числа. Правильно — {count > 0 && <Badge />} или тернарный оператор.',
      },
      {
        q: 'Когда использовать Context, а когда стейт-менеджер?',
        a: 'Context — для передачи редко меняющихся данных глубоко по дереву: тема, язык, текущий пользователь. При каждом изменении значения перерисовываются все потребители, поэтому для часто меняющегося состояния он неэффективен. Для сложного клиентского состояния — Zustand, Redux Toolkit, MobX с подписками на части состояния; для данных с сервера — TanStack Query с кэшем и инвалидацией.',
      },
    ],
    resources: [{ title: 'react.dev — официальная документация', url: 'https://react.dev/learn', kind: 'docs' }],
  },
  {
    id: 'react-hooks',
    title: 'Хуки React',
    area: R,
    level: 'core',
    minutes: 90,
    summary: 'useState, useEffect, useMemo, useCallback, useRef, свои хуки и правила хуков.',
    body: `### Правила хуков
Вызывать только на верхнем уровне компонента или своего хука — не в условиях, циклах и колбэках. React опирается на порядок вызовов, чтобы сопоставлять состояние с хуками.

### useEffect
Синхронизация с внешним миром (подписки, запросы, таймеры, DOM API). Выполняется **после** отрисовки. Функция очистки вызывается перед следующим эффектом и при размонтировании.
\`\`\`jsx
useEffect(() => {
  const controller = new AbortController()
  fetch(\`/api/search?q=\${query}\`, { signal: controller.signal })
    .then((r) => r.json())
    .then(setResults)
    .catch((e) => { if (e.name !== 'AbortError') setError(e) })
  return () => controller.abort()     // отменяем устаревший запрос
}, [query])
\`\`\`
- Массив зависимостей должен содержать все используемые значения из области компонента, иначе — устаревшие замыкания (stale closure).
- В StrictMode в разработке эффекты запускаются дважды — чтобы выявить отсутствие очистки.
- Не нужен эффект для вычисления данных из пропсов и состояния — вычисляйте при рендере.

### useMemo и useCallback
\`useMemo\` кэширует результат вычисления, \`useCallback\` — функцию, между рендерами при неизменных зависимостях. Нужны для дорогих вычислений и для стабильных ссылок в пропсах мемоизированных детей или зависимостях эффектов. Не оптимизируйте всё подряд — мемоизация тоже стоит.

### useRef
Изменяемый контейнер, переживающий рендеры, изменение не вызывает перерисовку: доступ к DOM-узлу, хранение id таймера, предыдущего значения.

### Другие
\`useReducer\` (сложные переходы состояния), \`useContext\`, \`useLayoutEffect\` (синхронно до отрисовки — измерения DOM), \`useId\`, \`useTransition\`, \`useDeferredValue\`, \`useSyncExternalStore\`.

### Свои хуки
Функция \`useSomething\`, использующая другие хуки, — переиспользование логики: \`useDebounce\`, \`useFetch\`, \`useLocalStorage\`, \`useMediaQuery\`.
\`\`\`jsx
function useDebounce(value, ms) {
  const [v, setV] = useState(value)
  useEffect(() => {
    const t = setTimeout(() => setV(value), ms)
    return () => clearTimeout(t)
  }, [value, ms])
  return v
}
\`\`\``,
    questions: [
      {
        q: 'Как работает useEffect и массив зависимостей?',
        a: 'Эффект выполняется после отрисовки. Без массива — после каждого рендера; с пустым массивом — один раз после монтирования; с зависимостями — когда хотя бы одна изменилась по Object.is. Перед повторным запуском и при размонтировании вызывается функция очистки. В зависимостях должны быть все значения из компонента, которые использует эффект.',
      },
      {
        q: 'Чем useMemo отличается от useCallback?',
        a: 'useMemo(() => compute(a), [a]) кэширует результат вызова функции. useCallback(fn, [a]) кэширует саму функцию и эквивалентен useMemo(() => fn, [a]). Оба возвращают прежнее значение, пока зависимости не изменились. Используются для дорогих вычислений и сохранения ссылочного равенства в пропсах memo-компонентов и зависимостях эффектов.',
      },
      {
        q: 'Что такое stale closure в хуках?',
        a: 'Колбэк или эффект захватывает значения из рендера, в котором был создан. Если не указать зависимость, эффект продолжит видеть старое значение состояния. Типичный пример — setInterval в эффекте с пустыми зависимостями, который всегда видит count = 0. Лечится правильными зависимостями, функциональным обновлением setCount(c => c + 1) или useRef для последнего значения.',
      },
      {
        q: 'Как правильно загрузить данные в useEffect и избежать гонки?',
        a: 'Создать AbortController в эффекте, передать signal в fetch и в очистке вызвать abort(): при смене зависимостей или размонтировании предыдущий запрос отменится, и его ответ не перезапишет свежие данные. Обрабатывать состояния загрузки и ошибки, игнорировать AbortError. В продакшене удобнее TanStack Query с кэшем и дедупликацией.',
      },
      {
        q: 'Чем useRef отличается от useState?',
        a: 'Оба сохраняют значение между рендерами, но изменение ref.current не вызывает перерисовку, а setState — вызывает. ref подходит для DOM-узлов, таймеров, предыдущих значений и любых данных, которые не отображаются. Читать ref.current во время рендера для вывода — плохая идея: UI не обновится.',
      },
      {
        q: 'Почему хуки нельзя вызывать в условиях?',
        a: 'React хранит состояние хуков компонента списком и сопоставляет их по порядку вызова при каждом рендере. Если хук вызывается условно, порядок меняется, и состояние одного хука достанется другому. Поэтому хуки вызывают безусловно на верхнем уровне; условную логику помещают внутрь хука.',
      },
    ],
  },
  {
    id: 'ts-basics',
    title: 'TypeScript',
    area: R,
    level: 'core',
    minutes: 75,
    summary: 'Типы и интерфейсы, дженерики, сужение, утилитарные типы.',
    body: `### Базовые типы
\`string\`, \`number\`, \`boolean\`, массивы \`T[]\`, кортежи \`[string, number]\`, \`unknown\` (безопасный «любой» — нужно сузить перед использованием), \`any\` (отключает проверку), \`never\` (невозможное значение), \`void\`, литеральные типы \`'GET' | 'POST'\`.

### type и interface
Оба описывают форму объекта. \`interface\` можно расширять (\`extends\`) и дополнять повторным объявлением (declaration merging). \`type\` умеет объединения, пересечения, условные и сопоставленные типы. Для объектов в команде выбирают один стиль.

### Объединения и сужение
\`\`\`ts
type Result = { ok: true; data: User } | { ok: false; error: string }   // размеченное объединение
function show(r: Result) {
  if (r.ok) return r.data.name     // TS знает, что здесь есть data
  return r.error
}
\`\`\`
Сужение: \`typeof\`, \`instanceof\`, \`in\`, проверка поля-дискриминанта, пользовательские type guards (\`x is User\`), \`never\` для исчерпывающей проверки в switch.

### Дженерики
\`function first<T>(xs: T[]): T | undefined\`. Ограничения \`<T extends { id: string }>\`, значения по умолчанию, \`keyof\`, индексные типы \`T[K]\`.

### Утилитарные типы
\`Partial<T>\`, \`Required<T>\`, \`Readonly<T>\`, \`Pick<T, K>\`, \`Omit<T, K>\`, \`Record<K, V>\`, \`ReturnType<F>\`, \`Parameters<F>\`, \`Awaited<P>\`, \`NonNullable<T>\`.

### Продвинутое
Условные типы \`T extends U ? X : Y\`, \`infer\`, сопоставленные типы \`{ [K in keyof T]: ... }\`, шаблонные литеральные типы. \`satisfies\` — проверить соответствие типу, не расширяя тип значения. \`as const\`.

### Важно помнить
TypeScript проверяет типы только при компиляции: во время выполнения типов нет, данные из сети нужно валидировать (zod, valibot).`,
    questions: [
      {
        q: 'Чем unknown отличается от any?',
        a: 'Оба принимают любое значение. any отключает проверку типов: с ним можно делать что угодно, ошибки всплывут во время выполнения. unknown безопасен: прежде чем использовать значение, его нужно сузить проверками typeof, instanceof или валидацией. Для данных неизвестного происхождения (JSON.parse, ответы API) правильно unknown.',
      },
      {
        q: 'Чем type отличается от interface?',
        a: 'interface описывает форму объекта, поддерживает extends и слияние объявлений (удобно для расширения чужих типов). type — псевдоним для любого типа: объединения, пересечения, кортежи, условные и сопоставленные типы. Для объектов оба взаимозаменяемы в большинстве случаев.',
      },
      {
        q: 'Что такое размеченное объединение?',
        a: 'Объединение объектных типов с общим литеральным полем-дискриминантом (type: \'loading\' | \'success\' | \'error\'). Проверка этого поля сужает тип до конкретного варианта. В switch с веткой default: const x: never = value компилятор проверяет, что обработаны все варианты.',
      },
      {
        q: 'Напишите тип, делающий все поля объекта необязательными рекурсивно.',
        a: 'type DeepPartial<T> = { [K in keyof T]?: T[K] extends object ? DeepPartial<T[K]> : T[K] }. Это сопоставленный тип с условным типом внутри. Для массивов и функций на практике добавляют отдельные ветки.',
      },
      {
        q: 'Гарантирует ли TypeScript, что данные с сервера соответствуют типу?',
        a: 'Нет. Типы стираются при компиляции, и приведение response.json() as User — просто утверждение программиста. Если сервер пришлёт другое, ошибка проявится во время выполнения. Нужна валидация схемой (zod, valibot) на границе приложения, из схемы же можно вывести тип.',
      },
    ],
  },
  {
    id: 'web-perf-security',
    title: 'Производительность и безопасность фронтенда',
    area: WEB,
    level: 'plus',
    minutes: 75,
    summary: 'Core Web Vitals, оптимизация загрузки, XSS, CSRF, CSP.',
    body: `### Метрики
**Core Web Vitals**: LCP (отрисовка самого большого элемента, цель < 2,5 с), INP (задержка реакции на взаимодействие, < 200 мс), CLS (сдвиги макета, < 0,1). Также TTFB, FCP. Инструменты: Lighthouse, вкладка Performance в DevTools, RUM-метрики с реальных пользователей.

### Ускорение загрузки
- Меньше JavaScript: разделение кода (\`import()\`, lazy-компоненты), tree shaking, анализ бандла.
- Сжатие (gzip, brotli), кэширование статики с хешами в именах, CDN.
- Изображения: современные форматы (WebP, AVIF), размеры под экран (\`srcset\`), \`loading="lazy"\`, заданные размеры против сдвигов.
- Критический CSS, шрифты с \`font-display: swap\`, \`preload\`/\`preconnect\`.
- SSR/SSG для быстрой первой отрисовки и SEO.

### Отзывчивость
Длинные задачи (> 50 мс) блокируют главный поток: разбивать работу, выносить в Web Worker, виртуализировать длинные списки, debounce для частых событий, мемоизация в React.

### Безопасность
- **XSS** — внедрение скрипта через пользовательские данные. Защита: экранирование при выводе (React экранирует по умолчанию), избегать \`innerHTML\`/\`dangerouslySetInnerHTML\` с непроверенными данными (санитайзер DOMPurify), **Content Security Policy**, cookie с HttpOnly.
- **CSRF** — сайт злоумышленника отправляет запрос от имени пользователя с его cookie. Защита: \`SameSite\`-cookie, CSRF-токены, проверка \`Origin\`.
- **Clickjacking** — \`X-Frame-Options\` / \`frame-ancestors\` в CSP.
- Зависимости: аудит npm-пакетов, lock-файлы, осторожность с \`postinstall\`.
- Секреты не хранят во фронтенд-коде — всё, что уходит в браузер, публично.`,
    questions: [
      {
        q: 'Что такое Core Web Vitals?',
        a: 'Ключевые метрики пользовательского опыта от Google: LCP — время отрисовки самого крупного элемента первого экрана (скорость загрузки), INP — задержка реакции на действия пользователя (отзывчивость), CLS — суммарные неожиданные сдвиги макета (визуальная стабильность). Влияют на ранжирование в поиске и конверсию.',
      },
      {
        q: 'Как ускорить загрузку тяжёлого SPA?',
        a: 'Разделить бандл по маршрутам и лениво грузить тяжёлые части, убрать лишние зависимости по анализу бандла, включить brotli и долгое кэширование статики с хешами, оптимизировать изображения и шрифты, отдавать первую страницу через SSR или предрендеринг, использовать CDN, preconnect к API, отложить некритичные скрипты.',
      },
      {
        q: 'Что такое XSS и как React защищает от него?',
        a: 'Межсайтовый скриптинг — выполнение чужого JavaScript на странице через внедрённые данные: он может украсть токены, действовать от имени пользователя. React экранирует значения в JSX, поэтому текст из данных не станет разметкой. Уязвимости остаются через dangerouslySetInnerHTML, ссылки javascript: в href, прямую работу с DOM. Дополнительно — CSP и HttpOnly-cookie.',
      },
      {
        q: 'Как защититься от CSRF?',
        a: 'Cookie с SameSite=Lax или Strict — браузер не отправит их в межсайтовых запросах. Для изменяющих запросов — CSRF-токен, который злоумышленник не может получить, или проверка заголовков Origin/Referer. Не использовать GET для изменений. Если аутентификация через заголовок Authorization, а не cookie, классический CSRF невозможен.',
      },
    ],
  },
  {
    id: 'node-basics',
    title: 'Node.js',
    area: WEB,
    level: 'plus',
    minutes: 60,
    summary: 'Event loop Node, модули, потоки, Express/Nest, особенности производительности.',
    body: `### Архитектура
Node.js — V8 + libuv. Один поток выполняет JavaScript, ввод-вывод неблокирующий: сетевые операции через epoll/kqueue, файловые и DNS — в пуле потоков libuv (по умолчанию 4). CPU-тяжёлая работа блокирует всё — выносится в \`worker_threads\` или отдельные сервисы.

### Фазы event loop в Node
timers (\`setTimeout\`) → pending callbacks → poll (I/O) → check (\`setImmediate\`) → close callbacks. Между фазами выполняются \`process.nextTick\` (приоритетнее промисов) и микрозадачи промисов.

### Модули и пакеты
CommonJS и ES-модули (\`"type": "module"\` в package.json). npm/pnpm, lock-файлы, semver (\`^1.2.3\` — совместимые минорные версии).

### Потоки (streams)
Обработка данных частями без загрузки целиком: Readable, Writable, Transform, \`pipeline\` с обработкой ошибок и backpressure (притормаживание источника, если приёмник не успевает). Пример — отдача большого файла или обработка CSV на гигабайты.

### Серверные фреймворки
Express (минимализм, middleware), Fastify (скорость, схемы), NestJS (модули, DI, декораторы — похож на Spring). Ошибки в асинхронных обработчиках нужно явно передавать в обработчик ошибок; необработанные отклонения промисов роняют процесс.

### Эксплуатация
Кластеризация на все ядра (cluster, PM2 или несколько контейнеров), graceful shutdown по SIGTERM, переменные окружения, структурированное логирование, мониторинг задержки event loop.`,
    questions: [
      {
        q: 'Как Node.js обрабатывает много запросов в одном потоке?',
        a: 'JavaScript выполняется в одном потоке, но ввод-вывод неблокирующий: libuv регистрирует операции в механизмах ОС (epoll) и возвращается к циклу событий, а файловые операции выполняет в пуле потоков. Когда операция завершена, колбэк ставится в очередь. Пока обработчики не выполняют долгих синхронных вычислений, один поток обслуживает тысячи соединений.',
      },
      {
        q: 'Чем process.nextTick отличается от setImmediate и Promise.then?',
        a: 'process.nextTick выполняется сразу после текущей операции, раньше промисов и до перехода к следующей фазе цикла. Promise.then — микрозадача, после очереди nextTick. setImmediate — макрозадача фазы check, выполняется после фазы poll текущей итерации. Злоупотребление nextTick может заблокировать ввод-вывод.',
      },
      {
        q: 'Зачем нужны streams?',
        a: 'Чтобы обрабатывать данные частями, не загружая их целиком в память: большие файлы, HTTP-тела, сжатие, парсинг CSV. Потоки соединяются через pipeline, который корректно передаёт ошибки и поддерживает backpressure — источник приостанавливается, если потребитель не успевает.',
      },
      {
        q: 'Как использовать все ядра процессора в Node.js?',
        a: 'Запустить несколько процессов: модуль cluster или PM2 с балансировкой входящих соединений, либо несколько контейнеров за балансировщиком. Для CPU-тяжёлых задач внутри одного процесса — worker_threads. Состояние между процессами хранить во внешних хранилищах (Redis, БД).',
      },
    ],
  },
]
