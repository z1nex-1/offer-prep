import type { Topic } from '../../types'

const P = 'Python'

export const python: Topic[] = [
  {
    id: 'py-types',
    title: 'Типы данных, изменяемость и копирование',
    area: P,
    level: 'base',
    minutes: 75,
    summary: 'Изменяемые и неизменяемые типы, ссылки, копии, сложность операций со встроенными коллекциями.',
    body: `### Всё — объекты и ссылки
Переменная — имя, привязанное к объекту. Присваивание не копирует объект, а создаёт ещё одну ссылку. \`is\` сравнивает идентичность объектов, \`==\` — значения.

### Изменяемые и неизменяемые
- **Неизменяемые**: \`int\`, \`float\`, \`bool\`, \`str\`, \`tuple\`, \`frozenset\`, \`bytes\`. Могут быть ключами словаря (если содержимое тоже хешируемо).
- **Изменяемые**: \`list\`, \`dict\`, \`set\`, \`bytearray\`, пользовательские объекты.

Ловушка с изменяемым аргументом по умолчанию:
\`\`\`python
def add(x, acc=[]):   # список создаётся один раз при определении функции
    acc.append(x)
    return acc
add(1); add(2)        # [1, 2] — «общий» список
# правильно: acc=None, и внутри acc = [] if acc is None else acc
\`\`\`

### Копирование
\`b = a\` — та же ссылка; \`a[:]\`, \`list(a)\`, \`copy.copy(a)\` — поверхностная копия (вложенные объекты общие); \`copy.deepcopy(a)\` — глубокая. Ловушка: \`[[0] * 3] * 3\` — три ссылки на один внутренний список, правильно \`[[0] * 3 for _ in range(3)]\`.

### Сложность операций
| Операция | list | dict / set |
|---|---|---|
| доступ по индексу/ключу | O(1) | O(1) в среднем |
| \`x in\` | O(n) | O(1) в среднем |
| append / add | O(1) амортизированно | O(1) |
| insert(0, x), pop(0) | O(n) | — |
| сортировка | O(n log n) | — |

\`dict\` сохраняет порядок вставки (гарантия с Python 3.7). \`set\` — хеш-таблица без значений.

### Числа и строки
\`int\` без переполнения (длинная арифметика). \`float\` — IEEE 754: \`0.1 + 0.2 != 0.3\`, для денег — \`decimal.Decimal\`. \`//\` — деление с округлением вниз: \`-7 // 2 == -4\`. Строки неизменяемы, Unicode; сборка — через \`''.join(parts)\`, f-строки.

### Кэширование малых объектов
CPython кэширует целые от −5 до 256 и интернирует некоторые строки, поэтому \`a is b\` иногда True для равных значений. Для сравнения значений всегда используйте \`==\`, \`is\` — для \`None\`.`,
    questions: [
      {
        q: 'Какие типы в Python изменяемые, а какие нет? Почему это важно?',
        a: 'Неизменяемые: int, float, bool, str, tuple, frozenset, bytes. Изменяемые: list, dict, set, bytearray, большинство пользовательских объектов. Важно, потому что только хешируемые (обычно неизменяемые) объекты могут быть ключами словаря и элементами множества; изменение изменяемого объекта видно через все ссылки на него; изменяемый аргумент по умолчанию разделяется между вызовами.',
      },
      {
        q: 'Что выведет функция с аргументом по умолчанию acc=[] при нескольких вызовах?',
        a: 'Список создаётся один раз при определении функции и сохраняется в её атрибуте __defaults__, поэтому каждый вызов без acc дописывает в один и тот же список: [1], затем [1, 2]. Правильный шаблон — acc=None и создание нового списка внутри функции.',
      },
      {
        q: 'Чем is отличается от ==?',
        a: '== сравнивает значения через метод __eq__. is проверяет, что это один и тот же объект в памяти (id совпадают). is используют для сравнения с синглтонами: None, True, False. Для чисел и строк is может случайно вернуть True из-за кэширования малых целых и интернирования строк, но полагаться на это нельзя.',
      },
      {
        q: 'Чем поверхностная копия отличается от глубокой?',
        a: 'Поверхностная (copy.copy, срез, list(a), dict.copy()) создаёт новый контейнер, но элементы — те же объекты. Изменение вложенного списка видно в обеих копиях. Глубокая (copy.deepcopy) рекурсивно копирует все вложенные объекты, корректно обрабатывая циклические ссылки.',
      },
      {
        q: 'Почему tuple может быть ключом словаря, а list — нет? Может ли tuple быть нехешируемым?',
        a: 'Ключ должен иметь неизменный хеш. list изменяем, у него нет __hash__. tuple неизменяем, его хеш вычисляется из элементов. Но tuple, содержащий список, например (1, [2]), нехешируем — hash выбросит TypeError.',
      },
      {
        q: 'Какая сложность у операции x in list и x in set?',
        a: 'Для списка — O(n): линейный поиск с сравнением элементов. Для множества и словаря — O(1) в среднем: вычисление хеша и проверка корзины. Поэтому при многократных проверках вхождения список стоит превратить в множество.',
      },
    ],
  },
  {
    id: 'py-functions',
    title: 'Функции, замыкания и декораторы',
    area: P,
    level: 'core',
    minutes: 90,
    summary: 'Аргументы, области видимости LEGB, замыкания, декораторы с параметрами.',
    body: `### Аргументы
\`def f(a, b=2, *args, c, d=4, **kwargs)\`: позиционные, со значением по умолчанию, произвольные позиционные, только именованные (после \`*\`), произвольные именованные. \`/\` в сигнатуре отмечает только позиционные параметры.

### Области видимости: LEGB
Поиск имени: **L**ocal → **E**nclosing (объемлющие функции) → **G**lobal (модуль) → **B**uiltins. Присваивание внутри функции делает имя локальным — чтобы изменить переменную объемлющей функции, нужен \`nonlocal\`, глобальную — \`global\`.

### Замыкания
Вложенная функция запоминает переменные объемлющей области:
\`\`\`python
def counter():
    n = 0
    def inc():
        nonlocal n
        n += 1
        return n
    return inc
\`\`\`
Ловушка позднего связывания: \`[lambda: i for i in range(3)]\` — все лямбды вернут 2, потому что читают \`i\` в момент вызова. Лечится аргументом по умолчанию \`lambda i=i: i\`.

### Декораторы
Функция, принимающая функцию и возвращающая новую. \`@deco\` над определением — это \`f = deco(f)\`.
\`\`\`python
import functools, time

def timed(func):
    @functools.wraps(func)          # сохраняет имя и docstring
    def wrapper(*args, **kwargs):
        t = time.perf_counter()
        try:
            return func(*args, **kwargs)
        finally:
            print(func.__name__, time.perf_counter() - t)
    return wrapper
\`\`\`
**Декоратор с параметрами** — фабрика декораторов, три уровня вложенности:
\`\`\`python
def retry(times):
    def deco(func):
        @functools.wraps(func)
        def wrapper(*a, **kw):
            for i in range(times):
                try:
                    return func(*a, **kw)
                except Exception:
                    if i == times - 1:
                        raise
        return wrapper
    return deco
\`\`\`
Встроенные: \`@property\`, \`@staticmethod\`, \`@classmethod\`, \`@functools.lru_cache\`, \`@dataclass\`.

### Функции как объекты
Функции можно передавать, возвращать, хранить. \`map\`, \`filter\`, \`sorted(key=...)\`, \`functools.partial\`, \`lambda\` — для коротких выражений.`,
    questions: [
      {
        q: 'Что такое замыкание?',
        a: 'Функция, которая запоминает переменные из области видимости, где была создана, и может использовать их после того, как объемлющая функция завершилась. В Python замкнутые переменные хранятся в ячейках (__closure__). Для изменения такой переменной внутри вложенной функции нужен nonlocal.',
      },
      {
        q: 'Как работает декоратор? Напишите декоратор, измеряющий время.',
        a: 'Декоратор — функция, которая получает функцию и возвращает замену; @deco эквивалентно f = deco(f). Внутри определяют wrapper(*args, **kwargs), который делает что-то до и после вызова исходной функции и возвращает её результат. functools.wraps копирует __name__, __doc__ и другие атрибуты. Для времени — time.perf_counter до и после вызова, вывод или логирование разницы в finally.',
      },
      {
        q: 'Как написать декоратор с параметрами, например @rate_limit(max_calls=5, period=60)?',
        a: 'Нужен дополнительный уровень: rate_limit(max_calls, period) возвращает декоратор, который возвращает wrapper. В замыкании хранится deque времён последних вызовов; в wrapper удаляем времена старше period, если осталось max_calls — бросаем исключение или ждём, иначе добавляем текущее время и вызываем функцию. Для многопоточности — threading.Lock вокруг проверки. Задача встречалась на собеседованиях в Касперском.',
        companies: ['kaspersky'],
      },
      {
        q: 'Что такое LEGB?',
        a: 'Порядок поиска имён: Local — локальная область функции, Enclosing — области объемлющих функций, Global — глобальная область модуля, Built-in — встроенные имена. Если имени нет ни в одной — NameError. Присваивание в функции делает имя локальным для всей функции, поэтому чтение до присваивания даёт UnboundLocalError.',
      },
      {
        q: 'Почему [lambda: i for i in range(3)] возвращает функции, которые все возвращают 2?',
        a: 'Замыкания связываются поздно: лямбды хранят ссылку на переменную i, а не её значение в момент создания. Когда их вызывают, цикл уже закончился и i = 2. Решение — зафиксировать значение аргументом по умолчанию: lambda i=i: i, или использовать functools.partial.',
      },
      {
        q: 'Чем staticmethod отличается от classmethod?',
        a: 'staticmethod не получает ни экземпляр, ни класс — это обычная функция в пространстве имён класса. classmethod получает класс первым аргументом (cls) — используется для альтернативных конструкторов (from_json) и учитывает наследование: у подкласса cls будет подклассом.',
      },
    ],
  },
  {
    id: 'py-iter',
    title: 'Итераторы, генераторы, comprehension',
    area: P,
    level: 'core',
    minutes: 75,
    summary: 'Протокол итерации, yield, ленивые вычисления и обработка больших данных.',
    body: `### Протокол итерации
**Итерируемый объект** имеет \`__iter__\`, возвращающий **итератор**. Итератор имеет \`__next__\`, который выдаёт следующий элемент или бросает \`StopIteration\`. Цикл \`for\` вызывает \`iter()\`, затем \`next()\` до исключения. Итератор одноразовый: второй проход ничего не даст.

### Генераторы
Функция с \`yield\` возвращает генератор — итератор, который вычисляет значения лениво и сохраняет состояние между вызовами:
\`\`\`python
def read_lines(path):
    with open(path) as f:
        for line in f:
            yield line.rstrip("\\n")

def top_urls(path, k=10):
    from collections import Counter
    cnt = Counter(line.split()[6] for line in read_lines(path))
    return cnt.most_common(k)
\`\`\`
Файл на 10 ГБ обрабатывается с памятью O(размер счётчика), а не O(файла). Задача такого типа встречалась в Ozon.

- \`yield from gen\` делегирует другому генератору.
- \`send()\` передаёт значение внутрь генератора (основа старых корутин).
- Генераторное выражение \`(x * x for x in xs)\` — ленивое; списковое включение \`[...]\` — сразу создаёт список.

### Comprehension
\`[f(x) for x in xs if cond(x)]\`, \`{k: v for ...}\`, \`{x for ...}\`. Читаемее и быстрее, чем append в цикле. Не злоупотребляйте вложенностью.

### itertools
\`chain\`, \`islice\`, \`groupby\` (группирует **соседние** равные элементы — нужна предварительная сортировка), \`product\`, \`permutations\`, \`combinations\`, \`accumulate\`, \`count\`, \`cycle\`, \`zip_longest\`.

### Встроенные ленивые функции
\`range\`, \`zip\`, \`map\`, \`filter\`, \`enumerate\`, \`reversed\` возвращают ленивые объекты, а не списки.`,
    questions: [
      {
        q: 'Чем итерируемый объект отличается от итератора?',
        a: 'Итерируемый объект умеет выдавать итератор через __iter__ (список, строка, словарь) и может обходиться многократно. Итератор хранит текущую позицию и выдаёт элементы через __next__, пока не бросит StopIteration; сам является итерируемым (__iter__ возвращает self) и одноразовый.',
      },
      {
        q: 'Что такое генератор и чем он полезен?',
        a: 'Функция с yield, которая при вызове возвращает объект-итератор. Значения вычисляются лениво, по одному, а состояние функции (локальные переменные, позиция) сохраняется между next(). Полезен для потоковой обработки больших данных без загрузки в память, бесконечных последовательностей, конвейеров обработки.',
      },
      {
        q: 'Как посчитать топ-10 URL в лог-файле на 10 ГБ?',
        a: 'Читать файл построчно (итерация по файловому объекту уже ленивая), генератором извлекать URL из каждой строки и накапливать в collections.Counter; в конце most_common(10). Память — O(числа уникальных URL). Если уникальных URL слишком много для памяти — внешнее решение: разбить по хешу URL на файлы-партиции и считать каждую отдельно.',
        companies: ['ozon'],
      },
      {
        q: 'Чем [x for x in xs] отличается от (x for x in xs)?',
        a: 'Первое — списковое включение: сразу вычисляет все элементы и создаёт список в памяти. Второе — генераторное выражение: ленивый итератор, элементы вычисляются по требованию, пройти можно один раз. sum(x*x for x in xs) не создаёт промежуточный список.',
      },
      {
        q: 'Что делает yield from?',
        a: 'Делегирует итерацию вложенному генератору или итерируемому объекту: все его значения выдаются наружу, а send и throw пробрасываются во вложенный генератор. Возвращаемое значение вложенного генератора становится результатом выражения yield from.',
      },
    ],
  },
  {
    id: 'py-oop',
    title: 'ООП в Python: классы, магические методы, MRO',
    area: P,
    level: 'core',
    minutes: 75,
    summary: 'Классы, dunder-методы, наследование и MRO, property, dataclasses, slots.',
    body: `### Классы и экземпляры
Атрибуты экземпляра хранятся в \`__dict__\`, атрибуты класса — общие для всех экземпляров. Ловушка: изменяемый атрибут класса (\`items = []\`) разделяется между всеми экземплярами.

### Магические методы
- Создание: \`__new__\` (создаёт объект), \`__init__\` (инициализирует).
- Представление: \`__repr__\` (для разработчика), \`__str__\` (для пользователя).
- Сравнение и хеш: \`__eq__\`, \`__lt__\`, \`__hash__\`. Если определить \`__eq__\`, \`__hash__\` станет None — объект перестанет быть хешируемым, пока не определите \`__hash__\`.
- Контейнеры: \`__len__\`, \`__getitem__\`, \`__contains__\`, \`__iter__\`.
- Вызов: \`__call__\`. Контекстный менеджер: \`__enter__\`, \`__exit__\`.
- Атрибуты: \`__getattr__\` (только если атрибут не найден), \`__getattribute__\` (всегда), \`__setattr__\`.

### Наследование и MRO
Python поддерживает множественное наследование. Порядок поиска методов — **MRO** (C3-линеаризация), смотреть через \`Class.__mro__\`. \`super()\` вызывает следующий класс по MRO, а не обязательно родителя — это важно в ромбовидном наследовании.

### Инкапсуляция
Соглашение: \`_protected\` — «не трогать снаружи», \`__private\` — искажение имени (name mangling: \`_Class__private\`). Настоящей приватности нет. \`@property\` — вычисляемые атрибуты и валидация с синтаксисом обычного поля.

### Современные инструменты
\`@dataclass\` генерирует \`__init__\`, \`__repr__\`, \`__eq__\` (\`frozen=True\` — неизменяемые, \`slots=True\` — экономия памяти). \`__slots__\` запрещает \`__dict__\` и экономит память на миллионах объектов. \`abc.ABC\` и \`@abstractmethod\` — абстрактные классы. \`typing.Protocol\` — структурная типизация (duck typing с проверкой типов).`,
    questions: [
      {
        q: 'Чем __new__ отличается от __init__?',
        a: '__new__ — статический метод, который создаёт и возвращает новый объект (вызывается первым, получает класс). __init__ инициализирует уже созданный объект и ничего не возвращает. __new__ переопределяют редко: для наследования от неизменяемых типов (str, tuple), синглтонов, метаклассов.',
      },
      {
        q: 'Что такое MRO и как работает super() при множественном наследовании?',
        a: 'Method Resolution Order — порядок, в котором Python ищет атрибуты в иерархии классов; вычисляется C3-линеаризацией и доступен через __mro__. super() возвращает прокси к следующему классу в MRO текущего объекта, а не к прямому родителю. Благодаря этому в ромбовидной иерархии каждый метод базового класса вызывается ровно один раз, если все классы используют super().',
      },
      {
        q: 'Чем __str__ отличается от __repr__?',
        a: '__repr__ — однозначное представление для разработчика, в идеале такое, по которому можно воссоздать объект; используется в консоли и отладке. __str__ — читаемое представление для пользователя, используется print и str(). Если __str__ не определён, используется __repr__.',
      },
      {
        q: 'Почему объект с переопределённым __eq__ нельзя положить в set?',
        a: 'Если класс определяет __eq__ без __hash__, Python устанавливает __hash__ = None, чтобы не нарушить контракт: равные объекты обязаны иметь равные хеши. Нужно явно определить __hash__ на основе тех же полей, что участвуют в сравнении, и сделать эти поля неизменяемыми (например, @dataclass(frozen=True)).',
      },
      {
        q: 'Зачем нужны __slots__?',
        a: '__slots__ фиксирует набор атрибутов экземпляра и убирает __dict__: объекты занимают заметно меньше памяти и доступ к атрибутам немного быстрее. Полезно, когда создаются миллионы объектов. Минусы — нельзя добавлять новые атрибуты динамически, есть нюансы с наследованием.',
      },
      {
        q: 'Как сделать атрибут только для чтения?',
        a: 'Через @property без сеттера: значение хранится в _attr, а свойство возвращает его. Для полностью неизменяемого объекта — @dataclass(frozen=True) или namedtuple. Полной защиты нет: при желании можно изменить _attr или object.__setattr__.',
      },
    ],
  },
  {
    id: 'py-internals',
    title: 'Внутренности CPython: память, GC, GIL',
    area: P,
    level: 'plus',
    minutes: 60,
    summary: 'Подсчёт ссылок, сборщик циклов, GIL и его последствия.',
    body: `### Управление памятью
CPython использует **подсчёт ссылок**: у каждого объекта счётчик, при обнулении объект освобождается сразу. Плюс — предсказуемость. Минус — циклические ссылки (объект ссылается сам на себя через цепочку) никогда не обнулятся.

**Сборщик циклов** (модуль \`gc\`) периодически находит недостижимые циклы. Объекты делятся на три поколения: новые объекты проверяются часто, выжившие переходят в старшие поколения и проверяются реже (гипотеза «большинство объектов умирает молодыми»).

Для мелких объектов работает собственный аллокатор (pymalloc) с пулами и аренами. \`sys.getsizeof\` показывает размер объекта без вложенных, \`tracemalloc\` — где выделяется память.

### GIL
**Global Interpreter Lock** — мьютекс, который позволяет только одному потоку выполнять байт-код Python в каждый момент. Он упрощает реализацию интерпретатора и подсчёт ссылок.
- **CPU-bound** задачи в потоках не ускоряются — используйте \`multiprocessing\`, \`concurrent.futures.ProcessPoolExecutor\` или вынос в C-расширения (NumPy отпускает GIL).
- **I/O-bound** задачи в потоках ускоряются: GIL отпускается на время ввода-вывода.
- Начиная с Python 3.13 есть экспериментальная сборка без GIL (free-threaded).

### Байт-код
Исходник компилируется в байт-код (\`.pyc\` в \`__pycache__\`), который исполняет виртуальная машина. \`dis.dis(func)\` показывает инструкции. Операция \`x += 1\` — несколько инструкций, поэтому даже с GIL она не атомарна между потоками.

### Производительность
Локальные переменные быстрее глобальных, встроенные функции и comprehension быстрее ручных циклов, \`''.join\` вместо конкатенации, профилирование через \`cProfile\`.`,
    questions: [
      {
        q: 'Что такое GIL и как он влияет на многопоточность?',
        a: 'Глобальная блокировка интерпретатора в CPython: одновременно байт-код Python выполняет только один поток. Поэтому потоки не ускоряют вычислительные задачи — для них используют процессы (multiprocessing) или библиотеки на C, отпускающие GIL. Для задач ввода-вывода потоки полезны: во время ожидания сети или диска GIL отпускается.',
      },
      {
        q: 'Как в Python освобождается память?',
        a: 'Основной механизм — подсчёт ссылок: объект удаляется, как только на него не остаётся ссылок. Циклические ссылки обрабатывает сборщик мусора с тремя поколениями, который периодически ищет недостижимые циклы. Мелкие объекты выделяются через pymalloc, и освобождённая память может не возвращаться ОС сразу.',
      },
      {
        q: 'Делает ли GIL операцию counter += 1 потокобезопасной?',
        a: 'Нет. counter += 1 компилируется в несколько инструкций байт-кода (загрузить, сложить, сохранить), и поток может быть прерван между ними. GIL гарантирует атомарность отдельных инструкций, а не составных операций. Нужен threading.Lock.',
      },
      {
        q: 'Когда использовать threading, multiprocessing и asyncio?',
        a: 'asyncio — много одновременных сетевых операций в одном потоке (тысячи соединений, запросы к API). threading — I/O-задачи с блокирующими библиотеками, не поддерживающими async. multiprocessing — вычисления на нескольких ядрах в обход GIL, с накладными расходами на процессы и передачу данных (сериализация pickle).',
      },
    ],
  },
  {
    id: 'py-async',
    title: 'Конкурентность: threading, multiprocessing, asyncio',
    area: P,
    level: 'core',
    minutes: 90,
    summary: 'Event loop, корутины, await, gather, блокирующие вызовы в асинхронном коде.',
    body: `### asyncio
Асинхронность в одном потоке: **event loop** переключается между корутинами в точках \`await\`, пока одни ждут ввода-вывода, другие работают.
\`\`\`python
import asyncio, aiohttp

async def fetch(session, url):
    async with session.get(url, timeout=aiohttp.ClientTimeout(total=5)) as r:
        return url, r.status

async def main(urls):
    sem = asyncio.Semaphore(10)              # не больше 10 одновременных запросов
    async with aiohttp.ClientSession() as s:
        async def limited(u):
            async with sem:
                return await fetch(s, u)
        return await asyncio.gather(*(limited(u) for u in urls), return_exceptions=True)

asyncio.run(main(["https://ya.ru"]))
\`\`\`
- \`async def\` определяет корутину; вызов создаёт объект корутины, а не запускает её.
- \`await\` приостанавливает корутину до готовности awaitable.
- \`asyncio.create_task\` запускает корутину конкурентно; \`gather\` ждёт несколько; \`wait_for\` — с таймаутом; \`TaskGroup\` (3.11+) — структурированная конкурентность.
- **Нельзя вызывать блокирующий код** (\`time.sleep\`, \`requests\`, тяжёлые вычисления) внутри корутины — он останавливает весь loop. Используйте \`await asyncio.sleep\`, асинхронные библиотеки или \`asyncio.to_thread\`.

### threading
Потоки ОС, общая память, GIL. Синхронизация: \`Lock\`, \`RLock\`, \`Event\`, \`Condition\`, \`Semaphore\`, потокобезопасная \`queue.Queue\`. \`concurrent.futures.ThreadPoolExecutor\` — удобный пул.

### multiprocessing
Отдельные процессы со своим интерпретатором и GIL: настоящий параллелизм для CPU-задач. Данные передаются через сериализацию (pickle) — накладные расходы. \`ProcessPoolExecutor\`, \`Pool.map\`.

### Где что
- Веб-краулер на 10 000 URL — asyncio с семафором.
- Обработка картинок на всех ядрах — ProcessPoolExecutor.
- Параллельный вызов старой синхронной библиотеки — ThreadPoolExecutor.`,
    questions: [
      {
        q: 'Как работает asyncio?',
        a: 'В одном потоке работает цикл событий, который хранит задачи-корутины. Корутина выполняется до await на операции ввода-вывода, затем отдаёт управление циклу, и тот запускает другую готовую задачу. Когда ввод-вывод завершается (ОС сообщает через селектор), ожидавшая корутина снова становится готовой. Так тысячи операций ожидания обслуживаются одним потоком без блокировок.',
      },
      {
        q: 'Что будет, если в асинхронной функции вызвать time.sleep(5) или requests.get?',
        a: 'Это блокирующие вызовы: они остановят весь поток, а с ним и цикл событий — все остальные корутины будут ждать 5 секунд. Нужно использовать await asyncio.sleep, асинхронный HTTP-клиент (aiohttp, httpx) или вынести блокирующий вызов в поток через asyncio.to_thread / run_in_executor.',
      },
      {
        q: 'Как ограничить число одновременных запросов в asyncio?',
        a: 'asyncio.Semaphore(N): каждая задача выполняет запрос внутри async with sem, и одновременно работают не больше N. Альтернативы — очередь с фиксированным числом воркеров или ограничение пула соединений клиента.',
      },
      {
        q: 'Чем asyncio.gather отличается от последовательных await?',
        a: 'Последовательные await выполняют операции одну за другой: общее время — сумма. gather запускает все корутины конкурентно и ждёт их завершения: время — примерно максимум из длительностей. С return_exceptions=True ошибки возвращаются в результатах, а не прерывают остальное.',
      },
      {
        q: 'Как сделать так, чтобы asyncio-задача не висела бесконечно?',
        a: 'Обернуть ожидание в asyncio.wait_for(coro, timeout) или использовать asyncio.timeout (3.11+). При превышении задача отменяется (CancelledError), а наружу бросается TimeoutError. Также задать таймауты на уровне HTTP-клиента.',
      },
    ],
  },
  {
    id: 'py-stdlib',
    title: 'Стандартная библиотека, исключения, контекстные менеджеры',
    area: P,
    level: 'base',
    minutes: 60,
    summary: 'collections, functools, исключения, with и полезные модули для задач.',
    body: `### collections
- \`Counter\` — подсчёт частот, \`most_common(k)\`.
- \`defaultdict(list)\` — словарь со значением по умолчанию.
- \`deque\` — O(1) добавление и удаление с обоих концов, \`maxlen\` для скользящего окна.
- \`OrderedDict\` — \`move_to_end\`, \`popitem(last=False)\` (LRU-кэш).
- \`namedtuple\` — лёгкие неизменяемые записи.

### Для алгоритмов
\`heapq\` (min-куча), \`bisect\` (бинарный поиск по отсортированному списку), \`itertools\`, \`functools.lru_cache\`/\`cache\` (мемоизация), \`math\`, \`sys.setrecursionlimit\`, быстрый ввод \`sys.stdin.readline\`.

### Исключения
\`\`\`python
try:
    value = int(raw)
except ValueError as e:
    log.warning("bad input: %s", e)
    raise InvalidInput(raw) from e   # цепочка причин
else:
    ...        # если исключения не было
finally:
    ...        # выполняется всегда
\`\`\`
- Ловите конкретные исключения, не голый \`except:\` (он поймает даже \`KeyboardInterrupt\` и \`SystemExit\`).
- Иерархия: \`BaseException\` → \`Exception\` → \`ValueError\`, \`KeyError\` и т. д.
- EAFP («проще попросить прощения») — питоничный стиль: пробуем и ловим исключение, а не проверяем всё заранее.

### Контекстные менеджеры
\`with\` гарантирует освобождение ресурса: файлы, блокировки, соединения, транзакции. Реализуются через \`__enter__\`/\`__exit__\` или декоратор \`contextlib.contextmanager\`:
\`\`\`python
from contextlib import contextmanager
import time

@contextmanager
def timer(name):
    t = time.perf_counter()
    try:
        yield
    finally:
        print(name, time.perf_counter() - t)
\`\`\`
\`__exit__\` получает информацию об исключении и может подавить его, вернув True.

### Типизация и тесты
Аннотации типов (\`list[int]\`, \`dict[str, int]\`, \`Optional\`, \`TypedDict\`, \`Protocol\`) и проверка mypy. Тесты — pytest: фикстуры, параметризация, \`monkeypatch\`, \`unittest.mock\`.`,
    questions: [
      {
        q: 'Что такое контекстный менеджер и как его написать?',
        a: 'Объект для использования с with, который гарантирует выполнение завершающих действий (закрыть файл, отпустить блокировку, откатить транзакцию) даже при исключении. Реализуется классом с __enter__ (возвращает ресурс) и __exit__(exc_type, exc, tb) — освобождает ресурс и может подавить исключение, вернув True. Короче — генератор с yield внутри try/finally и декоратором contextlib.contextmanager.',
      },
      {
        q: 'Чем отличаются блоки else и finally в try?',
        a: 'else выполняется, только если в try не было исключения — туда кладут код, который не должен перехватываться этим except. finally выполняется всегда: при успехе, при исключении и даже при return — для освобождения ресурсов.',
      },
      {
        q: 'Почему плохо писать голый except: или except Exception: pass?',
        a: 'Голый except перехватывает всё, включая KeyboardInterrupt и SystemExit — программу нельзя прервать. except Exception: pass молча проглатывает ошибки, в том числе баги, и они проявятся позже в непонятном месте. Нужно ловить конкретные ожидаемые исключения и хотя бы логировать.',
      },
      {
        q: 'Как в Python сделать мемоизацию?',
        a: 'Декоратор functools.lru_cache(maxsize=...) или functools.cache (без ограничения). Аргументы должны быть хешируемыми. Для рекурсивных ДП-решений это самый быстрый способ превратить экспоненциальную рекурсию в полиномиальную; при большой глубине рекурсии нужно увеличить лимит или перейти к таблице.',
      },
      {
        q: 'Какие структуры из collections вы используете в задачах?',
        a: 'Counter — частоты и top-k; defaultdict(list) — группировка; deque — очередь в BFS и скользящее окно (popleft за O(1)); OrderedDict — LRU-кэш; namedtuple — читаемые записи. Плюс heapq для кучи и bisect для бинарного поиска.',
      },
    ],
  },
  {
    id: 'py-web',
    title: 'Python-бэкенд: веб-фреймворки и работа с БД',
    area: P,
    level: 'plus',
    minutes: 60,
    summary: 'WSGI/ASGI, Django и FastAPI, ORM и проблема N+1, миграции.',
    body: `### WSGI и ASGI
- **WSGI** — синхронный интерфейс между веб-сервером и приложением (Django, Flask). Серверы: gunicorn, uWSGI.
- **ASGI** — асинхронный, поддерживает WebSocket и долгие соединения (FastAPI, Starlette, Django async). Серверы: uvicorn, hypercorn.

### Фреймворки
- **Django** — «батарейки в комплекте»: ORM, админка, миграции, аутентификация, формы. Хорош для продуктов с богатой бизнес-логикой.
- **FastAPI** — асинхронный, типы и Pydantic для валидации, автоматическая OpenAPI-документация, внедрение зависимостей через \`Depends\`.
- **Flask** — минималистичный, всё добавляется расширениями.

### ORM и N+1
ORM удобна, но легко генерирует лишние запросы:
\`\`\`python
for order in Order.objects.all():       # 1 запрос
    print(order.user.name)              # + N запросов
# решение: Order.objects.select_related("user")          (JOIN для FK)
#          prefetch_related("items")                      (отдельный IN-запрос для «многих»)
\`\`\`
В SQLAlchemy — \`joinedload\`, \`selectinload\`. Смотрите реальные SQL (django-debug-toolbar, логирование).

### Миграции
Схема БД меняется версионируемыми миграциями (Django migrations, Alembic). Безопасные изменения на живой базе: добавить nullable-столбец, заполнить, потом добавить ограничение; индексы — \`CREATE INDEX CONCURRENTLY\`.

### Прочее
Пул соединений к БД, фоновые задачи (Celery, RQ, arq) с брокером (Redis, RabbitMQ), кэширование, конфигурация через переменные окружения, логирование, линтеры (ruff), форматирование (black).`,
    questions: [
      {
        q: 'Что такое проблема N+1 и как её решить?',
        a: 'Код делает один запрос за списком объектов, а затем по отдельному запросу на связанный объект для каждого — N+1 запрос вместо одного-двух. Решается жадной загрузкой: select_related (JOIN для внешних ключей) и prefetch_related (второй запрос с IN для связей «многие») в Django, joinedload/selectinload в SQLAlchemy.',
      },
      {
        q: 'Чем WSGI отличается от ASGI?',
        a: 'WSGI — синхронный стандарт: один запрос обрабатывается одним рабочим потоком или процессом от начала до конца, нет поддержки WebSocket. ASGI — асинхронный: приложение — корутина, один процесс обслуживает много соединений, поддерживаются WebSocket и долгие соединения.',
      },
      {
        q: 'Почему FastAPI популярен?',
        a: 'Асинхронность из коробки, валидация и сериализация на Pydantic по аннотациям типов, автоматическая документация OpenAPI и Swagger UI, удобное внедрение зависимостей, высокая производительность на uvicorn. Код получается декларативным и хорошо типизированным.',
      },
      {
        q: 'Как выполнять долгие задачи в веб-приложении?',
        a: 'Не в обработчике запроса: ставить задачу в очередь (Celery, RQ, arq с Redis или RabbitMQ) и сразу отвечать клиенту (202 и id задачи), а воркеры выполняют её в фоне. Статус клиент получает опросом или через WebSocket. Задачи делают идемпотентными и с ретраями.',
      },
    ],
  },
]
