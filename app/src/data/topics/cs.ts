import type { Topic } from '../../types'

const CS = 'Computer Science'
const DB = 'Базы данных'
const ENG = 'Инженерная практика'

export const cs: Topic[] = [
  {
    id: 'cs-networks',
    title: 'Сети: модели OSI и TCP/IP, TCP и UDP, DNS',
    area: CS,
    level: 'core',
    minutes: 90,
    summary: 'Что происходит между компьютерами, когда вы открываете сайт.',
    body: `### Уровни
| TCP/IP | OSI | Что делает | Примеры |
|---|---|---|---|
| Прикладной | 7–5 | Протоколы приложений | HTTP, DNS, gRPC, SMTP |
| Транспортный | 4 | Доставка между процессами (порты) | TCP, UDP, QUIC |
| Сетевой | 3 | Маршрутизация между сетями | IP, ICMP |
| Канальный | 2–1 | Передача в пределах сегмента | Ethernet, Wi-Fi, ARP |

### TCP
- Устанавливает соединение **тройным рукопожатием**: SYN → SYN-ACK → ACK.
- Гарантирует доставку и порядок: нумерация байтов, подтверждения, повторная передача.
- **Управление потоком** (окно получателя) и **перегрузкой** (slow start, congestion window).
- Закрытие — обмен FIN/ACK; состояние TIME_WAIT защищает от «опоздавших» пакетов.
- Проблема **head-of-line blocking**: потерянный пакет задерживает всё, что за ним.

### UDP
Без соединения, без гарантий доставки и порядка, минимальные накладные расходы. Подходит там, где важнее задержка: DNS, видеозвонки, игры, стриминг. QUIC (основа HTTP/3) строит надёжность поверх UDP.

### IP-адреса и порты
IPv4 — 32 бита, IPv6 — 128 бит. Частные диапазоны (10.0.0.0/8, 192.168.0.0/16, 172.16.0.0/12) выходят в интернет через NAT. Порт идентифицирует процесс; соединение TCP определяется четвёркой (IP и порт источника, IP и порт назначения).

### DNS
Превращает имя в IP. Путь запроса: кэш браузера и ОС → рекурсивный резолвер провайдера → корневой сервер → сервер зоны \`.ru\` → авторитативный сервер домена. Записи: A/AAAA (адрес), CNAME (псевдоним), MX (почта), TXT, NS. У записей есть TTL — время кэширования.

### Что происходит при вводе адреса в браузер
1. Разбор URL, проверка кэша и HSTS.
2. DNS-резолвинг.
3. TCP-соединение (или QUIC), затем TLS-рукопожатие.
4. HTTP-запрос, ответ сервера (возможно, через балансировщик, CDN, кэш).
5. Браузер парсит HTML, загружает ресурсы, строит DOM и CSSOM, рендерит страницу.

Это самый популярный «разогревающий» вопрос — отвечайте по уровням и называйте детали, которые знаете уверенно.`,
    questions: [
      {
        q: 'Чем TCP отличается от UDP? Когда что выбрать?',
        a: 'TCP — с установлением соединения, гарантирует доставку, порядок и отсутствие дублей, регулирует скорость. UDP — без соединения, без гарантий, с минимальной задержкой и накладными расходами. TCP — для веба, API, передачи файлов, баз данных. UDP — для DNS, голоса и видео, игр, где устаревший пакет бесполезен, а задержка критична. Надёжность при необходимости строят поверх UDP (QUIC).',
      },
      {
        q: 'Что происходит, когда вы вводите адрес сайта в браузер и нажимаете Enter?',
        a: 'Браузер разбирает URL и проверяет кэши. DNS превращает имя в IP (кэш → резолвер → корневые → TLD → авторитативный сервер). Устанавливается TCP-соединение тройным рукопожатием, поверх него — TLS: согласование версии и шифров, проверка сертификата, выработка сессионных ключей. Отправляется HTTP-запрос; ответ может прийти от CDN, балансировщика или приложения. Браузер парсит HTML, строит DOM и CSSOM, загружает ресурсы, выполняет JS, рассчитывает раскладку и отрисовывает страницу.',
      },
      {
        q: 'Как устанавливается TCP-соединение и зачем три шага?',
        a: 'Клиент отправляет SYN со своим начальным номером последовательности, сервер отвечает SYN-ACK со своим номером и подтверждением, клиент отвечает ACK. Три шага нужны, чтобы обе стороны убедились, что другая сторона получила их начальные номера последовательности, и канал работает в обе стороны. Двух шагов не хватит: сервер не узнает, дошёл ли его SYN.',
      },
      {
        q: 'Как работает DNS и что такое TTL?',
        a: 'DNS — иерархическая распределённая база имён. Клиент спрашивает рекурсивный резолвер, тот при промахе кэша идёт к корневым серверам, затем к серверам зоны верхнего уровня и к авторитативному серверу домена. TTL — время, на которое ответ можно кэшировать. Маленький TTL ускоряет переключение адресов (миграции, отказоустойчивость), но увеличивает нагрузку на DNS.',
      },
      {
        q: 'Что такое NAT и зачем он нужен?',
        a: 'Трансляция сетевых адресов: устройства в частной сети выходят в интернет под одним публичным адресом, а маршрутизатор запоминает соответствие внутренних адресов и портов внешним. Появился из-за нехватки IPv4. Побочный эффект — входящие соединения к устройствам за NAT невозможны без проброса портов.',
      },
      {
        q: 'Что такое head-of-line blocking и как его решает HTTP/3?',
        a: 'В TCP данные доставляются строго по порядку: если потерялся один пакет, все следующие ждут его повторной отправки, даже если относятся к другим запросам (в HTTP/2 все потоки идут в одном TCP-соединении). HTTP/3 работает поверх QUIC на UDP, где у каждого потока свой порядок: потеря пакета блокирует только свой поток.',
      },
    ],
    resources: [
      { title: 'Олифер «Компьютерные сети. Принципы, технологии, протоколы»', url: 'https://www.piter.com/', kind: 'book' },
      { title: 'High Performance Browser Networking', url: 'https://hpbn.co', kind: 'book' },
    ],
  },
  {
    id: 'cs-http',
    title: 'HTTP, HTTPS, REST, gRPC, WebSocket',
    area: CS,
    level: 'base',
    minutes: 90,
    summary: 'Методы, коды ответов, заголовки, кэширование, TLS и стили API.',
    body: `### HTTP-запрос и ответ
Запрос: метод, путь, версия, заголовки, тело. Ответ: код состояния, заголовки, тело. HTTP не хранит состояние — его добавляют cookies и токены.

### Методы
| Метод | Смысл | Безопасный | Идемпотентный |
|---|---|---|---|
| GET | получить | да | да |
| HEAD | только заголовки | да | да |
| POST | создать или выполнить действие | нет | нет |
| PUT | заменить ресурс целиком | нет | да |
| PATCH | частично изменить | нет | не обязательно |
| DELETE | удалить | нет | да |

**Идемпотентность** — повтор запроса даёт тот же результат на сервере. Важно для ретраев: GET, PUT, DELETE можно повторять, POST — только с ключом идемпотентности.

### Коды ответа
- **2xx**: 200 OK, 201 Created, 204 No Content.
- **3xx**: 301/308 навсегда, 302/307 временно, 304 Not Modified (кэш).
- **4xx** — ошибка клиента: 400, 401 (не аутентифицирован), 403 (нет прав), 404, 405, 409 (конфликт), 422, 429 (слишком много запросов).
- **5xx** — ошибка сервера: 500, 502 (плохой ответ от upstream), 503 (недоступен), 504 (таймаут upstream).

### Заголовки, которые спрашивают
\`Content-Type\`, \`Authorization\`, \`Cookie\`/\`Set-Cookie\`, \`Cache-Control\`, \`ETag\`/\`If-None-Match\`, \`Location\`, \`Origin\` и CORS-заголовки, \`Accept\`.

### Версии
- HTTP/1.1 — keep-alive, одно соединение обрабатывает запросы последовательно.
- HTTP/2 — бинарный, мультиплексирование потоков в одном соединении, сжатие заголовков.
- HTTP/3 — поверх QUIC (UDP), без head-of-line blocking на уровне транспорта.

### HTTPS и TLS
TLS шифрует и аутентифицирует канал. Рукопожатие: клиент и сервер согласуют параметры, сервер предъявляет сертификат (подписанный центром сертификации), стороны вырабатывают общий сессионный ключ (обмен Диффи — Хеллмана), дальше данные шифруются симметрично. Асимметрия — только для аутентификации и обмена ключами.

### Стили API
- **REST** — ресурсы и HTTP-методы, JSON, кэшируемость. Просто и повсеместно.
- **gRPC** — вызов удалённых процедур поверх HTTP/2, контракт в Protobuf, бинарный формат, стриминг. Быстрее и строже, популярен между микросервисами.
- **GraphQL** — клиент сам описывает, какие поля нужны; одна точка входа.
- **WebSocket** — постоянное двустороннее соединение после Upgrade-запроса: чаты, котировки, совместное редактирование. Альтернативы — Server-Sent Events (сервер → клиент), long polling.`,
    questions: [
      {
        q: 'Чем отличаются методы PUT, PATCH и POST?',
        a: 'POST создаёт ресурс или выполняет действие, повтор создаст ещё один — не идемпотентен. PUT заменяет ресурс целиком по известному адресу — повтор даёт тот же результат, идемпотентен. PATCH частично изменяет ресурс; идемпотентность зависит от операции (установить поле — да, увеличить счётчик — нет).',
      },
      {
        q: 'Что такое идемпотентность и зачем она нужна?',
        a: 'Свойство операции, при котором повтор не меняет результат на сервере после первого выполнения. Нужна для безопасных повторов: сеть ненадёжна, клиент может не получить ответ и повторить запрос. Для неидемпотентных операций (платёж) используют ключ идемпотентности: сервер запоминает результат по ключу и при повторе возвращает его, не выполняя операцию снова.',
      },
      {
        q: 'Чем 401 отличается от 403? 502 от 504?',
        a: '401 — клиент не аутентифицирован (нет или неверный токен), 403 — аутентифицирован, но не имеет прав. 502 — прокси получил от вышестоящего сервера некорректный ответ или соединение сорвалось; 504 — прокси не дождался ответа за отведённое время.',
      },
      {
        q: 'Как работает HTTPS?',
        a: 'Поверх TCP выполняется TLS-рукопожатие. Сервер предъявляет сертификат с открытым ключом, подписанный доверенным центром сертификации; браузер проверяет цепочку, срок и имя домена. Стороны по алгоритму Диффи — Хеллмана (ECDHE) вырабатывают общий секрет, из него — симметричные ключи сессии. Дальше трафик шифруется быстрым симметричным шифром (AES-GCM, ChaCha20) с проверкой целостности.',
      },
      {
        q: 'Как работает кэширование в HTTP?',
        a: 'Сервер управляет кэшем заголовками. Cache-Control: max-age — сколько секунд ответ свежий; no-cache — можно хранить, но нужно перепроверять; no-store — не хранить; private/public — кто может кэшировать. Перепроверка (условный запрос): клиент отправляет If-None-Match с ETag или If-Modified-Since, сервер отвечает 304 Not Modified без тела, если ресурс не изменился.',
      },
      {
        q: 'REST или gRPC — когда что?',
        a: 'REST — для публичных API, браузерных клиентов, когда важны простота, читаемость JSON и HTTP-кэширование. gRPC — для взаимодействия микросервисов: строгий контракт в Protobuf с генерацией кода, компактный бинарный формат, HTTP/2 с мультиплексированием, двунаправленный стриминг. Минусы gRPC — сложнее отлаживать и ограниченная поддержка в браузере.',
      },
      {
        q: 'Как работает WebSocket и чем он лучше опроса сервера?',
        a: 'Клиент отправляет обычный HTTP-запрос с заголовками Upgrade: websocket, сервер отвечает 101 Switching Protocols, и соединение становится постоянным двусторонним каналом с лёгкими фреймами. В отличие от polling, нет постоянных запросов впустую, и сервер может сам отправлять данные с минимальной задержкой. Нужно учитывать переподключения, heartbeat и масштабирование: соединение привязано к конкретному серверу.',
      },
    ],
  },
  {
    id: 'cs-os',
    title: 'Операционные системы: процессы, потоки, память',
    area: CS,
    level: 'core',
    minutes: 90,
    summary: 'Процессы и потоки, планировщик, виртуальная память, системные вызовы.',
    body: `### Процесс и поток
- **Процесс** — исполняемая программа со своим адресным пространством, дескрипторами файлов, окружением. Процессы изолированы друг от друга.
- **Поток** — единица исполнения внутри процесса. Потоки одного процесса делят память и ресурсы, но имеют свои стек и регистры.
- Переключение между потоками дешевле, чем между процессами, но общая память требует синхронизации.
- **Корутины / горутины / зелёные потоки** — лёгкие единицы, которые планирует рантайм языка поверх небольшого числа потоков ОС.

### Жизненный цикл процесса в Linux
\`fork()\` создаёт копию процесса (с copy-on-write), \`exec()\` заменяет программу, \`wait()\` забирает статус завершения потомка. Процесс-**зомби** — завершился, но родитель не вызвал wait. **Сирота** — родитель умер, процесс переходит к init/systemd.

### Планирование
Планировщик делит процессорное время между готовыми потоками (в Linux — CFS/EEVDF). Состояния: выполняется, готов, ожидает (I/O, блокировка). Переключение контекста сохраняет регистры и переключает таблицы страниц — это накладные расходы.

### Виртуальная память
Каждый процесс видит своё виртуальное адресное пространство. Таблицы страниц отображают виртуальные страницы (обычно 4 КБ) на физические. **Page fault** — обращение к странице, которой нет в памяти: ОС подгружает её (с диска или выделяет новую). **TLB** кэширует трансляции адресов. Swap — вытеснение страниц на диск.

Сегменты процесса: код, данные, **куча** (динамическая память, растёт вверх), **стек** (локальные переменные и кадры вызовов, растёт вниз).

### Системные вызовы и ввод-вывод
Программа просит ядро о ресурсах через системные вызовы: \`open\`, \`read\`, \`write\`, \`mmap\`, \`socket\`. Переход в режим ядра дорогой — поэтому буферизация. Модели I/O: блокирующий, неблокирующий, мультиплексирование (\`select\`, \`epoll\`) — на нём построены nginx, Node.js, Go.

### Межпроцессное взаимодействие
Каналы (pipe), сокеты, разделяемая память, сигналы, очереди сообщений, файлы.`,
    questions: [
      {
        q: 'Чем процесс отличается от потока?',
        a: 'Процесс — изолированная единица с собственным адресным пространством и ресурсами ОС. Поток — единица исполнения внутри процесса: потоки одного процесса разделяют память, открытые файлы, но имеют собственные стек и регистры. Потоки дешевле создавать и переключать, обмениваются данными через общую память, но из-за этого нужны синхронизация и аккуратность: ошибка в одном потоке может уронить весь процесс.',
      },
      {
        q: 'Что такое виртуальная память и зачем она нужна?',
        a: 'Абстракция, при которой каждый процесс видит собственное непрерывное адресное пространство, а ОС с помощью таблиц страниц отображает его на физическую память. Даёт изоляцию процессов, позволяет использовать больше памяти, чем есть физически (swap), делить страницы между процессами (разделяемые библиотеки, copy-on-write после fork) и отображать файлы в память.',
      },
      {
        q: 'Чем стек отличается от кучи?',
        a: 'Стек — область для кадров вызовов функций: локальные переменные, адреса возврата. Выделение мгновенное (сдвиг указателя), освобождается автоматически при выходе из функции, размер ограничен (обычно мегабайты), своя у каждого потока. Куча — для динамической памяти произвольного размера и времени жизни; выделение дороже, освобождение вручную или сборщиком мусора, общая для потоков процесса.',
      },
      {
        q: 'Что такое процесс-зомби?',
        a: 'Процесс, который завершился, но запись о нём осталась в таблице процессов, потому что родитель ещё не прочитал код завершения через wait. Зомби не потребляет ресурсов, кроме записи, но их накопление может исчерпать лимит PID. Лечится исправлением родителя (обработка SIGCHLD) или его завершением — тогда зомби «усыновляет» init и убирает.',
      },
      {
        q: 'Что такое переключение контекста и почему оно дорогое?',
        a: 'Сохранение состояния текущего потока (регистры, счётчик команд) и восстановление состояния другого. При переключении между процессами ещё меняются таблицы страниц, что сбрасывает TLB. Кроме прямых затрат, страдают кэши процессора — новый поток работает с другими данными. Поэтому тысячи потоков ОС на тысячи соединений неэффективны, и используют event loop или лёгкие горутины.',
      },
      {
        q: 'Как nginx или Node.js обслуживают десятки тысяч соединений небольшим числом потоков?',
        a: 'Через неблокирующий ввод-вывод и мультиплексирование: системный вызов epoll (в Linux) сообщает, какие сокеты готовы к чтению или записи. Один поток в цикле событий обрабатывает только готовые соединения и никогда не блокируется на ожидании. Тяжёлые вычисления в таком цикле блокируют всех, поэтому их выносят в пул потоков.',
      },
    ],
    resources: [{ title: 'Operating Systems: Three Easy Pieces', url: 'https://pages.cs.wisc.edu/~remzi/OSTEP/', kind: 'book' }],
  },
  {
    id: 'cs-concurrency',
    title: 'Многопоточность и синхронизация',
    area: CS,
    level: 'core',
    minutes: 90,
    summary: 'Гонки, мьютексы, взаимоблокировки, атомарные операции, модели конкурентности.',
    body: `### Проблемы общей памяти
- **Гонка данных (data race)**: два потока обращаются к одной переменной, хотя бы один пишет, без синхронизации. Результат \`counter++\` из двух потоков может «потерять» инкременты — это чтение, изменение и запись.
- **Состояние гонки (race condition)**: результат зависит от порядка выполнения, даже если каждое обращение атомарно («проверил, что файла нет, → создал» — между шагами файл создал другой).

### Примитивы
- **Мьютекс** — только один поток в критической секции.
- **RW-lock** — много читателей или один писатель.
- **Семафор** — счётчик разрешений: не больше N потоков одновременно (пул соединений).
- **Условная переменная** — ждать события под мьютексом.
- **Атомарные операции** (CAS — compare-and-swap) — основа lock-free структур и счётчиков.
- **Каналы / очереди** — обмен сообщениями вместо общей памяти («не общайтесь через общую память, делитесь памятью через общение» — девиз Go).

### Взаимоблокировка (deadlock)
Четыре условия Коффмана: взаимное исключение, удержание и ожидание, отсутствие принудительного освобождения, циклическое ожидание. Классический пример — два потока берут два мьютекса в разном порядке. Лечится **единым порядком захвата**, таймаутами, \`tryLock\`.

Родственники: **livelock** (потоки активно уступают друг другу и не продвигаются), **starvation** (поток никогда не получает ресурс).

### Видимость и переупорядочивание
Компилятор и процессор переупорядочивают операции, значения могут залипать в кэшах ядер. Модели памяти языков (Java Memory Model, Go memory model, C++ memory_order) определяют, когда запись одного потока видна другому. Синхронизация (мьютекс, volatile в Java, атомики) создаёт отношение happens-before.

### Параллелизм и конкурентность
**Конкурентность** — структура программы, в которой несколько задач продвигаются, чередуясь (даже на одном ядре). **Параллелизм** — одновременное выполнение на нескольких ядрах. Для I/O-задач достаточно конкурентности (async/await, event loop), для вычислений нужен параллелизм.

### Практика
Пул потоков вместо создания потока на задачу, неизменяемые данные, минимальная критическая секция, детекторы гонок (\`go test -race\`, ThreadSanitizer).`,
    questions: [
      {
        q: 'Почему counter++ из нескольких потоков даёт неверный результат?',
        a: 'Инкремент — это три операции: прочитать значение, прибавить единицу, записать. Два потока могут прочитать одно и то же значение, оба прибавят единицу и запишут — один инкремент потеряется. Решения: мьютекс вокруг операции, атомарный инкремент (AtomicInteger, atomic.AddInt64, std::atomic) или сведение подсчёта к одному потоку.',
      },
      {
        q: 'Что такое deadlock и как его избежать?',
        a: 'Взаимоблокировка — потоки бесконечно ждут ресурсы, удерживаемые друг другом. Возникает при одновременном выполнении четырёх условий: взаимное исключение, удержание и ожидание, невозможность отобрать ресурс, циклическое ожидание. Практически избегают, устанавливая глобальный порядок захвата блокировок, захватывая всё сразу, используя tryLock с таймаутом, уменьшая число блокировок.',
      },
      {
        q: 'Чем мьютекс отличается от семафора?',
        a: 'Мьютекс даёт эксклюзивный доступ одному потоку и обычно должен освобождаться тем же потоком, который его захватил. Семафор — счётчик разрешений: пропускает до N потоков одновременно и может освобождаться любым потоком. Семафор с N = 1 похож на мьютекс, но без понятия владельца. Семафоры используют для ограничения параллелизма, например числа одновременных запросов к внешнему API.',
      },
      {
        q: 'Чем конкурентность отличается от параллелизма?',
        a: 'Конкурентность — способ организации программы, когда несколько задач находятся в процессе выполнения и чередуются; возможна на одном ядре. Параллелизм — фактическое одновременное выполнение на нескольких ядрах. Асинхронный веб-сервер на одном потоке конкурентен, но не параллелен; расчёт матриц в нескольких потоках — параллелен.',
      },
      {
        q: 'Что такое CAS и где он используется?',
        a: 'Compare-and-swap — атомарная инструкция процессора: записать новое значение, только если текущее равно ожидаемому, и вернуть результат. На ней строят атомарные счётчики, lock-free очереди и стеки, оптимистичные блокировки: прочитали значение, вычислили новое, попытались записать через CAS, при неудаче повторили. Проблема ABA решается версионированием.',
      },
      {
        q: 'Что такое проблема видимости и как её решает volatile в Java?',
        a: 'Запись одного потока может не стать видимой другому: значение осталось в кэше ядра или регистре, а компилятор переставил операции. volatile гарантирует, что чтение и запись идут через основную память и не переупорядочиваются относительно друг друга, устанавливая happens-before. Но volatile не делает составные операции атомарными: volatile count++ всё равно гонка.',
      },
    ],
  },
  {
    id: 'cs-db',
    title: 'Реляционные базы: индексы, транзакции, изоляция',
    area: DB,
    level: 'core',
    minutes: 120,
    summary: 'B-деревья, планы запросов, ACID, уровни изоляции, нормализация.',
    body: `### Индексы
Индекс — дополнительная структура для быстрого поиска. Стандарт — **B-tree** (B+-дерево): поиск, диапазоны и сортировка за O(log n).
- **Составной индекс** \`(a, b)\` работает для условий по \`a\` и по \`a, b\`, но не по одному \`b\` — правило самого левого префикса.
- **Покрывающий индекс** содержит все нужные запросу столбцы — таблицу читать не нужно (index-only scan).
- **Hash-индекс** — только равенство. **GIN** — массивы, JSON, полнотекстовый поиск. **GiST** — геоданные, диапазоны.
- Индекс ускоряет чтение, но **замедляет запись** и занимает место. Индекс не поможет, если на столбце функция (\`WHERE lower(email) = ...\` без функционального индекса) или низкая селективность.

Смотрите план: \`EXPLAIN ANALYZE\` — Seq Scan, Index Scan, Bitmap Heap Scan, Nested Loop / Hash Join / Merge Join.

### Транзакции и ACID
- **Atomicity** — всё или ничего.
- **Consistency** — ограничения целостности соблюдены до и после.
- **Isolation** — параллельные транзакции не мешают друг другу (в той или иной степени).
- **Durability** — зафиксированное не пропадёт (журнал упреждающей записи, WAL).

### Аномалии и уровни изоляции
| Уровень | Грязное чтение | Неповторяющееся чтение | Фантомы |
|---|---|---|---|
| Read Uncommitted | возможно | возможно | возможно |
| Read Committed | — | возможно | возможно |
| Repeatable Read | — | — | возможно (в PostgreSQL — нет) |
| Serializable | — | — | — |

- **Грязное чтение** — видим незафиксированные изменения чужой транзакции.
- **Неповторяющееся чтение** — повторное чтение строки даёт другое значение.
- **Фантом** — повторный запрос с условием возвращает новые строки.
- **Потерянное обновление** и **write skew** — лечатся \`SELECT ... FOR UPDATE\`, Serializable или оптимистичной блокировкой (версия строки).

PostgreSQL по умолчанию — Read Committed, MySQL InnoDB — Repeatable Read. Реализация — **MVCC**: у строк есть версии, читатели не блокируют писателей.

### Нормализация
- **1НФ** — атомарные значения, нет повторяющихся групп.
- **2НФ** — неключевые атрибуты зависят от всего составного ключа.
- **3НФ** — нет транзитивных зависимостей (неключевые атрибуты зависят только от ключа).
Денормализация — осознанное дублирование ради скорости чтения (витрины, счётчики).

### Ключи и связи
Первичный ключ, внешний ключ (ссылочная целостность), один-ко-многим, многие-ко-многим через связующую таблицу. Суррогатные ключи (id) против естественных (ИНН).`,
    questions: [
      {
        q: 'Как устроен индекс B-tree и почему он ускоряет поиск?',
        a: 'Это сбалансированное сильноветвящееся дерево: каждый узел — страница с отсортированными ключами и ссылками на дочерние узлы, листья содержат ключи и указатели на строки и связаны между собой. Высота маленькая (3–4 уровня на миллионы строк), поэтому поиск — несколько чтений страниц, O(log n). Связанные листья позволяют быстро читать диапазоны и выдавать данные отсортированными.',
      },
      {
        q: 'Есть индекс (city, age). Какие запросы он ускорит?',
        a: 'Условия по city, по city и age (равенство city и диапазон по age), сортировку ORDER BY city, age. Не поможет условию только по age — нарушено правило левого префикса (кроме редких случаев skip scan). Порядок столбцов выбирают по селективности и типичным запросам: столбец с равенством — первым, с диапазоном — последним.',
      },
      {
        q: 'Почему индекс может не использоваться?',
        a: 'Низкая селективность — запрос возвращает большую долю таблицы, и последовательное чтение дешевле. Функция или приведение типа на столбце (WHERE date(created_at) = …). LIKE с ведущим подстановочным знаком (%abc). Условие не по левому префиксу составного индекса. Устаревшая статистика у планировщика. OR по разным столбцам без подходящих индексов.',
      },
      {
        q: 'Расскажите про уровни изоляции транзакций.',
        a: 'Read Uncommitted допускает грязное чтение. Read Committed видит только зафиксированные данные, но повторное чтение может дать другой результат. Repeatable Read гарантирует, что прочитанные строки не изменятся в рамках транзакции, но возможны фантомы (в PostgreSQL снапшот защищает и от них). Serializable даёт результат, эквивалентный последовательному выполнению, ценой откатов при конфликтах. Чем выше уровень, тем больше гарантий и ниже параллелизм.',
      },
      {
        q: 'Что такое MVCC?',
        a: 'Многоверсионное управление конкурентным доступом: при изменении строки создаётся новая версия, а старая остаётся видимой транзакциям, начавшимся раньше. Каждая транзакция читает согласованный снимок данных. Читатели не блокируют писателей и наоборот. Цена — хранение старых версий и их уборка (VACUUM в PostgreSQL).',
      },
      {
        q: 'Как предотвратить потерянное обновление при одновременном изменении баланса?',
        a: 'Варианты: атомарный UPDATE balance = balance - 100 WHERE id = 1 AND balance >= 100 (проверка и изменение в одном операторе); пессимистичная блокировка SELECT ... FOR UPDATE внутри транзакции; оптимистичная блокировка — столбец version и UPDATE ... WHERE version = прочитанной, при нуле обновлённых строк повторить; уровень Serializable с повтором транзакции при конфликте.',
      },
      {
        q: 'Что такое нормализация и когда от неё отказываются?',
        a: 'Приведение схемы к нормальным формам, чтобы каждый факт хранился в одном месте: нет аномалий вставки, обновления и удаления. Денормализуют ради производительности чтения: хранят посчитанные агрегаты, дублируют поля, чтобы избежать дорогих JOIN, строят витрины для аналитики. Цена — нужно поддерживать согласованность копий.',
      },
    ],
    resources: [
      { title: 'Use The Index, Luke — про индексы', url: 'https://use-the-index-luke.com/', kind: 'article' },
      { title: 'Документация PostgreSQL: изоляция транзакций', url: 'https://postgrespro.ru/docs/postgresql/current/transaction-iso', kind: 'docs' },
    ],
  },
  {
    id: 'cs-sql',
    title: 'SQL: JOIN, агрегаты, подзапросы, оконные функции',
    area: DB,
    level: 'base',
    minutes: 150,
    summary: 'Всё, что нужно для SQL-задач на контестах и собеседованиях.',
    body: `### Порядок выполнения запроса
\`FROM / JOIN → WHERE → GROUP BY → HAVING → SELECT (в т. ч. оконные функции) → DISTINCT → ORDER BY → LIMIT\`.
Отсюда правила: алиасы из SELECT нельзя использовать в WHERE; условие на агрегат пишется в HAVING; оконные функции нельзя фильтровать в WHERE — только во внешнем запросе или CTE.

### JOIN
- **INNER** — только пары, где условие выполнено.
- **LEFT** — все строки слева, справа NULL, если пары нет. Поиск «записей без пары»: \`LEFT JOIN ... WHERE right.id IS NULL\`.
- **FULL** — все строки обеих таблиц. **CROSS** — декартово произведение.
- **Self join** — таблица с собой: сотрудник и руководитель.
- Ловушка: условие на правую таблицу в WHERE превращает LEFT JOIN в INNER. Такие условия пишут в ON.
- Ловушка: JOIN «один ко многим» размножает строки — суммы удваиваются. Агрегируйте до соединения.

### Агрегаты и NULL
\`COUNT(*)\` считает строки, \`COUNT(col)\` — непустые значения, \`COUNT(DISTINCT col)\` — уникальные. \`SUM\`, \`AVG\` игнорируют NULL. Сравнение с NULL через \`=\` всегда даёт NULL — используйте \`IS NULL\`, \`COALESCE\`. Целочисленное деление: \`1 / 2 = 0\` — умножайте на \`1.0\`.

### Подзапросы и CTE
Подзапрос в WHERE (\`IN\`, \`EXISTS\`), в FROM (производная таблица), коррелированный (ссылается на внешний запрос). CTE (\`WITH\`) делает запрос читаемым; \`WITH RECURSIVE\` обходит иерархии.
\`NOT IN\` с NULL в подзапросе возвращает пустоту — безопаснее \`NOT EXISTS\`.

### Оконные функции
Считают значение по «окну» строк, не схлопывая их:
\`\`\`sql
SELECT user_id, created_at, amount,
  ROW_NUMBER() OVER (PARTITION BY user_id ORDER BY created_at) AS n,
  SUM(amount) OVER (PARTITION BY user_id ORDER BY created_at) AS running_total,
  LAG(created_at) OVER (PARTITION BY user_id ORDER BY created_at) AS prev_order
FROM orders;
\`\`\`
- Ранжирование: \`ROW_NUMBER\` (1,2,3,4), \`RANK\` (1,2,2,4), \`DENSE_RANK\` (1,2,2,3).
- Смещение: \`LAG\`, \`LEAD\`, \`FIRST_VALUE\`, \`LAST_VALUE\`.
- Агрегаты по окну: \`SUM/AVG/COUNT OVER\`, рамка \`ROWS BETWEEN 6 PRECEDING AND CURRENT ROW\` — скользящее среднее за 7 строк.

### Типовые задачи
Топ-N в группе, первая/последняя покупка, накопительный итог, доля от общего, retention и когорты, воронки через \`COUNT(DISTINCT CASE WHEN ...)\`, «острова» подряд идущих дней через разницу даты и ROW_NUMBER, дубликаты через \`GROUP BY ... HAVING COUNT(*) > 1\`.`,
    questions: [
      {
        q: 'Чем WHERE отличается от HAVING?',
        a: 'WHERE фильтрует строки до группировки и не может использовать агрегатные функции. HAVING фильтрует группы после GROUP BY и работает с агрегатами. Условия, не зависящие от агрегатов, лучше писать в WHERE — меньше строк попадёт в группировку.',
      },
      {
        q: 'Чем отличаются ROW_NUMBER, RANK и DENSE_RANK?',
        a: 'Все нумеруют строки внутри окна по порядку. ROW_NUMBER даёт уникальные номера даже при равенстве значений (1, 2, 3, 4). RANK даёт равным одинаковый номер и пропускает следующие (1, 2, 2, 4). DENSE_RANK не пропускает (1, 2, 2, 3). Для «второй по величине уникальной зарплаты» нужен DENSE_RANK.',
      },
      {
        q: 'Как найти вторую по величине зарплату?',
        a: 'SELECT DISTINCT salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET 1. Или через DENSE_RANK() OVER (ORDER BY salary DESC) = 2. Или MAX(salary) WHERE salary < (SELECT MAX(salary) ...). Важно учесть одинаковые зарплаты и случай, когда второй зарплаты нет (вернуть NULL).',
      },
      {
        q: 'Почему после LEFT JOIN строки исчезают, если в WHERE есть условие на правую таблицу?',
        a: 'Для строк без пары значения правой таблицы — NULL, и условие вроде WHERE o.status = \'completed\' для них даёт NULL, то есть строка отфильтровывается. LEFT JOIN фактически превращается в INNER. Если нужно сохранить все строки левой таблицы, условие на правую переносят в ON.',
      },
      {
        q: 'Как найти пользователей, у которых нет заказов?',
        a: 'LEFT JOIN orders ON orders.user_id = users.id WHERE orders.id IS NULL; или WHERE NOT EXISTS (SELECT 1 FROM orders WHERE orders.user_id = users.id). NOT IN с подзапросом опасен: если в подзапросе встретится NULL, результат будет пустым.',
      },
      {
        q: 'Чем COUNT(*) отличается от COUNT(column)?',
        a: 'COUNT(*) считает все строки группы. COUNT(column) — только строки, где column не NULL. COUNT(DISTINCT column) — количество различных непустых значений. Это часто используют в LEFT JOIN: COUNT(o.id) даст 0 для пользователей без заказов, а COUNT(*) — 1.',
      },
      {
        q: 'Как посчитать накопительный итог выручки по дням?',
        a: 'Сначала агрегировать выручку по дням, затем SUM(revenue) OVER (ORDER BY day). По умолчанию при ORDER BY окно — от начала до текущей строки (RANGE UNBOUNDED PRECEDING, с учётом одинаковых значений сортировки). Для отдельных накоплений по пользователям добавить PARTITION BY user_id.',
      },
      {
        q: 'Как найти пользователей, заходивших несколько дней подряд?',
        a: 'Задача «островов»: оставить уникальные дни пользователя, пронумеровать их ROW_NUMBER по дате и вычесть номер из даты. Для подряд идущих дней разность одинакова — это идентификатор серии. Затем GROUP BY user_id, серия и HAVING COUNT(*) >= N.',
      },
    ],
    resources: [
      { title: 'SQL-тренажёр sql-ex.ru', url: 'https://sql-ex.ru', kind: 'practice' },
      { title: 'Оконные функции в PostgreSQL', url: 'https://postgrespro.ru/docs/postgresql/current/tutorial-window', kind: 'docs' },
    ],
  },
  {
    id: 'cs-nosql',
    title: 'NoSQL, репликация, шардирование, CAP',
    area: DB,
    level: 'plus',
    minutes: 90,
    summary: 'Когда реляционной базы мало и как распределять данные.',
    body: `### Виды NoSQL
- **Key-value** (Redis, Memcached): кэш, сессии, счётчики, очереди. Redis хранит данные в памяти, поддерживает структуры (списки, множества, sorted sets), TTL, персистентность.
- **Документные** (MongoDB): JSON-документы с гибкой схемой.
- **Колоночные / широкие строки** (Cassandra, HBase): огромные объёмы записи, запросы по ключу партиции.
- **Колоночные аналитические** (ClickHouse): быстрые агрегации по миллиардам строк, OLAP.
- **Графовые** (Neo4j): связи, рекомендации, антифрод.
- **Поисковые** (Elasticsearch, OpenSearch): полнотекстовый поиск, логи.

### Репликация
Копии данных на нескольких узлах.
- **Leader–follower** (master–replica): запись на лидер, чтение с реплик. Асинхронная репликация быстрее, но реплики отстают (replication lag) — можно прочитать устаревшее.
- **Синхронная** — надёжнее, медленнее запись.
- **Multi-leader / leaderless** (Cassandra, кворумы \`R + W > N\`) — запись в несколько узлов, разрешение конфликтов.

### Шардирование (партиционирование)
Разделение данных по узлам по ключу шардирования.
- **По диапазону** — удобно для диапазонных запросов, но возможны горячие шарды.
- **По хешу** — равномерно, но диапазоны разбросаны.
- **Консистентное хеширование** — при добавлении узла перемещается малая часть ключей.
Проблемы: запросы через несколько шардов, распределённые транзакции, перебалансировка, выбор ключа.

### CAP и PACELC
При сетевом разделении (**P**artition) система выбирает между согласованностью (**C**onsistency — все видят последнюю запись) и доступностью (**A**vailability — каждый запрос получает ответ). PACELC добавляет: без разделения выбираем между задержкой и согласованностью. Банковские остатки — CP, лента соцсети — AP с конечной согласованностью (eventual consistency).

### Кэширование
Паттерны: cache-aside (приложение читает кэш, при промахе — БД и кладёт в кэш), write-through, write-behind. Проблемы: инвалидация, «лавина» при одновременном истечении ключей, прогрев, согласованность с БД.`,
    questions: [
      {
        q: 'Чем репликация отличается от шардирования?',
        a: 'Репликация — копирование одних и тех же данных на несколько узлов: отказоустойчивость и масштабирование чтения. Шардирование — разделение данных на части по разным узлам: масштабирование объёма и записи. В больших системах применяют оба: каждый шард реплицируется.',
      },
      {
        q: 'Что говорит теорема CAP?',
        a: 'Распределённая система при сетевом разделении не может одновременно гарантировать согласованность (любое чтение видит последнюю запись) и доступность (каждый запрос к живому узлу получает ответ). Разделение сети неизбежно, поэтому выбор на практике — между CP и AP поведением в момент сбоя. Без сбоев выбор между задержкой и согласованностью (PACELC).',
      },
      {
        q: 'Как выбрать ключ шардирования?',
        a: 'Ключ должен равномерно распределять данные и нагрузку (без горячих шардов) и совпадать с основными запросами, чтобы они шли в один шард. Например, для чатов — chat_id, чтобы история чата лежала в одном месте; для заказов — user_id. Плохой выбор — монотонно растущий ключ при шардировании по диапазону: вся запись идёт в последний шард.',
      },
      {
        q: 'Что такое replication lag и как с ним жить?',
        a: 'Задержка применения изменений на асинхронных репликах. Пользователь может сделать запись и не увидеть её, прочитав с реплики. Решения: читать свои записи с лидера (read-your-writes), привязка сессии к реплике, синхронная репликация для критичных данных, мониторинг отставания и вывод отстающих реплик из балансировки.',
      },
      {
        q: 'Когда выбирать Redis, а когда PostgreSQL?',
        a: 'PostgreSQL — основное хранилище с транзакциями, сложными запросами, ограничениями целостности. Redis — хранилище в памяти для скорости: кэш, сессии, счётчики, лимиты, очереди, лидерборды на sorted set. Redis обычно дополняет основную базу, а не заменяет: персистентность у него слабее, и объём ограничен памятью.',
      },
      {
        q: 'Какие проблемы бывают с кэшем?',
        a: 'Инвалидация — устаревшие данные после изменения в БД. Cache stampede — при истечении популярного ключа тысячи запросов одновременно идут в БД (лечится блокировкой на перестроение, ранним обновлением, случайным разбросом TTL). Холодный старт после перезапуска. Кэширование отсутствующих значений от атак на несуществующие ключи. Рост памяти — нужны TTL и политика вытеснения.',
      },
    ],
    resources: [{ title: 'Клеппман «Высоконагруженные приложения»', url: 'https://dataintensive.net/', kind: 'book' }],
  },
  {
    id: 'cs-oop',
    title: 'ООП, SOLID и паттерны проектирования',
    area: ENG,
    level: 'core',
    minutes: 90,
    summary: 'Инкапсуляция, наследование, полиморфизм, принципы SOLID и главные паттерны.',
    body: `### Принципы ООП
- **Инкапсуляция** — объект скрывает внутреннее состояние и даёт доступ через методы, сохраняя инварианты.
- **Наследование** — класс переиспользует и расширяет поведение родителя. Создаёт сильную связь.
- **Полиморфизм** — код работает с общим интерфейсом, а конкретная реализация выбирается во время выполнения.
- **Абстракция** — выделение существенного, скрытие деталей.

«Композиция лучше наследования»: наследование жёстко связывает классы и ломается при изменении родителя, композиция гибче.

### SOLID
- **S — единственная ответственность**: у класса одна причина для изменения.
- **O — открытость/закрытость**: расширяем поведение без изменения существующего кода (новые реализации интерфейса).
- **L — подстановка Лисков**: подкласс можно использовать везде вместо родителя без нарушения поведения. Классический контрпример — квадрат, наследующий прямоугольник.
- **I — разделение интерфейсов**: много маленьких интерфейсов лучше одного толстого.
- **D — инверсия зависимостей**: модули верхнего уровня зависят от абстракций, а не от конкретных реализаций; зависимости передаются снаружи (DI).

### Паттерны, которые спрашивают
- **Порождающие**: Singleton (и почему его не любят — глобальное состояние, сложно тестировать), Factory Method, Builder.
- **Структурные**: Adapter, Decorator, Facade, Proxy.
- **Поведенческие**: Strategy, Observer, Command, Template Method, Iterator, Chain of Responsibility.

### Другие принципы
DRY, KISS, YAGNI, закон Деметры, «высокая связность внутри модуля, слабая связанность между модулями».

### Архитектурные подходы
Слоистая архитектура, чистая/гексагональная архитектура (домен в центре, инфраструктура на краях), MVC/MVVM, микросервисы против монолита — плюсы и минусы.`,
    questions: [
      {
        q: 'Расскажите о принципах SOLID с примерами.',
        a: 'S: класс Report не должен сам отправлять почту — это отдельная ответственность. O: новый способ оплаты добавляется новой реализацией интерфейса PaymentMethod, без правки switch в существующем коде. L: подкласс не должен ужесточать требования или ломать ожидания — Square, наследующий Rectangle, ломает setWidth. I: интерфейс Printer со сканированием и факсом заставит простой принтер реализовывать лишнее — лучше разделить. D: сервис заказов зависит от интерфейса OrderRepository, а конкретная реализация (PostgreSQL или заглушка в тестах) передаётся извне.',
      },
      {
        q: 'Почему говорят «композиция лучше наследования»?',
        a: 'Наследование создаёт сильную связь: подкласс зависит от деталей реализации родителя, изменение базового класса может сломать наследников (хрупкий базовый класс), иерархии разрастаются. Композиция собирает поведение из независимых объектов через интерфейсы, их можно подменять во время выполнения и тестировать по отдельности. Наследование уместно для настоящего отношения «является» при стабильном родителе.',
      },
      {
        q: 'Что такое полиморфизм?',
        a: 'Возможность работать с объектами разных типов через общий интерфейс. Подтиповой полиморфизм — вызов метода через ссылку на базовый тип выполняет реализацию конкретного класса (динамическая диспетчеризация). Параметрический — обобщённые типы (generics). Ad hoc — перегрузка функций.',
      },
      {
        q: 'Что не так с Singleton?',
        a: 'Это глобальное состояние: скрытые зависимости, классы неявно связаны, трудно подменить в тестах, проблемы с многопоточной инициализацией и временем жизни. Обычно лучше создать один экземпляр в точке сборки приложения и передавать его через внедрение зависимостей.',
      },
      {
        q: 'Какие паттерны вы применяли? Приведите пример Strategy и Observer.',
        a: 'Strategy — семейство взаимозаменяемых алгоритмов за общим интерфейсом: разные стратегии расчёта скидки или доставки, выбираемые во время выполнения. Observer — объект уведомляет подписчиков об изменениях: события в UI, подписка на изменение данных, EventEmitter. Хороший ответ связывает паттерн с реальной задачей из вашего проекта.',
      },
      {
        q: 'Монолит или микросервисы?',
        a: 'Монолит проще разрабатывать, деплоить и отлаживать, транзакции локальные — хороший выбор на старте и для небольших команд. Микросервисы дают независимые релизы и масштабирование частей, изоляцию отказов и технологий, но добавляют сетевые вызовы, распределённые транзакции, наблюдаемость, сложность инфраструктуры. Разумный путь — модульный монолит, из которого выделяют сервисы по реальной необходимости.',
      },
    ],
    resources: [{ title: 'Refactoring.Guru — паттерны проектирования', url: 'https://refactoring.guru/ru/design-patterns', kind: 'course' }],
  },
  {
    id: 'cs-git',
    title: 'Git и командная разработка',
    area: ENG,
    level: 'base',
    minutes: 45,
    summary: 'Коммиты, ветки, merge и rebase, код-ревью, решение конфликтов.',
    body: `### Модель Git
Коммит — снимок проекта со ссылкой на родителя(ей), автора и сообщение. Ветка — подвижный указатель на коммит. HEAD — текущий коммит или ветка. Три зоны: рабочая директория, индекс (staging), репозиторий.

### Команды
\`status\`, \`add\`, \`commit\`, \`log --oneline --graph\`, \`diff\`, \`branch\`, \`switch\`/\`checkout\`, \`merge\`, \`rebase\`, \`fetch\`, \`pull\`, \`push\`, \`stash\`, \`reset\`, \`revert\`, \`cherry-pick\`, \`reflog\`, \`bisect\`.

### merge и rebase
- **merge** создаёт коммит слияния, сохраняет историю как было. Безопасен для общих веток.
- **rebase** переносит коммиты ветки поверх другой — линейная история, но переписывает коммиты. **Не делайте rebase уже опубликованных общих веток.**
- **squash** — объединение коммитов в один при слиянии PR.

### reset и revert
- \`reset --soft/--mixed/--hard\` двигает ветку назад (переписывает историю, для локальных изменений).
- \`revert\` создаёт новый коммит, отменяющий изменения, — безопасно для общей истории.
- Потеряли коммит — \`git reflog\` покажет, где он был.

### Процессы
GitHub Flow (короткие ветки фич + PR в main), Git Flow (develop, release, hotfix), trunk-based development (частые мелкие слияния в main, фича-флаги). Код-ревью: маленькие PR, описание зачем, автотесты в CI.

### Хорошие коммиты
Одно логическое изменение, понятное сообщение в повелительном наклонении («Добавить валидацию email»), без секретов и сгенерированных файлов (\`.gitignore\`).`,
    questions: [
      {
        q: 'Чем merge отличается от rebase?',
        a: 'merge объединяет ветки коммитом слияния, история сохраняется как была, коммиты не переписываются. rebase переносит коммиты текущей ветки поверх целевой, создавая новые коммиты — история становится линейной. rebase удобен для обновления своей локальной ветки перед PR, но нельзя переписывать уже опубликованные коммиты, которыми пользуются другие.',
      },
      {
        q: 'Как отменить коммит, который уже отправлен в общую ветку?',
        a: 'git revert <commit> — создаёт новый коммит с обратными изменениями и не переписывает историю, поэтому безопасен. reset с последующим push --force переписывает историю и ломает работу коллег — для общих веток его не используют.',
      },
      {
        q: 'Что делать при конфликте слияния?',
        a: 'Посмотреть конфликтующие файлы (git status), в каждом вручную выбрать правильный результат между маркерами <<<<<<<, =======, >>>>>>>, понимая смысл обоих изменений, при необходимости обсудив с автором. Запустить тесты, добавить файлы (git add) и завершить merge или rebase (--continue). Уменьшить конфликты помогают маленькие ветки и частые обновления от main.',
      },
      {
        q: 'Что делает git stash?',
        a: 'Временно убирает незакоммиченные изменения в отдельное хранилище и возвращает рабочую директорию к последнему коммиту. Полезно, чтобы быстро переключиться на другую задачу. Вернуть — git stash pop или apply.',
      },
      {
        q: 'Как найти коммит, который сломал тест?',
        a: 'git bisect: отмечаем известный хороший и плохой коммиты, Git бинарным поиском предлагает промежуточные коммиты, мы помечаем их good/bad (или автоматизируем через git bisect run со скриптом теста). За log₂(n) шагов находится первый плохой коммит.',
      },
    ],
  },
  {
    id: 'cs-linux',
    title: 'Linux и командная строка',
    area: ENG,
    level: 'core',
    minutes: 90,
    summary: 'Файлы и права, процессы, пайпы, поиск и обработка текста.',
    body: `### Файлы и права
- Всё — файл: обычные файлы, каталоги, устройства, сокеты.
- Права \`rwx\` для владельца, группы и остальных: \`chmod 640 file\` (rw-r-----), \`chown user:group\`.
- Для каталога: \`r\` — читать список, \`w\` — создавать и удалять файлы, \`x\` — заходить внутрь.
- Особые биты: SUID (запуск с правами владельца файла — \`passwd\`), SGID, sticky bit (\`/tmp\`: удалять может только владелец файла).
- Жёсткая ссылка — ещё одно имя того же inode; символическая — файл-указатель на путь.

### Процессы
\`ps aux\`, \`top\`/\`htop\`, \`pgrep\`, \`kill -TERM pid\` (вежливо), \`kill -9\` (SIGKILL, нельзя перехватить), \`nohup\`, \`&\`, \`jobs\`, \`fg\`. Load average — средняя длина очереди на CPU (и на непрерываемый I/O) за 1, 5 и 15 минут.

### Потоки ввода-вывода
stdin (0), stdout (1), stderr (2). \`>\` перезапись, \`>>\` добавление, \`2>&1\` объединение, \`|\` конвейер.

### Инструменты обработки текста
\`\`\`bash
# топ-10 IP по числу запросов в логе nginx
awk '{print $1}' access.log | sort | uniq -c | sort -rn | head -10
# найти все строки с ошибкой в подкаталогах
grep -rn "ERROR" /var/log/app/
# файлы больше 100 МБ
find / -type f -size +100M 2>/dev/null
\`\`\`
\`grep\`, \`sed\`, \`awk\`, \`cut\`, \`sort\`, \`uniq\`, \`wc\`, \`head\`/\`tail -f\`, \`xargs\`, \`jq\` для JSON.

### Система и сеть
\`df -h\`, \`du -sh\`, \`free -h\`, \`lsof\`, \`ss -tulpn\` (кто слушает порты), \`curl -v\`, \`dig\`, \`ping\`, \`traceroute\`, \`systemctl\`, \`journalctl -u service\`.`,
    questions: [
      {
        q: 'Что означают права 755 и 644?',
        a: 'Цифры — права владельца, группы и остальных, каждая — сумма r=4, w=2, x=1. 755 — rwxr-xr-x: владелец всё, остальные читают и исполняют (типично для каталогов и скриптов). 644 — rw-r--r--: владелец читает и пишет, остальные только читают (обычные файлы).',
      },
      {
        q: 'Чем kill -15 отличается от kill -9?',
        a: 'SIGTERM (15) — просьба завершиться: процесс может перехватить сигнал, закрыть соединения, сохранить состояние (graceful shutdown). SIGKILL (9) — немедленное завершение ядром, перехватить нельзя, ресурсы приложения не освобождаются корректно. Сначала SIGTERM, и только если процесс не завершился — SIGKILL.',
      },
      {
        q: 'Как найти 10 самых частых IP в логе nginx?',
        a: "awk '{print $1}' access.log | sort | uniq -c | sort -rn | head -10. awk вырезает первый столбец (IP), sort группирует одинаковые, uniq -c считает повторы, sort -rn сортирует по числу по убыванию, head оставляет первые 10.",
      },
      {
        q: 'Чем жёсткая ссылка отличается от символической?',
        a: 'Жёсткая ссылка — ещё одно имя того же inode: данные удаляются, когда удалены все имена; не может указывать на каталог и на другой раздел. Символическая — отдельный файл, хранящий путь; может указывать куда угодно, в том числе на другой раздел, но «ломается», если цель удалена или перемещена.',
      },
      {
        q: 'Что такое load average?',
        a: 'Средняя за 1, 5 и 15 минут длина очереди процессов, которые выполняются или ждут CPU, а в Linux ещё и находятся в непрерываемом ожидании ввода-вывода. Сравнивать нужно с числом ядер: load 8 на 8 ядрах — полная загрузка, на 2 ядрах — перегрузка. Высокий load при низком CPU часто указывает на проблемы с диском или сетью.',
      },
      {
        q: 'Как узнать, какой процесс слушает порт 8080?',
        a: 'ss -tulpn | grep 8080 или lsof -i :8080. Покажут PID и имя процесса. netstat -tulpn — устаревший вариант того же.',
      },
    ],
    resources: [{ title: 'Linux Journey', url: 'https://linuxjourney.com', kind: 'course' }],
  },
  {
    id: 'cs-sysdesign',
    title: 'Основы проектирования систем',
    area: ENG,
    level: 'plus',
    minutes: 120,
    summary: 'Как отвечать на секции системного дизайна: от требований до масштабирования.',
    body: `### Когда это спросят
Для стажёров — редко и упрощённо, но в Т-Банке секция системного дизайна есть у бэкендеров и системных аналитиков. Понимание основ отличает сильного кандидата.

### Структура ответа (45–60 минут)
1. **Требования** (5–10 мин): функциональные (что делает система) и нефункциональные (нагрузка, задержка, доступность, согласованность, объём данных).
2. **Оценки** (5 мин): RPS на чтение и запись, объём хранения, трафик. 1 день ≈ 86 400 с ≈ 10⁵ с.
3. **API**: основные методы.
4. **Модель данных** и выбор хранилища.
5. **Высокоуровневая схема**: клиент → балансировщик → сервисы → кэш → БД → очереди.
6. **Углубление**: узкие места, масштабирование, отказоустойчивость.
7. **Компромиссы**: что выбрали и чем пожертвовали.

### Строительные блоки
- **Балансировщик нагрузки** (L4/L7): распределение, health checks.
- **Горизонтальное масштабирование** stateless-сервисов; состояние — в БД, кэше, хранилищах.
- **Кэш** (Redis, CDN для статики).
- **Очереди и брокеры** (Kafka, RabbitMQ): асинхронная обработка, сглаживание пиков, развязка сервисов.
- **Репликация и шардирование** БД.
- **Объектное хранилище** (S3) для файлов.
- **Поиск** (Elasticsearch), **аналитика** (ClickHouse).

### Надёжность
Ретраи с экспоненциальной задержкой и jitter, таймауты, circuit breaker, идемпотентность, graceful degradation, rate limiting, мониторинг и алерты. Доступность 99,9% — около 8,8 часа простоя в год, 99,99% — 53 минуты.

### Числа, которые полезно помнить
Чтение из памяти — ~100 нс, из SSD — ~100 мкс, сетевой запрос внутри ДЦ — ~0,5 мс, между континентами — ~150 мс.`,
    questions: [
      {
        q: 'С чего начать ответ на задачу «спроектируйте X»?',
        a: 'С уточнения требований: какие функции обязательны, сколько пользователей и запросов, соотношение чтения и записи, требования к задержке, доступности и согласованности, объём данных. Затем — быстрые оценки нагрузки и объёма, API и модель данных, и только потом схема. Прыжок сразу к технологиям — частая ошибка.',
      },
      {
        q: 'Как масштабировать сервис, который перестал справляться с нагрузкой?',
        a: 'Найти узкое место по метрикам. Stateless-приложение масштабировать горизонтально за балансировщиком. Разгрузить БД: индексы, кэш для горячих чтений, реплики для чтения, затем шардирование. Тяжёлые операции вынести в асинхронную обработку через очередь. Статику отдать через CDN. Ограничить злоупотребления rate limiting.',
      },
      {
        q: 'Зачем нужна очередь сообщений между сервисами?',
        a: 'Развязывает отправителя и получателя: отправителю не нужно ждать обработки и не страшна временная недоступность получателя. Сглаживает пики нагрузки — сообщения накапливаются и обрабатываются с доступной скоростью. Позволяет нескольким потребителям обрабатывать события независимо (fan-out). Цена — асинхронность, конечная согласованность, необходимость идемпотентной обработки.',
      },
      {
        q: 'Что такое circuit breaker?',
        a: 'Паттерн защиты от каскадных отказов: если вызовы зависимости часто падают, «автомат размыкается», и следующие вызовы сразу возвращают ошибку или запасной ответ, не нагружая больной сервис и не занимая потоки ожиданием. Через время пропускается пробный запрос, и при успехе цепь замыкается снова.',
      },
      {
        q: 'Что значит доступность 99,9% и как её обеспечить?',
        a: 'Сервис может быть недоступен не более 0,1% времени — около 43 минут в месяц. Обеспечивают резервированием всех компонентов (несколько экземпляров, реплики БД, несколько зон доступности), автоматическим переключением, health checks, безопасными релизами (канареечные выкладки, быстрый откат), мониторингом и дежурствами.',
      },
    ],
    resources: [
      { title: 'System Design Primer', url: 'https://github.com/donnemartin/system-design-primer', kind: 'course' },
      { title: 'Architectural Katas', url: 'https://www.architecturalkatas.com/', kind: 'practice' },
    ],
  },
  {
    id: 'cs-testing-dev',
    title: 'Тестирование для разработчика',
    area: ENG,
    level: 'core',
    minutes: 45,
    summary: 'Юнит- и интеграционные тесты, моки, пирамида тестирования, TDD.',
    body: `### Пирамида тестирования
- **Юнит-тесты** — много, быстрые, проверяют отдельную функцию или класс в изоляции.
- **Интеграционные** — меньше, проверяют взаимодействие с БД, очередями, другими модулями.
- **End-to-end** — немного, проверяют сценарий пользователя целиком; медленные и хрупкие.

### Хороший юнит-тест
Структура **Arrange — Act — Assert**. Один сценарий на тест, понятное имя («возвращает 0 для пустой корзины»), независимость от порядка запуска, детерминированность (без реального времени, сети и случайности).

### Тестовые двойники
- **Stub** — возвращает заранее заданные ответы.
- **Mock** — проверяет, что с ним взаимодействовали ожидаемым образом.
- **Fake** — упрощённая рабочая реализация (БД в памяти).
- **Spy** — обёртка, записывающая вызовы.
Чтобы подменять зависимости, их нужно передавать снаружи (инверсия зависимостей).

### Что тестировать
Граничные случаи, ошибки и исключения, бизнес-правила. Покрытие кода — полезный индикатор, но 100% не означает отсутствия ошибок.

### Инструменты
pytest (фикстуры, параметризация), JUnit 5 + Mockito, go test (табличные тесты, \`-race\`), Jest/Vitest, Testcontainers для интеграционных тестов с настоящей БД в Docker.

### TDD
Красный (падающий тест) → зелёный (минимальная реализация) → рефакторинг. Помогает проектировать API с точки зрения использования.`,
    questions: [
      {
        q: 'Чем юнит-тест отличается от интеграционного?',
        a: 'Юнит-тест проверяет небольшую единицу кода в изоляции, зависимости заменены двойниками; работает миллисекунды. Интеграционный проверяет совместную работу компонентов с настоящими зависимостями — БД, брокером, HTTP; медленнее, но ловит ошибки на стыках (неправильный SQL, сериализация, конфигурация).',
      },
      {
        q: 'Чем mock отличается от stub?',
        a: 'Stub подставляет заранее заданные ответы, чтобы тестируемый код мог работать, — тест проверяет результат. Mock настраивается ожиданиями о вызовах, и тест проверяет само взаимодействие: был ли вызван метод, с какими аргументами, сколько раз. Злоупотребление моками делает тесты хрупкими — они ломаются при рефакторинге без изменения поведения.',
      },
      {
        q: 'Что такое пирамида тестирования?',
        a: 'Рекомендация по соотношению тестов: много быстрых дешёвых юнит-тестов в основании, меньше интеграционных в середине и немного медленных end-to-end наверху. Перевёрнутая пирамида (много E2E) даёт медленный и нестабильный CI.',
      },
      {
        q: 'Почему тесты бывают нестабильными (flaky) и как с этим бороться?',
        a: 'Причины: зависимость от времени и таймаутов, порядка выполнения, общего состояния между тестами, сети и внешних сервисов, случайных данных, гонок в асинхронном коде. Решения: изолировать состояние, подменять время и сеть, ждать условия, а не фиксированное время, фиксировать seed, запускать тесты в случайном порядке, чтобы выявить зависимости.',
      },
    ],
  },
]
