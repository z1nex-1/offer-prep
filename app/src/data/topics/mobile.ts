import type { Topic } from '../../types'

const IOS = 'iOS'
const AND = 'Android'
const MOB = 'Мобильная архитектура'

export const mobile: Topic[] = [
  {
    id: 'ios-swift',
    title: 'Swift: типы, опционалы, протоколы, замыкания',
    area: IOS,
    level: 'base',
    minutes: 90,
    summary: 'struct и class, опционалы, протоколы и дженерики, замыкания и захват.',
    body: `### Значимые и ссылочные типы
- **struct, enum, кортежи** — значимые типы: копируются при присваивании и передаче (коллекции стандартной библиотеки — с copy-on-write, реальное копирование только при изменении).
- **class, замыкания, акторы** — ссылочные: несколько переменных указывают на один объект, управляются ARC.
Выбор по умолчанию — \`struct\`: нет общих изменяемых состояний, потокобезопаснее, нет накладных расходов ARC. \`class\` — когда нужна идентичность, наследование или общая изменяемость.
\`mutating\` — метод struct, меняющий её поля. \`let\` для struct запрещает менять поля, для class — только переприсваивать ссылку.

### Опционалы
\`String?\` — либо значение, либо \`nil\` (на самом деле \`enum Optional { case some, none }\`). Работа: \`if let\`, \`guard let\` (ранний выход), \`??\`, опциональная цепочка \`a?.b\`, \`map\`/\`flatMap\`. Принудительное извлечение \`!\` падает на nil — только когда nil невозможен по логике.

### Протоколы
Контракты для классов, структур и перечислений. Расширения протоколов дают реализацию по умолчанию (**protocol-oriented programming**). Ассоциированные типы, \`some Protocol\` (непрозрачный тип — конкретный, скрытый от вызывающего) и \`any Protocol\` (экзистенциальный контейнер — любой тип, динамическая диспетчеризация).

### Перечисления
Связанные значения (\`case failure(Error)\`), \`switch\` должен быть исчерпывающим, \`Result<Success, Failure>\`.

### Замыкания
Захватывают переменные из окружения **по ссылке**. \`@escaping\` — замыкание переживёт вызов функции (сохраняется или вызывается асинхронно). Список захвата \`[weak self]\`, \`[unowned self]\` управляет силой ссылки. Автозамыкания \`@autoclosure\`.

### Обработка ошибок
\`throws\`, \`do/try/catch\`, \`try?\` (в опционал), \`try!\`. С Swift 6 — типизированные throws.

### Диспетчеризация
Статическая (struct, final, private — быстрее, возможен инлайнинг) и динамическая (vtable для классов, witness table для протоколов, message dispatch для \`@objc dynamic\`).`,
    questions: [
      {
        q: 'Чем struct отличается от class в Swift?',
        a: 'struct — значимый тип: при присваивании и передаче копируется (с оптимизацией copy-on-write для коллекций), нет наследования, методы, меняющие поля, помечаются mutating, память управляется без счётчика ссылок. class — ссылочный тип: общий объект, наследование, деинициализатор, управление памятью через ARC, возможны циклы ссылок. По умолчанию выбирают struct.',
      },
      {
        q: 'Что такое опционал и какие способы его безопасно извлечь?',
        a: 'Тип, который содержит либо значение, либо nil; реализован как enum Optional. Безопасные способы: if let и guard let (с ранним выходом), оператор ?? со значением по умолчанию, опциональная цепочка ?., map/flatMap. Принудительное ! приводит к падению при nil.',
      },
      {
        q: 'Что такое copy-on-write?',
        a: 'Оптимизация значимых типов: при копировании массив, строка или словарь делят один буфер, а реальное копирование происходит только при первой записи, если буфер используется больше чем одним владельцем (проверка isKnownUniquelyReferenced). Даёт семантику значений без лишних копий.',
      },
      {
        q: 'Что означает @escaping?',
        a: 'Замыкание, переданное в функцию, может быть вызвано после её завершения — сохранено в свойство или вызвано асинхронно. Такие замыкания должны явно захватывать self и могут создавать циклы ссылок, поэтому обычно используют [weak self]. Неэскейпящие замыкания (по умолчанию) вызываются только внутри функции.',
      },
      {
        q: 'Чем some Protocol отличается от any Protocol?',
        a: 'some — непрозрачный тип: функция возвращает один конкретный тип, известный компилятору, но скрытый от вызывающего; статическая диспетчеризация, сохраняются ассоциированные типы (SwiftUI: some View). any — экзистенциальный тип: контейнер, в котором может лежать значение любого соответствующего типа, выбор во время выполнения, накладные расходы на упаковку.',
      },
    ],
    resources: [{ title: 'The Swift Programming Language', url: 'https://docs.swift.org/swift-book/', kind: 'docs' }],
  },
  {
    id: 'ios-memory',
    title: 'Управление памятью: ARC и циклы ссылок',
    area: IOS,
    level: 'core',
    minutes: 60,
    summary: 'strong, weak, unowned, retain cycle в замыканиях и делегатах.',
    body: `### ARC
Automatic Reference Counting: компилятор вставляет увеличение и уменьшение счётчика сильных ссылок на объект класса. Когда счётчик падает до нуля, вызывается \`deinit\` и память освобождается. Это не сборщик мусора: освобождение детерминированное, но **циклы** не разрываются автоматически.

### Виды ссылок
- **strong** — по умолчанию, увеличивает счётчик.
- **weak** — не увеличивает, всегда опционал, автоматически становится nil при освобождении объекта. Для делегатов и обратных ссылок.
- **unowned** — не увеличивает и не опционал; обращение после освобождения — падение. Когда объект гарантированно живёт дольше (ссылка ребёнка на родителя с тем же временем жизни).

### Типичные циклы
1. **Делегат**: контроллер держит view, view держит делегата-контроллера сильно. Решение — \`weak var delegate\` (протокол с \`AnyObject\`).
2. **Замыкание**: объект хранит замыкание, замыкание захватывает \`self\` сильно.
\`\`\`swift
viewModel.onUpdate = { [weak self] data in
    guard let self else { return }
    self.render(data)
}
\`\`\`
3. **Timer** сильно держит target; **NotificationCenter** с блоками — нужно отписываться или использовать weak.

### Поиск утечек
Memory Graph Debugger в Xcode, Instruments (Leaks, Allocations), проверка вызова \`deinit\` при закрытии экрана.`,
    questions: [
      {
        q: 'Как работает ARC?',
        a: 'Для каждого объекта класса хранится счётчик сильных ссылок. Компилятор вставляет retain при появлении новой сильной ссылки и release при её исчезновении. Когда счётчик становится нулём, вызывается deinit и память освобождается сразу. В отличие от сборщика мусора, ARC не находит циклы — их разрывают слабыми ссылками.',
      },
      {
        q: 'Чем weak отличается от unowned?',
        a: 'Обе не удерживают объект. weak — всегда опционал и обнуляется, когда объект освобождается; безопасна, если объект может исчезнуть раньше. unowned — неопциональная, не обнуляется, и обращение к освобождённому объекту приводит к падению; используется, когда время жизни ссылаемого объекта гарантированно не короче.',
      },
      {
        q: 'Как возникает retain cycle в замыкании и как его избежать?',
        a: 'Объект хранит замыкание в свойстве, а замыкание захватывает self сильно — они удерживают друг друга, и deinit никогда не вызовется. Избегают списком захвата [weak self] с guard let self внутри или [unowned self], если замыкание не переживёт объект. Для неэскейпящих замыканий (map, UIView.animate) цикла нет.',
      },
      {
        q: 'Почему делегат объявляют weak?',
        a: 'Обычно владелец (контроллер) сильно держит объект (таблицу, view), а тот хранит ссылку на делегата — того же владельца. Сильная ссылка создала бы цикл. weak var delegate разрывает его; протокол делегата помечают AnyObject, чтобы weak было допустимо.',
      },
    ],
  },
  {
    id: 'ios-ui',
    title: 'UIKit и SwiftUI',
    area: IOS,
    level: 'core',
    minutes: 90,
    summary: 'Жизненный цикл контроллера, Auto Layout, таблицы, основы SwiftUI и управление состоянием.',
    body: `### Жизненный цикл UIViewController
\`init\` → \`loadView\` → \`viewDidLoad\` (один раз: настройка) → \`viewWillAppear\` → \`viewIsAppearing\` → \`viewWillLayoutSubviews\` / \`viewDidLayoutSubviews\` (размеры известны) → \`viewDidAppear\` → \`viewWillDisappear\` → \`viewDidDisappear\` → \`deinit\`. Геометрию нельзя надёжно использовать в \`viewDidLoad\`.

### Жизненный цикл приложения
\`UIApplicationDelegate\` и \`UISceneDelegate\` (с iOS 13 — сцены): foreground/background, приостановка. В фоне приложение может быть завершено без предупреждения.

### Auto Layout
Ограничения (constraints) описывают отношения между элементами. Приоритеты, content hugging и compression resistance для ячеек с динамическим текстом. \`UIStackView\` упрощает вёрстку. Frame-based раскладка — для максимальной производительности.

### Таблицы и коллекции
\`UITableView\`/\`UICollectionView\` переиспользуют ячейки (\`dequeueReusableCell\`) — при переиспользовании нужно сбрасывать состояние (\`prepareForReuse\`), отменять загрузку картинок. \`DiffableDataSource\` — анимированные обновления без ручных индексов, compositional layout.

### Главный поток
Весь UI обновляется только на главном потоке. Тяжёлые операции (декодирование, диск, сеть) — в фоне.

### SwiftUI
Декларативный UI: \`View\` — значимый тип, описание интерфейса как функции состояния.
- \`@State\` — локальное состояние view.
- \`@Binding\` — ссылка на чужое состояние.
- \`@Observable\` (iOS 17) / \`ObservableObject\` + \`@Published\` — модели, \`@StateObject\` владеет, \`@ObservedObject\` наблюдает.
- \`@Environment\` — значения окружения.
Идентичность view и \`id\` в \`ForEach\` влияют на сохранение состояния и анимации. Интеграция с UIKit — \`UIViewRepresentable\`, \`UIHostingController\`.`,
    questions: [
      {
        q: 'Опишите жизненный цикл UIViewController.',
        a: 'Создание (init), загрузка view (loadView, затем viewDidLoad — однократная настройка), появление (viewWillAppear, viewIsAppearing, раскладка viewWillLayoutSubviews/viewDidLayoutSubviews, viewDidAppear), исчезновение (viewWillDisappear, viewDidDisappear) и освобождение (deinit). Методы появления вызываются при каждом показе экрана, viewDidLoad — один раз.',
      },
      {
        q: 'Почему UI нельзя обновлять из фонового потока?',
        a: 'UIKit не потокобезопасен: состояние view и слоёв рассчитано на доступ из главного потока, где работает run loop отрисовки и обработки событий. Обновление из другого потока приводит к гонкам, артефактам и падениям. Результаты фоновой работы передают на главный через DispatchQueue.main, MainActor или await MainActor.run.',
      },
      {
        q: 'Как работает переиспользование ячеек и какие с ним проблемы?',
        a: 'Таблица держит в памяти только видимые ячейки и переиспользует ушедшие с экрана для новых строк. Проблемы: в ячейке остаётся состояние от предыдущих данных, а асинхронная загрузка картинки может прийти в уже переиспользованную ячейку. Решение — сбрасывать состояние в prepareForReuse, отменять задачи и проверять идентификатор данных перед установкой результата.',
      },
      {
        q: 'Чем @State отличается от @StateObject и @ObservedObject?',
        a: '@State хранит простое значение, принадлежащее view, SwiftUI сохраняет его между пересозданиями структуры. @StateObject создаёт и владеет объектом-моделью, сохраняя его на всё время жизни view. @ObservedObject наблюдает за объектом, который создал и владеет кто-то другой; если создать объект через @ObservedObject, он может пересоздаваться при каждом обновлении родителя. В iOS 17 с @Observable используют @State для владения моделью.',
      },
      {
        q: 'Когда выбрать SwiftUI, а когда UIKit?',
        a: 'SwiftUI — быстрее разработка, декларативность, превью, меньше кода; хорош для новых экранов и приложений с минимальной версией iOS 15–16+. UIKit — полный контроль, зрелость, сложные списки и анимации, поддержка старых iOS, огромная кодовая база в крупных компаниях. На практике их комбинируют.',
      },
    ],
  },
  {
    id: 'ios-concurrency',
    title: 'Многопоточность в iOS: GCD, async/await, акторы',
    area: IOS,
    level: 'core',
    minutes: 75,
    summary: 'Очереди GCD, sync и async, deadlock на главной очереди, структурированная конкурентность.',
    body: `### GCD
- **Последовательные** очереди выполняют задачи по одной, **конкурентные** — параллельно.
- \`DispatchQueue.main\` — главная последовательная очередь (UI). Глобальные конкурентные очереди с QoS: userInteractive, userInitiated, utility, background.
- \`async\` — поставить задачу и не ждать, \`sync\` — поставить и ждать завершения.
- **Deadlock**: \`DispatchQueue.main.sync { }\` с главного потока — поток ждёт сам себя. То же с \`sync\` на свою же последовательную очередь.
- \`DispatchGroup\` — дождаться группы задач, \`DispatchSemaphore\` — ограничение, \`barrier\` на конкурентной очереди — эксклюзивная запись (паттерн «много читателей, один писатель»).

### OperationQueue
Операции с зависимостями, отменой, ограничением числа одновременных (\`maxConcurrentOperationCount\`).

### Swift Concurrency
- \`async/await\` — асинхронный код в линейном стиле.
- \`Task { }\` — запуск асинхронной работы, \`Task.detached\`, отмена через \`task.cancel()\` и проверку \`Task.isCancelled\`.
- \`async let\` и \`withTaskGroup\` — структурированный параллелизм.
- \`actor\` — тип, защищающий своё изменяемое состояние: доступ извне только асинхронно, по одному.
- \`@MainActor\` — выполнение на главном потоке (UI-классы, ViewModel).
- \`Sendable\` — тип безопасно передаётся между потоками; Swift 6 проверяет гонки данных при компиляции.

### Гонки
Одновременное изменение массива из нескольких потоков приводит к падению. Защита: последовательная очередь, блокировка (\`NSLock\`, \`os_unfair_lock\`), actor.`,
    questions: [
      {
        q: 'Что будет, если вызвать DispatchQueue.main.sync на главном потоке?',
        a: 'Deadlock: sync ставит задачу в главную очередь и блокирует текущий поток до её выполнения, но текущий поток и есть главный — он не может выполнить задачу, пока ждёт. Приложение зависнет (в отладке — падение). Для главного потока используют async.',
      },
      {
        q: 'Чем последовательная очередь отличается от конкурентной, а sync от async?',
        a: 'Тип очереди определяет, как задачи выполняются: последовательная — по одной в порядке постановки, конкурентная — несколько одновременно. sync и async определяют поведение вызывающего: sync ждёт завершения задачи, async возвращается сразу. Это независимые оси.',
      },
      {
        q: 'Что такое actor в Swift?',
        a: 'Ссылочный тип, изолирующий своё изменяемое состояние: одновременно с ним работает только одна задача, а обращения извне асинхронные (await). Это устраняет гонки данных без ручных блокировок. @MainActor — глобальный актор главного потока для UI-кода. Внутри актора возможна реентерабельность после await — состояние могло измениться.',
      },
      {
        q: 'Как выполнить несколько запросов параллельно и дождаться всех?',
        a: 'В Swift Concurrency — async let для фиксированного числа или withTaskGroup / withThrowingTaskGroup для динамического, затем await результатов. В GCD — DispatchGroup: enter перед каждым запросом, leave в колбэке, notify(queue: .main) для обработки результата. Отмена в структурированной конкурентности распространяется на дочерние задачи автоматически.',
      },
    ],
  },
  {
    id: 'mobile-arch',
    title: 'Архитектура мобильных приложений',
    area: MOB,
    level: 'core',
    minutes: 75,
    summary: 'MVC, MVVM, MVI, VIPER, Clean Architecture, DI, навигация, модульность.',
    body: `### Паттерны уровня экрана
- **MVC** (Apple) — контроллер часто разрастается («Massive View Controller»).
- **MVVM** — ViewModel хранит состояние и логику экрана, View подписывается на изменения (Combine, @Observable, StateFlow). Легко тестировать ViewModel без UI.
- **MVI / UDF** — однонаправленный поток: намерения пользователя → редьюсер → новое неизменяемое состояние → отрисовка. Предсказуемо, удобно для сложных экранов (популярно в Android с Compose).
- **VIPER** — View, Interactor, Presenter, Entity, Router: сильное разделение, много кода; встречается в крупных iOS-командах.

### Clean Architecture
Слои: **presentation** (UI и ViewModel), **domain** (сущности и use case, без зависимостей от фреймворков), **data** (репозитории, сеть, БД). Зависимости направлены внутрь — к домену. Репозиторий скрывает источник данных.

### Внедрение зависимостей
Зависимости передаются снаружи (через инициализатор), а не создаются внутри: тестируемость и подмена. Android — Hilt/Dagger, Koin; iOS — ручной DI, Swinject, фабрики, Needle.

### Навигация
iOS — координаторы (Coordinator), \`NavigationStack\`; Android — Navigation Component / Compose Navigation. Deep links.

### Модульность
Разбиение на feature-модули ускоряет сборку и разделяет ответственность команд. Общие модули: дизайн-система, сеть, аналитика.

### Что важно показать на секции
Экран проектируют с учётом состояний (загрузка, контент, пусто, ошибка), единого источника правды, тестируемости, жизненного цикла (поворот экрана, фон) и разделения слоёв.`,
    questions: [
      {
        q: 'Чем MVVM лучше MVC?',
        a: 'В MVC на iOS контроллер совмещает логику экрана, работу с view и навигацию и быстро разрастается. MVVM выносит состояние и логику представления в ViewModel без зависимостей от UIKit/Android View — её легко покрыть юнит-тестами, а View только отображает состояние и передаёт действия. Цена — привязка данных и дополнительный слой.',
      },
      {
        q: 'Что такое Clean Architecture в мобильном приложении?',
        a: 'Разделение на слои presentation, domain и data с правилом зависимостей внутрь: доменный слой (сущности и сценарии использования) не знает ни об UI, ни о сети и базе. Данные поставляют репозитории, реализованные в data-слое по интерфейсам домена. Это упрощает тестирование и замену источников данных, но добавляет шаблонного кода — для маленьких приложений избыточно.',
      },
      {
        q: 'Что такое однонаправленный поток данных (MVI)?',
        a: 'Состояние экрана — один неизменяемый объект. Действия пользователя превращаются в намерения, которые обрабатывает редьюсер или ViewModel, выдавая новое состояние; UI только отображает его. Поток всегда в одну сторону, поэтому поведение предсказуемо, легко логировать и воспроизводить состояния. Побочные эффекты (навигация, тосты) выносят отдельно.',
      },
      {
        q: 'Зачем нужен DI в мобильном приложении?',
        a: 'Классы получают зависимости снаружи вместо создания внутри: их можно подменить заглушками в тестах, заменить реализацию без правки потребителей, управлять временем жизни (синглтоны, скоупы экрана). В Android стандарт — Hilt на базе Dagger с генерацией кода; в iOS часто достаточно ручного внедрения через инициализаторы и фабрик.',
      },
    ],
  },
  {
    id: 'mobile-network-storage',
    title: 'Сеть и хранение данных в мобильном приложении',
    area: MOB,
    level: 'plus',
    minutes: 60,
    summary: 'URLSession/Retrofit, кэширование, офлайн-режим, базы данных, безопасное хранение.',
    body: `### Сеть
- iOS: \`URLSession\` (async/await), \`Codable\` для JSON; Android: Retrofit + OkHttp, kotlinx.serialization/Moshi.
- Таймауты, ретраи для идемпотентных запросов, обработка отсутствия сети, отмена запросов при уходе с экрана.
- Авторизация: токен в заголовке, обновление по refresh-токену в перехватчике (interceptor), защита от одновременного обновления.
- Пагинация курсорами, сжатие, HTTP-кэш.
- Безопасность: только HTTPS, при необходимости certificate pinning.

### Хранение
| Что | iOS | Android |
|---|---|---|
| Настройки | UserDefaults | DataStore (SharedPreferences) |
| Секреты | Keychain | EncryptedSharedPreferences / Keystore |
| Структурированные данные | Core Data, SwiftData, SQLite (GRDB) | Room (SQLite) |
| Файлы | FileManager | internal/external storage |

Токены и пароли — **только** в Keychain/Keystore, не в UserDefaults и SharedPreferences.

### Офлайн-first
Локальная база — единый источник правды, UI подписан на неё; сеть синхронизирует данные в фоне. Действия офлайн складываются в очередь и отправляются при появлении сети (WorkManager на Android, BGTaskScheduler на iOS). Конфликты синхронизации решаются по временным меткам или правилам предметной области.

### Картинки
Кэш в памяти и на диске, даунсэмплинг до размера отображения (картинка 4000×3000 в ячейке 100×100 — лишние десятки мегабайт памяти), отмена загрузок. Библиотеки: Kingfisher/Nuke, Coil/Glide.`,
    questions: [
      {
        q: 'Где хранить токен авторизации в мобильном приложении?',
        a: 'В защищённом хранилище ОС: Keychain на iOS, Android Keystore (через EncryptedSharedPreferences или DataStore с шифрованием) на Android. UserDefaults и обычные SharedPreferences хранят данные открытым текстом и доступны при получении доступа к файловой системе устройства или резервной копии.',
      },
      {
        q: 'Как сделать приложение, работающее без сети?',
        a: 'Сделать локальную базу единственным источником правды: UI читает данные из неё и подписан на изменения, сеть только обновляет базу. Показывать сохранённые данные и индикатор офлайн-режима. Изменения пользователя сохранять локально и отправлять очередью при восстановлении связи (WorkManager, BGTaskScheduler), решая конфликты.',
      },
      {
        q: 'Как обновлять access-токен, если несколько запросов одновременно получили 401?',
        a: 'Централизовать обновление в перехватчике: первый запрос с 401 запускает обновление токена, остальные ждут его результата (мьютекс или общая задача), затем все повторяются с новым токеном. Если refresh-токен тоже истёк — разлогинить пользователя. Без синхронизации получится шквал параллельных обновлений и гонка токенов.',
      },
      {
        q: 'Почему большие изображения нужно даунсэмплить?',
        a: 'В памяти картинка хранится как несжатый битмап: ширина × высота × 4 байта. Фото 4000×3000 занимает около 48 МБ, даже если показывается в миниатюре. Декодирование в размер отображения экономит память и время, предотвращает падения по памяти и подтормаживания при прокрутке.',
      },
    ],
  },
  {
    id: 'android-kotlin',
    title: 'Kotlin',
    area: AND,
    level: 'base',
    minutes: 90,
    summary: 'Null safety, data и sealed классы, функции расширения, scope-функции, коллекции.',
    body: `### Null safety
Типы делятся на ненулевые (\`String\`) и нулевые (\`String?\`). Безопасный вызов \`?.\`, элвис \`?:\`, \`!!\` (бросает NPE — избегать), \`let\` для выполнения с ненулевым значением. Платформенные типы из Java (\`String!\`) — источник NPE.

### Классы
- \`data class\` — автоматически \`equals\`, \`hashCode\`, \`toString\`, \`copy\`, \`componentN\` по полям первичного конструктора.
- \`sealed class\`/\`sealed interface\` — закрытая иерархия, \`when\` без \`else\` проверяет полноту (состояния экрана, результаты).
- \`object\` — синглтон, \`companion object\` — «статические» члены.
- \`enum class\`, \`value class\` (обёртка без накладных расходов).
- Классы \`final\` по умолчанию, наследование — через \`open\`.

### Функции
Функции расширения (\`fun String.isEmail(): Boolean\`) — статически разрешаются, не меняют класс. Функции высшего порядка, лямбды, \`inline\` (встраивание лямбды — без аллокаций, возможен нелокальный return), именованные аргументы и значения по умолчанию.

### Scope-функции
| Функция | Объект внутри | Возвращает | Типичное применение |
|---|---|---|---|
| \`let\` | \`it\` | результат лямбды | работа с nullable |
| \`run\` | \`this\` | результат лямбды | вычислить на объекте |
| \`with\` | \`this\` | результат лямбды | группа вызовов |
| \`apply\` | \`this\` | сам объект | настройка объекта |
| \`also\` | \`it\` | сам объект | побочные действия |

### Коллекции
Только для чтения \`List\` и изменяемые \`MutableList\`. \`map\`, \`filter\`, \`groupBy\`, \`associateBy\`, \`fold\`. \`Sequence\` — ленивые цепочки для больших коллекций.

### Прочее
\`val\`/\`var\`, \`lateinit\`, \`by lazy\`, делегированные свойства, \`when\` как выражение, строковые шаблоны, \`Result\`, generics с \`in\`/\`out\` (вариантность) и \`reified\`.`,
    questions: [
      {
        q: 'Как работает null safety в Kotlin?',
        a: 'Система типов различает типы, допускающие null (String?), и не допускающие (String). Компилятор не даст вызвать метод у nullable без проверки: нужен безопасный вызов ?., оператор элвис ?: со значением по умолчанию, проверка if (x != null) со smart cast или !!, который бросит NPE. Остаточные риски — Java-код (платформенные типы) и lateinit.',
      },
      {
        q: 'Что даёт data class?',
        a: 'Компилятор генерирует equals и hashCode по свойствам первичного конструктора, читаемый toString, copy с изменением отдельных полей и componentN для деструктуризации. Удобно для моделей и состояний. Свойства вне первичного конструктора в equals не участвуют.',
      },
      {
        q: 'Зачем нужны sealed-классы?',
        a: 'Для закрытой иерархии, все наследники которой известны компилятору (объявлены в том же модуле и пакете). when по такому типу проверяется на полноту без ветки else — при добавлении нового варианта компилятор укажет все места, где его нужно обработать. Типичное применение — состояния экрана (Loading, Content, Error) и результаты операций.',
      },
      {
        q: 'Чем отличаются let, apply, also, run и with?',
        a: 'Они различаются тем, как доступен объект (this или it) и что возвращают. let (it, результат лямбды) — для nullable и преобразований. apply (this, сам объект) — для настройки при создании. also (it, сам объект) — для побочных действий вроде логирования. run (this, результат) — вычисление в контексте объекта. with — как run, но объект передаётся аргументом.',
      },
      {
        q: 'Как работают функции расширения?',
        a: 'Это статические функции, которые принимают объект как неявный первый параметр и вызываются с синтаксисом метода. Класс не меняется, доступа к приватным членам нет, разрешение статическое — по объявленному типу, а не фактическому, поэтому расширения не полиморфны. Если у класса есть метод с той же сигнатурой, побеждает метод.',
      },
    ],
    resources: [{ title: 'Kotlin: документация', url: 'https://kotlinlang.org/docs/home.html', kind: 'docs' }],
  },
  {
    id: 'android-components',
    title: 'Компоненты Android и жизненный цикл',
    area: AND,
    level: 'core',
    minutes: 90,
    summary: 'Activity и Fragment, смена конфигурации, Intent, Service, BroadcastReceiver, процесс приложения.',
    body: `### Четыре компонента
- **Activity** — экран с UI.
- **Service** — фоновая работа без UI (foreground service с уведомлением для долгих задач).
- **BroadcastReceiver** — реакция на системные и пользовательские события.
- **ContentProvider** — предоставление данных другим приложениям.
Все объявляются в \`AndroidManifest.xml\`.

### Жизненный цикл Activity
\`onCreate\` → \`onStart\` (видима) → \`onResume\` (на переднем плане, получает ввод) → \`onPause\` → \`onStop\` (не видна) → \`onDestroy\`. \`onRestart\` при возврате.

### Смена конфигурации
Поворот экрана, смена языка или темы по умолчанию **пересоздаёт Activity**: всё состояние в полях теряется. Сохранение:
- \`ViewModel\` переживает пересоздание (но не смерть процесса).
- \`onSaveInstanceState\` / \`SavedStateHandle\` / \`rememberSaveable\` — небольшое состояние, переживающее и смерть процесса.
- Постоянные данные — в БД.

### Fragment
Часть UI со своим жизненным циклом внутри Activity. Отдельный жизненный цикл view (\`onCreateView\` … \`onDestroyView\`) — подписки на данные привязывают к \`viewLifecycleOwner\`, иначе утечки и двойные подписки. В новых приложениях фрагменты заменяет Compose.

### Intent
Явный (конкретный компонент) и неявный (действие, система выбирает приложение). Передача данных через extras, Parcelable.

### Процесс и фон
Система может убить процесс приложения в фоне в любой момент. Фоновая работа ограничена: для отложенных гарантированных задач — **WorkManager**, для длительных видимых — foreground service. Главный поток нельзя блокировать больше ~5 секунд — **ANR**.

### Утечки памяти
Ссылка на Activity или Context из долгоживущих объектов (синглтонов, статических полей, незавершённых колбэков) не даёт освободить экран. Используйте \`applicationContext\` там, где не нужен UI, и LeakCanary для поиска.`,
    questions: [
      {
        q: 'Опишите жизненный цикл Activity.',
        a: 'onCreate — создание и инициализация UI; onStart — Activity видима; onResume — на переднем плане и взаимодействует с пользователем; onPause — теряет фокус (другая Activity частично перекрыла); onStop — полностью невидима; onDestroy — уничтожение (завершение или смена конфигурации). onRestart вызывается при возврате из остановленного состояния.',
      },
      {
        q: 'Что происходит при повороте экрана и как сохранить состояние?',
        a: 'По умолчанию Activity уничтожается и создаётся заново с новой конфигурацией. Данные, нужные экрану, хранят во ViewModel — она переживает пересоздание. Небольшое состояние UI (введённый текст, позиция), которое должно пережить и смерть процесса, сохраняют через SavedStateHandle или rememberSaveable. Отключать пересоздание через configChanges — плохая практика.',
      },
      {
        q: 'Что такое ANR и как его избежать?',
        a: 'Application Not Responding — система показывает диалог, если главный поток не отвечает на ввод около 5 секунд (или BroadcastReceiver выполняется слишком долго). Избегают, вынося сеть, базу данных, файлы и тяжёлые вычисления в фоновые потоки: корутины с Dispatchers.IO/Default, WorkManager.',
      },
      {
        q: 'Как в Android возникает утечка Activity?',
        a: 'Когда объект, живущий дольше экрана, держит ссылку на Activity или её Context: статическое поле или синглтон, внутренний класс или лямбда с незавершённой асинхронной задачей, неотписанный слушатель. После закрытия экрана сборщик мусора не может освободить Activity со всем её деревом view. Находят LeakCanary, исправляют слабыми ссылками, applicationContext, отпиской по жизненному циклу.',
      },
      {
        q: 'Чем отличается жизненный цикл Fragment и его view?',
        a: 'Фрагмент может существовать дольше своего view: при переходе в back stack view уничтожается (onDestroyView), а фрагмент остаётся. Если подписаться на LiveData или Flow с lifecycleOwner = this (фрагмент), подписки будут накапливаться при каждом пересоздании view. Поэтому используют viewLifecycleOwner, а ссылку на binding обнуляют в onDestroyView.',
      },
    ],
  },
  {
    id: 'android-coroutines',
    title: 'Корутины и Flow',
    area: AND,
    level: 'core',
    minutes: 90,
    summary: 'suspend, скоупы, диспетчеры, структурированная конкурентность, Flow и StateFlow.',
    body: `### Корутины
Лёгкие приостанавливаемые вычисления. \`suspend\`-функция может приостановиться, не блокируя поток, и продолжиться позже (возможно, на другом потоке).
- Билдеры: \`launch\` (без результата, возвращает \`Job\`), \`async\` (возвращает \`Deferred\`, результат через \`await\`), \`runBlocking\` (только для main и тестов).
- \`withContext(Dispatchers.IO) { }\` — переключение контекста для блока.

### Диспетчеры
\`Dispatchers.Main\` (UI), \`IO\` (блокирующий ввод-вывод, большой пул), \`Default\` (CPU-вычисления, по числу ядер), \`Unconfined\` (редко).

### Структурированная конкурентность
Корутины запускаются в **скоупе**; родитель ждёт детей, отмена родителя отменяет детей, ошибка ребёнка отменяет родителя и соседей (кроме \`SupervisorJob\`/\`supervisorScope\`). Скоупы Android: \`viewModelScope\` (отменяется при очистке ViewModel), \`lifecycleScope\`. \`GlobalScope\` — антипаттерн: утечки и неуправляемое время жизни.

### Отмена
Кооперативная: отмена срабатывает в точках приостановки (\`delay\`, \`yield\`, suspend-функции) или при проверке \`isActive\`/\`ensureActive()\`. \`CancellationException\` не нужно глотать в \`catch (e: Exception)\`.

### Обработка ошибок
Исключение в \`launch\` уходит в \`CoroutineExceptionHandler\` родителя; в \`async\` — сохраняется до \`await\`. Для независимых задач — \`supervisorScope\`.

### Flow
Холодный асинхронный поток значений: код выполняется при каждом \`collect\`. Операторы \`map\`, \`filter\`, \`debounce\`, \`flatMapLatest\` (поиск по вводу — отмена предыдущего запроса), \`combine\`, \`catch\`, \`flowOn\`.
- **StateFlow** — горячий, всегда есть текущее значение, для состояния UI.
- **SharedFlow** — горячий, для событий (одноразовые события — осторожно, или Channel).
- Сбор в UI с учётом жизненного цикла: \`repeatOnLifecycle(STARTED)\` или \`collectAsStateWithLifecycle\` в Compose.`,
    questions: [
      {
        q: 'Чем корутина отличается от потока?',
        a: 'Поток — ресурс ОС с собственным стеком, дорогой в создании и переключении. Корутина — лёгкая единица работы, которая выполняется на пуле потоков и в точке приостановки освобождает поток без блокировки; её состояние хранится в объекте-продолжении. Можно запускать сотни тысяч корутин. Корутины кооперативны: отмена и переключение происходят в точках приостановки.',
      },
      {
        q: 'Что такое структурированная конкурентность?',
        a: 'Правило, при котором каждая корутина запускается в скоупе и имеет родителя: родитель не завершается, пока не завершатся дети; отмена родителя отменяет всех детей; ошибка ребёнка по умолчанию отменяет родителя и соседей. Это исключает «потерянные» корутины и утечки. В Android скоупы привязаны к жизненному циклу — viewModelScope, lifecycleScope.',
      },
      {
        q: 'Чем launch отличается от async?',
        a: 'launch запускает корутину без результата и возвращает Job для управления (отмена, ожидание). async возвращает Deferred, результат получают через await. Исключение в launch сразу распространяется к родителю, в async — выбрасывается при await (в зависимости от скоупа). async используют для параллельных вычислений с результатом.',
      },
      {
        q: 'Чем Flow отличается от StateFlow и SharedFlow?',
        a: 'Flow — холодный: выполняет код заново для каждого подписчика и только при сборе. StateFlow — горячий, хранит текущее значение, новый подписчик сразу получает последнее, повторяющиеся значения не эмитит — для состояния UI. SharedFlow — горячий поток событий с настраиваемым replay и буфером, без обязательного текущего значения.',
      },
      {
        q: 'Почему нельзя использовать GlobalScope?',
        a: 'Корутины в GlobalScope живут всё время жизни приложения и не отменяются вместе с экраном: при уходе пользователя работа продолжается, держит ссылки на UI (утечки), результаты некуда доставить. Нарушается структурированная конкурентность, ошибки теряются. Нужно запускать в скоупе с понятным временем жизни.',
      },
    ],
  },
  {
    id: 'android-jetpack',
    title: 'Jetpack: ViewModel, Compose, Room, Navigation',
    area: AND,
    level: 'core',
    minutes: 75,
    summary: 'Современный стек Android: ViewModel, Jetpack Compose, рекомпозиция, Room, WorkManager.',
    body: `### ViewModel
Хранит состояние экрана и переживает смену конфигурации. Создаётся через \`by viewModels()\` / \`hiltViewModel()\`. Не должна держать ссылки на Activity, View, Context экрана. Работа — в \`viewModelScope\`. Состояние наружу — \`StateFlow<UiState>\`.

### Jetpack Compose
Декларативный UI: \`@Composable\` функции описывают интерфейс по состоянию.
- **Рекомпозиция** — повторный вызов composable при изменении прочитанного им состояния. Compose пропускает функции, чьи параметры стабильны и не изменились.
- \`remember\` — сохранить значение между рекомпозициями, \`rememberSaveable\` — и при пересоздании Activity.
- \`mutableStateOf\` — наблюдаемое состояние.
- **State hoisting** — состояние поднимается к вызывающему, composable получает значение и колбэк (stateless компоненты легче переиспользовать и тестировать).
- Побочные эффекты: \`LaunchedEffect(key)\` (корутина, перезапускается при смене ключа), \`DisposableEffect\` (с очисткой), \`SideEffect\`, \`rememberCoroutineScope\`, \`derivedStateOf\`.
- Списки — \`LazyColumn\` с \`key\` для стабильной идентичности.
- Нестабильные параметры (обычный \`List\`, лямбды, создаваемые каждый раз) вызывают лишние рекомпозиции.

### Room
ORM поверх SQLite: \`@Entity\`, \`@Dao\` с запросами, проверяемыми при компиляции, возвращает \`Flow\` для наблюдения, миграции схемы.

### Прочее
Navigation (граф экранов, аргументы, deep links), WorkManager (гарантированные фоновые задачи с ограничениями: сеть, зарядка), DataStore (вместо SharedPreferences), Paging 3, Hilt.`,
    questions: [
      {
        q: 'Почему ViewModel переживает поворот экрана и когда она уничтожается?',
        a: 'ViewModel хранится в ViewModelStore, который привязан не к экземпляру Activity, а к записи в её владельце и сохраняется при смене конфигурации. Уничтожается (onCleared, отмена viewModelScope), когда экран завершается окончательно: пользователь ушёл назад, вызван finish, фрагмент удалён из back stack. При смерти процесса ViewModel теряется.',
      },
      {
        q: 'Что такое рекомпозиция в Compose?',
        a: 'Повторный вызов composable-функций, которые читали изменившееся состояние, чтобы обновить UI. Compose отслеживает чтения State и перезапускает только затронутые области; функции со стабильными неизменившимися параметрами пропускаются. Рекомпозиция может происходить часто и в любом порядке, поэтому composable должны быть без побочных эффектов, а эффекты — в LaunchedEffect и аналогах.',
      },
      {
        q: 'Чем remember отличается от rememberSaveable?',
        a: 'remember сохраняет значение между рекомпозициями, пока composable находится в композиции, но теряет его при пересоздании Activity (поворот) и смерти процесса. rememberSaveable дополнительно сохраняет значение в Bundle, поэтому оно переживает смену конфигурации и восстановление процесса; подходит для небольших сериализуемых значений.',
      },
      {
        q: 'Что такое state hoisting?',
        a: 'Подъём состояния из composable к вызывающему: компонент получает текущее значение и колбэк изменения (value, onValueChange) вместо хранения своего состояния. Такой компонент без состояния легко переиспользовать, тестировать и превьюить, а источник правды один — обычно ViewModel.',
      },
      {
        q: 'Когда использовать WorkManager?',
        a: 'Для отложенной фоновой работы, которая должна быть выполнена гарантированно, даже после перезапуска приложения или устройства: синхронизация данных, отправка логов, загрузка файлов. Поддерживает ограничения (есть сеть, заряжается), ретраи с задержкой, цепочки задач. Для немедленной работы, пока пользователь на экране, достаточно корутин.',
      },
    ],
  },
]
