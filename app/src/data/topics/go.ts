import type { Topic } from '../../types'

const G = 'Go'

export const golang: Topic[] = [
  {
    id: 'go-basics',
    title: 'Типы Go: слайсы, мапы, строки',
    area: G,
    level: 'base',
    minutes: 90,
    summary: 'Устройство слайса и append, мапы, строки и руны, значения и указатели.',
    body: `### Слайсы
Слайс — структура из трёх полей: **указатель** на массив, **длина** и **ёмкость**. Несколько слайсов могут смотреть в один массив.
\`\`\`go
a := []int{1, 2, 3, 4}
b := a[1:3]       // [2 3], len 2, cap 3 — общий массив с a
b[0] = 20         // a == [1 20 3 4]
b = append(b, 99) // места хватает — перезапишет a[3]: a == [1 20 3 99]
\`\`\`
- \`append\` при нехватке ёмкости выделяет новый массив (рост примерно вдвое для маленьких, медленнее для больших) и копирует — после этого слайсы «расходятся».
- Передача слайса в функцию копирует заголовок, а не данные: изменение элементов видно снаружи, а \`append\` внутри — нет (длина у вызывающего прежняя). Поэтому \`s = append(s, x)\` и возврат слайса.
- Полная форма среза \`a[low:high:max]\` ограничивает ёмкость и защищает от случайной перезаписи.
- Нулевой слайс \`nil\` — длина 0, append работает; \`make([]T, 0, n)\` — заранее выделить ёмкость.

### Мапы
Хеш-таблица, ссылочный тип. Запись в nil-мапу — **паника**, чтение — нулевое значение. Проверка наличия: \`v, ok := m[k]\`. Порядок обхода намеренно случайный. **Мапа не потокобезопасна**: одновременная запись из горутин приводит к фатальной ошибке — нужен \`sync.Mutex\` или \`sync.Map\`. Брать адрес элемента мапы нельзя (элементы переезжают при росте).

### Строки
Строка — неизменяемая последовательность **байтов** (обычно UTF-8). \`len(s)\` — байты, не символы. \`for i, r := range s\` итерирует по рунам (\`rune\` = \`int32\`, кодовая точка). \`[]rune(s)\` — для работы с символами. Сборка строк — \`strings.Builder\`.

### Значения и указатели
Всё передаётся **по значению**: структура копируется целиком. Указатель — чтобы изменить исходную структуру или не копировать большую. Методы с получателем-указателем (\`func (s *S) M()\`) могут менять структуру.

### Нулевые значения и объявления
Каждый тип имеет нулевое значение: 0, "", false, nil. \`var x int\`, \`x := 1\`, \`const\`, \`iota\`. Неиспользуемые переменные и импорты — ошибка компиляции.`,
    questions: [
      {
        q: 'Как устроен слайс и что происходит при append?',
        a: 'Слайс — заголовок из указателя на базовый массив, длины и ёмкости. append дописывает элемент, если есть свободная ёмкость, — в тот же массив (что может перезаписать данные другого слайса, смотрящего туда же). Если ёмкости не хватает, выделяется новый массив большего размера (примерно вдвое для небольших слайсов), элементы копируются, и возвращается заголовок на новый массив.',
      },
      {
        q: 'Почему append внутри функции не меняет слайс у вызывающего?',
        a: 'В функцию передаётся копия заголовка слайса. append меняет длину (и, возможно, указатель) у копии, а у вызывающего остаётся старая длина. Элементы в пределах старой длины при этом могут поменяться, если ёмкости хватило. Правильно — возвращать слайс из функции или передавать указатель на слайс.',
      },
      {
        q: 'Что будет при записи в nil-мапу? А при одновременной записи из горутин?',
        a: 'Запись в nil-мапу вызывает панику assignment to entry in nil map — мапу нужно создать через make или литерал. Одновременная запись (или запись и чтение) из нескольких горутин без синхронизации приводит к гонке, и рантайм обычно завершает программу фатальной ошибкой concurrent map writes. Нужен sync.Mutex/RWMutex или sync.Map.',
      },
      {
        q: 'Чем len(s) отличается от количества символов в строке?',
        a: 'len возвращает число байтов. Кириллический символ в UTF-8 занимает 2 байта, эмодзи — 4. Число символов — utf8.RuneCountInString(s) или len([]rune(s)). Индексация s[i] даёт байт, а range по строке — руны.',
      },
      {
        q: 'Почему порядок обхода мапы случайный?',
        a: 'Разработчики Go специально рандомизируют начальную позицию обхода, чтобы программисты не полагались на порядок, который зависит от реализации и может меняться при росте мапы. Для детерминированного вывода ключи собирают в слайс и сортируют.',
      },
      {
        q: 'Когда использовать получатель-указатель в методе?',
        a: 'Если метод меняет состояние структуры, если структура большая (чтобы не копировать) и для единообразия — если хотя бы один метод типа с указателем, обычно все делают с указателем. Получатель-значение — для маленьких неизменяемых типов. Важно для интерфейсов: методы с указателем входят в набор методов только *T.',
      },
    ],
    resources: [
      { title: 'Tour of Go', url: 'https://go.dev/tour/', kind: 'course' },
      { title: 'Go Slices: usage and internals', url: 'https://go.dev/blog/slices-intro', kind: 'article' },
    ],
  },
  {
    id: 'go-interfaces',
    title: 'Интерфейсы, ошибки и структуры',
    area: G,
    level: 'core',
    minutes: 75,
    summary: 'Неявная реализация интерфейсов, nil-интерфейс, обработка ошибок, встраивание.',
    body: `### Интерфейсы
Тип реализует интерфейс **неявно** — достаточно иметь нужные методы. Интерфейсы объявляют там, где их используют, и делают маленькими (\`io.Reader\`, \`io.Writer\`, \`fmt.Stringer\`).

Значение интерфейса — пара **(тип, значение)**. Ловушка:
\`\`\`go
func find() error {
    var e *MyErr = nil
    return e          // интерфейс (тип *MyErr, значение nil) != nil!
}
if err := find(); err != nil { /* сработает */ }
\`\`\`
Интерфейс равен nil, только если и тип, и значение nil.

\`any\` (\`interface{}\`) — любой тип. Приведение: \`v, ok := x.(T)\`, type switch \`switch v := x.(type)\`.

### Ошибки
Ошибки — обычные значения, возвращаемые последними. Проверка \`if err != nil\` сразу после вызова.
- Оборачивание с контекстом: \`fmt.Errorf("load user %d: %w", id, err)\`.
- Проверка: \`errors.Is(err, sql.ErrNoRows)\` — сравнение по цепочке, \`errors.As(err, &target)\` — поиск типа.
- Сентинельные ошибки: \`var ErrNotFound = errors.New("not found")\`.
- \`panic\` — для невозможных ситуаций, \`recover\` в \`defer\` — перехват паники (например, в HTTP-сервере, чтобы не упал весь процесс).

### defer
Откладывает вызов до выхода из функции, выполняется в порядке LIFO. Аргументы вычисляются **в момент defer**, а не при выполнении. Используется для закрытия ресурсов и разблокировки мьютексов. Именованные возвращаемые значения можно менять в defer.

### Структуры и встраивание
Композиция вместо наследования: встроенная структура «поднимает» свои поля и методы. Это не наследование — нет полиморфизма по встроенному типу.

### Дженерики
С Go 1.18: \`func Map[T, U any](xs []T, f func(T) U) []U\`, ограничения через интерфейсы (\`constraints.Ordered\`, \`comparable\`).`,
    questions: [
      {
        q: 'Почему функция, вернувшая nil-указатель как error, даёт err != nil?',
        a: 'Интерфейсное значение хранит пару (динамический тип, значение). Возвращая типизированный nil-указатель *MyErr, мы получаем интерфейс с типом *MyErr и значением nil — это не nil-интерфейс, у которого пустые обе части. Правильно — возвращать литерал nil, когда ошибки нет.',
      },
      {
        q: 'Как в Go правильно обрабатывать и оборачивать ошибки?',
        a: 'Проверять err сразу после вызова и либо обработать, либо вернуть наверх с контекстом: fmt.Errorf("операция: %w", err). %w сохраняет исходную ошибку в цепочке, поэтому выше можно проверить errors.Is(err, ErrNotFound) или достать тип через errors.As. Не логировать и возвращать одновременно — ошибку обрабатывают один раз.',
      },
      {
        q: 'В каком порядке выполняются defer и когда вычисляются их аргументы?',
        a: 'Отложенные вызовы выполняются в обратном порядке (LIFO) при выходе из функции, в том числе при панике. Аргументы вычисляются в момент объявления defer: defer fmt.Println(i) напечатает значение i на тот момент. Замыкание defer func(){ fmt.Println(i) }() прочитает i в момент выполнения.',
      },
      {
        q: 'Чем встраивание структур отличается от наследования?',
        a: 'Встраивание — композиция: поля и методы встроенного типа доступны через внешний тип, но внешний тип не является встроенным, нельзя передать его туда, где ожидается встроенный тип. Если внешний тип определяет метод с тем же именем, он затеняет встроенный. Полиморфизм в Go достигается интерфейсами.',
      },
      {
        q: 'Когда использовать panic?',
        a: 'Для непредвиденных ситуаций, которые означают ошибку программиста или невозможность продолжать (нарушенный инвариант, невозможная ветка, ошибка инициализации). Для ожидаемых ошибок (нет файла, неверный ввод) — возвращать error. recover применяют на границах — в обработчике HTTP или воркере, — чтобы одна паника не уронила весь сервис.',
      },
    ],
  },
  {
    id: 'go-concurrency',
    title: 'Горутины, каналы и select',
    area: G,
    level: 'core',
    minutes: 120,
    summary: 'Главная тема Go-собеседований: каналы, их закрытие, select, типичные паттерны.',
    body: `### Горутины
Лёгкие потоки, управляемые рантаймом Go: стартовый стек ~2 КБ (растёт динамически), создание дешёвое, можно запускать сотни тысяч. \`go f()\` запускает функцию конкурентно. Программа завершается, когда завершается \`main\` — не дожидаясь горутин.

### Каналы
- Небуферизованный \`make(chan T)\`: отправка блокируется, пока получатель не заберёт значение — синхронизация «рандеву».
- Буферизованный \`make(chan T, n)\`: отправка блокируется, только когда буфер полон.
- **Закрывает канал отправитель**, и только один раз. Чтение из закрытого канала сразу возвращает нулевое значение и \`ok == false\`; \`for v := range ch\` завершается после закрытия.

| Операция | nil-канал | закрытый канал |
|---|---|---|
| отправка | блокировка навсегда | **паника** |
| чтение | блокировка навсегда | нулевое значение, ok=false |
| close | **паника** | **паника** |

### select
Ждёт готовности одного из каналов; если готовы несколько — выбирает случайно. \`default\` делает операцию неблокирующей. Таймаут — \`case <-time.After(d)\` или контекст.

### Паттерны
**Worker pool** — задача с интервью Авито:
\`\`\`go
func process(ctx context.Context, jobs []Job, workers int) []Result {
    in := make(chan Job)
    out := make(chan Result)
    var wg sync.WaitGroup
    for i := 0; i < workers; i++ {
        wg.Add(1)
        go func() {
            defer wg.Done()
            for j := range in {
                select {
                case out <- handle(j):
                case <-ctx.Done():
                    return
                }
            }
        }()
    }
    go func() {                     // отправитель закрывает in
        defer close(in)
        for _, j := range jobs {
            select {
            case in <- j:
            case <-ctx.Done():
                return
            }
        }
    }()
    go func() { wg.Wait(); close(out) } () // закрыть out, когда все воркеры завершились
    var res []Result
    for r := range out {
        res = append(res, r)
    }
    return res
}
\`\`\`
**Fan-in** (слияние каналов): горутина на каждый вход пересылает в общий выход, \`WaitGroup\` закрывает выход после всех. **Fan-out** — несколько воркеров читают один канал. **Pipeline** — цепочка стадий, соединённых каналами. **Semaphore** — буферизованный канал \`make(chan struct{}, n)\`.

### Утечки горутин
Горутина, навсегда заблокированная на канале, никогда не освободится. Причины: никто не читает результат, никто не закрывает канал, нет выхода по \`ctx.Done()\`. Каждой горутине — понятное условие завершения.`,
    questions: [
      {
        q: 'Что будет при отправке в закрытый канал, чтении из него и повторном закрытии?',
        a: 'Отправка в закрытый канал — паника. Чтение возвращает оставшиеся в буфере значения, а затем нулевое значение с ok == false без блокировки. Повторное закрытие (как и закрытие nil-канала) — паника. Поэтому закрывает канал только отправитель, и только когда больше ничего не отправит.',
      },
      {
        q: 'Чем буферизованный канал отличается от небуферизованного?',
        a: 'Небуферизованный синхронизирует отправителя и получателя: отправка завершится только когда получатель забрал значение. Буферизованный позволяет отправить до n значений без получателя и блокирует, только когда буфер полон (а получение — когда пуст). Буфер сглаживает разницу скоростей, но не должен использоваться для маскировки проблем синхронизации.',
      },
      {
        q: 'Как написать worker pool с корректным завершением?',
        a: 'Канал задач, N горутин-воркеров, которые читают из него через range, и канал результатов. Отправитель задач закрывает канал задач после отправки всех — воркеры выходят из range. WaitGroup считает воркеров; отдельная горутина делает wg.Wait() и закрывает канал результатов, чтобы потребитель результатов вышел из цикла. Для отмены — context, проверка ctx.Done() в select при отправке и получении.',
        companies: ['avito'],
      },
      {
        q: 'Как объединить несколько каналов в один (fan-in)?',
        a: 'Для каждого входного канала запустить горутину, которая читает из него через range и пересылает значения в общий выходной канал. WaitGroup ждёт завершения всех пересылающих горутин, после чего отдельная горутина закрывает выходной канал. Альтернатива для фиксированного числа каналов — select в цикле с обнулением закрытых каналов (nil-канал в select никогда не готов).',
      },
      {
        q: 'Что такое утечка горутин и как её избежать?',
        a: 'Горутина, которая заблокирована навсегда (ждёт отправки в канал, который никто не читает, или чтения из канала, который никто не закроет), не освобождает память и ресурсы. Избегают, давая каждой горутине условие выхода: закрытие входного канала, context с отменой, буфер для результатов, если получатель может уйти раньше. Находят через pprof goroutine или goleak в тестах.',
      },
      {
        q: 'Как работает select, если готовы несколько каналов?',
        a: 'Выбирается один из готовых случаев псевдослучайно, чтобы не было голодания. Если ни один не готов — select блокируется, а при наличии default выполняется default. Случай с nil-каналом никогда не выбирается — этим пользуются, чтобы динамически отключать ветки.',
      },
      {
        q: 'Как ограничить число одновременно выполняющихся горутин?',
        a: 'Семафор на буферизованном канале: sem := make(chan struct{}, N); перед работой sem <- struct{}{}, после — <-sem. Или worker pool с фиксированным числом воркеров. Или golang.org/x/sync/errgroup с SetLimit.',
      },
    ],
    resources: [{ title: 'Go Concurrency Patterns: Pipelines and cancellation', url: 'https://go.dev/blog/pipelines', kind: 'article' }],
  },
  {
    id: 'go-sync',
    title: 'sync, context и гонки данных',
    area: G,
    level: 'core',
    minutes: 75,
    summary: 'Mutex, WaitGroup, Once, atomic, context, graceful shutdown и поиск гонок.',
    body: `### Пакет sync
- \`Mutex\` / \`RWMutex\` — взаимное исключение; \`RWMutex\` пускает много читателей. Мьютекс нельзя копировать после использования (передавайте структуру по указателю).
- \`WaitGroup\` — ждать завершения группы горутин. \`Add\` вызывать **до** запуска горутины, а не внутри неё.
- \`Once\` — выполнить инициализацию один раз.
- \`sync.Map\` — для специфичных сценариев (много чтений, ключи редко меняются); обычно хватает мапы с мьютексом.
- \`sync.Pool\` — переиспользование временных объектов для снижения нагрузки на GC.
- \`atomic\` — атомарные операции над числами и указателями (\`atomic.Int64\`, \`CompareAndSwap\`).

### Классические баги (задача из Wildberries)
\`\`\`go
var count int
var wg sync.WaitGroup
for i := 0; i < 10; i++ {
    go func() {
        wg.Add(1)          // ошибка: Add может выполниться после Wait
        defer wg.Done()
        count++            // гонка данных
        fmt.Println(i)     // до Go 1.22 — захват общей переменной цикла
    }()
}
wg.Wait()
\`\`\`
Исправления: \`wg.Add(1)\` перед \`go\`, \`count\` под мьютексом или \`atomic\`, передать \`i\` аргументом (в Go 1.22+ у каждой итерации своя переменная).

### Детектор гонок
\`go test -race\`, \`go run -race\` — инструментирует доступы к памяти и сообщает о гонках во время выполнения.

### context
Передаёт отмену, дедлайны и значения уровня запроса по цепочке вызовов. Первый параметр функции: \`ctx context.Context\`.
- \`context.WithCancel\`, \`WithTimeout\`, \`WithDeadline\` — всегда вызывайте \`cancel()\` (обычно через \`defer\`).
- \`ctx.Done()\` — канал, закрывающийся при отмене; \`ctx.Err()\` — причина.
- Значения (\`WithValue\`) — только для данных запроса (trace id), не для параметров функций.

### Graceful shutdown
\`\`\`go
srv := &http.Server{Addr: ":8080", Handler: h}
go srv.ListenAndServe()
stop := make(chan os.Signal, 1)
signal.Notify(stop, syscall.SIGINT, syscall.SIGTERM)
<-stop
ctx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
defer cancel()
srv.Shutdown(ctx) // перестаёт принимать новые запросы и ждёт текущие
\`\`\``,
    questions: [
      {
        q: 'Найдите ошибки: горутины в цикле увеличивают общий счётчик и вызывают wg.Add внутри горутины.',
        a: 'Три проблемы. wg.Add внутри горутины может выполниться после wg.Wait — Wait вернётся раньше, чем горутины отработают; Add нужно вызывать до go. count++ из многих горутин — гонка данных: нужен мьютекс или atomic. Захват переменной цикла в замыкании до Go 1.22 даёт общую переменную для всех итераций — передавать значение аргументом.',
        companies: ['wb'],
      },
      {
        q: 'Зачем нужен context и как им пользоваться?',
        a: 'Для распространения отмены, дедлайнов и значений уровня запроса через границы API и горутин. Если клиент отключился или истёк таймаут, все дочерние операции получают сигнал через ctx.Done() и прекращают работу. Контекст передают первым параметром, создают производные через WithTimeout/WithCancel и обязательно вызывают cancel, чтобы освободить ресурсы.',
      },
      {
        q: 'Чем Mutex отличается от RWMutex?',
        a: 'Mutex даёт эксклюзивный доступ одному владельцу. RWMutex разделяет блокировку на чтение (RLock — много одновременных читателей) и запись (Lock — эксклюзивно). RWMutex выгоден, когда чтений много больше, чем записей, и критическая секция не микроскопическая; иначе накладные расходы делают его медленнее обычного мьютекса.',
      },
      {
        q: 'Что такое graceful shutdown и как его сделать в Go?',
        a: 'Корректное завершение: перестать принимать новые запросы, дождаться текущих, закрыть соединения с БД и брокерами. Ловим SIGTERM через signal.Notify, вызываем http.Server.Shutdown(ctx) с таймаутом, отменяем контекст фоновых воркеров и ждём их через WaitGroup, затем закрываем ресурсы. Важно в Kubernetes, где под получает SIGTERM перед остановкой.',
      },
      {
        q: 'Как найти гонку данных?',
        a: 'Запустить тесты или программу с флагом -race: детектор отслеживает обращения к памяти и сообщает о конкурентных несинхронизированных доступах с трассировками обеих горутин. Он находит только гонки, которые реально произошли во время запуска, поэтому нужны тесты с конкурентной нагрузкой.',
      },
      {
        q: 'Когда использовать atomic вместо мьютекса?',
        a: 'Для простых операций над одним значением — счётчики, флаги, указатель на конфигурацию: atomic быстрее и не блокирует. Если нужно согласованно изменить несколько полей или выполнить составную операцию — мьютекс.',
      },
    ],
  },
  {
    id: 'go-runtime',
    title: 'Рантайм Go: планировщик и сборщик мусора',
    area: G,
    level: 'plus',
    minutes: 60,
    summary: 'Модель GMP, escape analysis, устройство GC.',
    body: `### Планировщик GMP
- **G** — горутина, **M** — поток ОС, **P** — логический процессор с локальной очередью горутин. Число P = \`GOMAXPROCS\` (по умолчанию — число ядер).
- M выполняет горутины, только держа P. Когда горутина блокируется в системном вызове, P отдаётся другому M, чтобы остальные горутины продолжали работать.
- **Work stealing**: P с пустой очередью крадёт горутины у других.
- Вытесняющая многозадачность: с Go 1.14 горутину можно прервать асинхронно, даже в цикле без вызовов.
- Сетевой ввод-вывод идёт через netpoller (epoll/kqueue): горутина «блокируется» логически, а поток ОС свободен.

### Память и escape analysis
Компилятор решает, где разместить переменную: на **стеке** горутины (дёшево, освобождается автоматически) или в **куче** (управляется GC). Переменная «убегает» в кучу, если на неё остаётся ссылка после выхода из функции, если её размер неизвестен при компиляции, если она попадает в интерфейс. Посмотреть: \`go build -gcflags=-m\`.

### Сборщик мусора
Конкурентный, **трёхцветный mark-and-sweep**, без поколений и без перемещения объектов. Большая часть работы идёт параллельно с программой, паузы (stop-the-world) — доли миллисекунды. Барьер записи сохраняет корректность, пока программа меняет указатели во время маркировки. Настройки: \`GOGC\` (при каком росте кучи запускать сборку, по умолчанию 100%), \`GOMEMLIMIT\` (мягкий лимит памяти).

### Как снизить нагрузку на GC
Меньше аллокаций: предвыделять слайсы (\`make([]T, 0, n)\`), переиспользовать буферы (\`sync.Pool\`), избегать лишних преобразований \`string\` ↔ \`[]byte\`, указателей в огромных структурах. Профилирование — \`pprof\` (CPU, heap, allocs, goroutine).`,
    questions: [
      {
        q: 'Как устроен планировщик горутин?',
        a: 'Модель GMP: горутины (G) выполняются на потоках ОС (M), которым для работы нужен логический процессор (P), число P равно GOMAXPROCS. У каждого P своя очередь готовых горутин, есть глобальная очередь и кража работы у соседей. При блокирующем системном вызове P передаётся другому потоку. Сетевые операции идут через netpoller, не блокируя поток. С Go 1.14 есть асинхронное вытеснение.',
      },
      {
        q: 'Что такое escape analysis?',
        a: 'Анализ компилятора, который определяет, может ли переменная жить на стеке. Если ссылка на неё переживает функцию (возвращается указатель, сохраняется в глобальной переменной или замыкании, передаётся в интерфейс), переменная размещается в куче и становится заботой GC. Результаты видны через go build -gcflags=-m.',
      },
      {
        q: 'Как работает сборщик мусора Go?',
        a: 'Конкурентный трёхцветный mark-and-sweep: объекты делятся на белые (не посещены), серые (найдены, но не просканированы) и чёрные (просканированы). Маркировка идёт параллельно с программой, барьер записи не даёт потерять объекты, на которые программа создаёт ссылки. Короткие паузы нужны только на старте и завершении маркировки. Затем недостижимые белые объекты освобождаются. Поколений и уплотнения нет.',
      },
      {
        q: 'Чем горутина дешевле потока ОС?',
        a: 'Маленький стек (около 2 КБ), который растёт по мере необходимости, вместо мегабайт у потока; переключение между горутинами происходит в пространстве пользователя планировщиком Go без системных вызовов и смены контекста ядра; создание и уничтожение дешёвые. Поэтому можно держать сотни тысяч горутин.',
      },
    ],
  },
]
