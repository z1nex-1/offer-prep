import type { Topic } from '../../types'

const J = 'Java'

export const java: Topic[] = [
  {
    id: 'java-core',
    title: 'Java Core: JVM, типы, String, equals и hashCode',
    area: J,
    level: 'base',
    minutes: 90,
    summary: 'JDK/JRE/JVM, примитивы и обёртки, неизменяемость String, контракт equals/hashCode.',
    body: `### JDK, JRE, JVM
- **JVM** исполняет байт-код (\`.class\`), управляет памятью, JIT-компилирует горячий код в машинный.
- **JRE** — JVM + стандартная библиотека. **JDK** — JRE + компилятор и инструменты.
- «Написано однажды — работает везде» благодаря байт-коду.

### Примитивы и обёртки
8 примитивов: \`byte, short, int, long, float, double, char, boolean\`. Обёртки (\`Integer\` и т. д.) — объекты, нужны для коллекций и дженериков. Автоупаковка скрывает ловушки:
- \`Integer a = 127, b = 127; a == b\` → true (кэш −128…127), а для 128 → false. Объекты сравнивают через \`equals\`.
- Распаковка \`null\` в \`int\` → \`NullPointerException\`.

### Передача параметров
Всегда **по значению**. Для объектов значение — ссылка: метод может изменить состояние объекта, но не может заменить объект у вызывающего.

### String
Неизменяем (безопасность, кэширование хеша, пул строк). Литералы попадают в **String pool**: \`"a" == "a"\` → true, \`new String("a") == "a"\` → false. Конкатенация в цикле — \`StringBuilder\` (не синхронизирован) или \`StringBuffer\` (синхронизирован, почти не нужен).

### equals и hashCode
Контракт:
- Равные по \`equals\` объекты **обязаны** иметь равные \`hashCode\`.
- Равные хеши не означают равенства.
- \`equals\` рефлексивен, симметричен, транзитивен, согласован, \`x.equals(null) == false\`.
Если переопределить только \`equals\`, объекты «потеряются» в \`HashMap\`/\`HashSet\`. \`hashCode\`, возвращающий константу, корректен, но превращает хеш-таблицу в список. Поля, участвующие в \`hashCode\`, не должны меняться, пока объект лежит в коллекции.

### Модификаторы и ключевые слова
\`final\` (переменная — нельзя переприсвоить, метод — нельзя переопределить, класс — нельзя наследовать), \`static\`, \`abstract\`, модификаторы доступа (\`private\` → package-private → \`protected\` → \`public\`). \`record\` (Java 16+) — неизменяемые классы данных с готовыми \`equals\`/\`hashCode\`/\`toString\`.

### Object
Методы: \`equals\`, \`hashCode\`, \`toString\`, \`getClass\`, \`clone\`, \`finalize\` (устарел), \`wait\`/\`notify\`/\`notifyAll\`.`,
    questions: [
      {
        q: 'Расскажите про контракт equals и hashCode.',
        a: 'Если a.equals(b), то a.hashCode() == b.hashCode(); обратное не обязательно. equals должен быть рефлексивным, симметричным, транзитивным, согласованным и возвращать false для null. Нарушение контракта ломает хеш-коллекции: объект, равный искомому, окажется в другой корзине и не будет найден. Переопределяют оба метода на одном наборе неизменяемых полей.',
      },
      {
        q: 'Почему String неизменяем?',
        a: 'Безопасность (строки используются как имена классов, пути, параметры подключения — их нельзя подменить после проверки), потокобезопасность без синхронизации, возможность пула строк (одна копия литерала для всех), кэширование hashCode — строки эффективны как ключи HashMap.',
      },
      {
        q: 'Что выведет Integer a = 128, b = 128; a == b?',
        a: 'false: == сравнивает ссылки, а Integer.valueOf кэширует только значения от −128 до 127. Для 127 результат был бы true. Объекты-обёртки нужно сравнивать через equals.',
      },
      {
        q: 'Java передаёт параметры по ссылке или по значению?',
        a: 'Всегда по значению. Для объектов передаётся копия ссылки: метод может изменить состояние объекта через неё, но присваивание параметру нового объекта не повлияет на переменную вызывающего.',
      },
      {
        q: 'Чем отличаются JDK, JRE и JVM?',
        a: 'JVM — виртуальная машина, которая загружает и исполняет байт-код, управляет памятью и JIT-компиляцией. JRE — среда выполнения: JVM и стандартные библиотеки. JDK — набор разработчика: JRE плюс компилятор javac, отладчик, профилировщики и другие инструменты.',
      },
      {
        q: 'Что будет, если переопределить только equals?',
        a: 'hashCode останется унаследованным от Object (обычно зависит от адреса), и два равных объекта попадут в разные корзины HashMap/HashSet. set.contains(копия) вернёт false, в множестве появятся «дубликаты». Именно такие баги ищут на код-ревью-задачах в Сбере.',
        companies: ['sber'],
      },
    ],
  },
  {
    id: 'java-collections',
    title: 'Коллекции Java',
    area: J,
    level: 'core',
    minutes: 90,
    summary: 'List, Set, Map, их реализации и сложности; устройство HashMap.',
    body: `### Иерархия
\`Collection\` → \`List\` (упорядоченный, с дубликатами), \`Set\` (без дубликатов), \`Queue\`/\`Deque\`. \`Map\` — отдельная иерархия.

| Реализация | Основа | get / contains | add | Порядок |
|---|---|---|---|---|
| ArrayList | динамический массив | O(1) по индексу, O(n) поиск | O(1) аморт. | вставки |
| LinkedList | двусвязный список | O(n) | O(1) в концы | вставки |
| HashSet / HashMap | хеш-таблица | O(1) в среднем | O(1) | нет |
| LinkedHashMap | хеш + список | O(1) | O(1) | вставки или доступа |
| TreeMap / TreeSet | красно-чёрное дерево | O(log n) | O(log n) | сортировка |
| ArrayDeque | кольцевой массив | — | O(1) | стек и очередь |
| PriorityQueue | двоичная куча | O(1) peek | O(log n) | по приоритету |

### Устройство HashMap
- Массив корзин, индекс = \`hash(key) & (n − 1)\`, ёмкость — степень двойки. Хеш «перемешивается» (\`h ^ (h >>> 16)\`), чтобы старшие биты влияли на индекс.
- Коллизии — связный список в корзине; при длине > 8 (и ёмкости ≥ 64) список превращается в **красно-чёрное дерево** — худший случай O(log n).
- **Load factor** 0,75: при превышении ёмкость удваивается, элементы перераспределяются.
- Допускает один \`null\`-ключ. Не потокобезопасен.

### Потокобезопасные варианты
\`ConcurrentHashMap\` (блокировки на уровне корзин и CAS, чтение без блокировок), \`CopyOnWriteArrayList\` (для редких записей), \`Collections.synchronizedMap\` (одна блокировка на всё — медленно), \`BlockingQueue\` для producer–consumer.

### Итераторы
Итераторы большинства коллекций — **fail-fast**: изменение коллекции во время обхода (не через итератор) бросает \`ConcurrentModificationException\`. Удалять при обходе — через \`iterator.remove()\` или \`removeIf\`.

### Сравнение
\`Comparable\` — естественный порядок (\`compareTo\` в самом классе), \`Comparator\` — внешний порядок: \`Comparator.comparing(User::age).thenComparing(User::name)\`.`,
    questions: [
      {
        q: 'Как устроен HashMap?',
        a: 'Массив корзин, индекс вычисляется из хеша ключа (с перемешиванием старших битов) по маске ёмкости. В корзине — связный список пар, при большом числе коллизий (больше 8 в корзине при ёмкости от 64) он превращается в красно-чёрное дерево. При заполнении выше load factor 0,75 массив удваивается и элементы перераспределяются. get и put — O(1) в среднем.',
      },
      {
        q: 'Чем ArrayList отличается от LinkedList? Что выбрать?',
        a: 'ArrayList — массив: доступ по индексу O(1), вставка в конец амортизированно O(1), в середину O(n) со сдвигом, хорошая локальность данных. LinkedList — двусвязный список: вставка и удаление по итератору O(1), доступ по индексу O(n), большой расход памяти на узлы и плохая работа с кэшем. На практике почти всегда ArrayList; для очереди и стека — ArrayDeque.',
      },
      {
        q: 'Чем HashMap отличается от ConcurrentHashMap и Hashtable?',
        a: 'HashMap не потокобезопасен и допускает null. Hashtable — устаревший, все методы synchronized (одна блокировка), null запрещён. ConcurrentHashMap потокобезопасен с тонкой синхронизацией: чтения без блокировок, запись блокирует только корзину (плюс CAS), поддерживает атомарные операции compute, merge, putIfAbsent; null запрещён.',
      },
      {
        q: 'Когда возникает ConcurrentModificationException и как её избежать?',
        a: 'Когда коллекцию структурно меняют во время обхода итератором не через сам итератор — например, удаляют элемент в for-each. Итератор замечает изменение счётчика modCount. Решения: iterator.remove(), removeIf, сбор изменений и применение после обхода, потокобезопасные коллекции для многопоточного доступа.',
      },
      {
        q: 'Когда использовать TreeMap?',
        a: 'Когда ключи нужны в отсортированном порядке или нужны запросы «ближайший ключ»: floorKey, ceilingKey, headMap, tailMap, диапазоны. Операции O(log n). Ключи должны быть Comparable или нужен Comparator.',
      },
    ],
  },
  {
    id: 'java-oop',
    title: 'ООП в Java, исключения и дженерики',
    area: J,
    level: 'core',
    minutes: 75,
    summary: 'Интерфейсы и абстрактные классы, checked/unchecked исключения, try-with-resources, generics.',
    body: `### Интерфейс или абстрактный класс
- **Интерфейс** — контракт; класс может реализовать много интерфейсов. С Java 8 — \`default\` и \`static\` методы, с Java 9 — \`private\`. Нет состояния (только константы).
- **Абстрактный класс** — частичная реализация с состоянием и конструктором; наследовать можно только один.
- **sealed** классы (Java 17) ограничивают список наследников.

### Перегрузка и переопределение
Перегрузка (overload) — одно имя, разные параметры, выбор при компиляции. Переопределение (override) — та же сигнатура в подклассе, выбор во время выполнения. Нельзя сузить видимость и расширить checked-исключения.

### Исключения
\`Throwable\` → \`Error\` (OutOfMemoryError — не ловим) и \`Exception\`. **Checked** (IOException, SQLException) нужно объявить или обработать. **Unchecked** (\`RuntimeException\`: NPE, IllegalArgumentException) — ошибки программирования.
\`\`\`java
try (var in = new BufferedReader(new FileReader(path))) {   // try-with-resources
    return in.readLine();
} catch (IOException e) {
    throw new UncheckedIOException("read " + path, e);
}
\`\`\`
\`finally\` выполняется всегда (кроме \`System.exit\`); \`return\` в \`finally\` перекрывает исключение — так делать нельзя.

### Дженерики
Проверка типов при компиляции, стираются во время выполнения (**type erasure**): нельзя \`new T()\`, \`instanceof List<String>\`.
- \`<? extends T>\` — читать как T (producer), \`<? super T>\` — записывать T (consumer). Правило **PECS**.
- \`List<Integer>\` не является подтипом \`List<Number>\` (инвариантность).

### Внутренние классы и лямбды
Статические вложенные, внутренние (держат ссылку на внешний объект — возможная утечка памяти), анонимные, лямбды (функциональный интерфейс с одним абстрактным методом). Лямбда может захватывать только effectively final переменные.`,
    questions: [
      {
        q: 'Чем интерфейс отличается от абстрактного класса?',
        a: 'Класс может реализовать несколько интерфейсов, но наследовать только один класс. Абстрактный класс может иметь состояние (поля), конструкторы и методы с любой видимостью; интерфейс — только константы и методы (абстрактные, default, static, private). Интерфейс описывает возможность («умеет сравниваться»), абстрактный класс — общую основу родственных классов.',
      },
      {
        q: 'Чем checked-исключения отличаются от unchecked?',
        a: 'Checked (наследники Exception, кроме RuntimeException) компилятор заставляет объявить в throws или обработать — это ожидаемые внешние сбои, например IOException. Unchecked (RuntimeException и наследники) не требуют объявления — обычно ошибки программирования: NullPointerException, IllegalArgumentException. Error — серьёзные проблемы JVM, их не обрабатывают.',
      },
      {
        q: 'Что такое try-with-resources?',
        a: 'Конструкция, которая автоматически закрывает ресурсы, реализующие AutoCloseable, в обратном порядке открытия — даже если произошло исключение. Если исключение бросило и тело, и close, основное сохраняется, а исключение close добавляется как suppressed. Заменяет громоздкий finally с проверками на null.',
      },
      {
        q: 'Что такое type erasure и какие у него последствия?',
        a: 'Информация о параметрах дженериков стирается при компиляции: List<String> и List<Integer> во время выполнения — один класс List. Поэтому нельзя создать new T() или массив T[], проверить instanceof List<String>, перегрузить методы, отличающиеся только параметром дженерика. Компилятор вставляет приведения типов.',
      },
      {
        q: 'Что означает PECS?',
        a: 'Producer Extends, Consumer Super. Если коллекция выдаёт значения (читаем) — используем ? extends T; если принимает значения (пишем) — ? super T. Например, Collections.copy(List<? super T> dest, List<? extends T> src).',
      },
    ],
  },
  {
    id: 'java-stream',
    title: 'Stream API, лямбды и Optional',
    area: J,
    level: 'core',
    minutes: 60,
    summary: 'Промежуточные и терминальные операции, ленивость, коллекторы, Optional.',
    body: `### Stream API
Поток — конвейер обработки данных: источник → промежуточные операции (ленивые) → терминальная операция (запускает вычисление).
\`\`\`java
Map<String, Long> byCity = users.stream()
    .filter(u -> u.age() >= 18)
    .collect(Collectors.groupingBy(User::city, Collectors.counting()));

List<String> top = orders.stream()
    .sorted(Comparator.comparingLong(Order::total).reversed())
    .limit(3)
    .map(Order::id)
    .toList();
\`\`\`
- Промежуточные: \`filter\`, \`map\`, \`flatMap\`, \`distinct\`, \`sorted\`, \`peek\`, \`limit\`, \`skip\`.
- Терминальные: \`collect\`, \`forEach\`, \`reduce\`, \`count\`, \`anyMatch\`, \`findFirst\`, \`min/max\`, \`toList\`.
- Поток одноразовый, не хранит данные, не должен изменять источник.
- Ленивость: \`filter\` и \`map\` выполняются поэлементно и только до нужного количества (с \`findFirst\` обработка остановится на первом совпадении).
- \`parallelStream\` — параллельная обработка в общем ForkJoinPool; оправдан для больших объёмов CPU-работы без блокировок.

### Коллекторы
\`toList\`, \`toSet\`, \`toMap\` (осторожно с дубликатами ключей — нужен merge), \`groupingBy\`, \`partitioningBy\`, \`joining\`, \`summingInt\`, \`averagingDouble\`.

### Функциональные интерфейсы
\`Function<T,R>\`, \`Predicate<T>\`, \`Supplier<T>\`, \`Consumer<T>\`, \`BiFunction\`, \`UnaryOperator\`. Ссылки на методы: \`String::length\`, \`this::process\`, \`User::new\`.

### Optional
Контейнер «значение или пусто» для возвращаемых значений: \`map\`, \`flatMap\`, \`filter\`, \`orElse\` (вычисляет аргумент всегда), \`orElseGet\` (лениво), \`orElseThrow\`. Не используют как поле класса и параметр метода, не вызывают \`get()\` без проверки.`,
    questions: [
      {
        q: 'Чем промежуточные операции Stream отличаются от терминальных?',
        a: 'Промежуточные (filter, map, sorted) возвращают новый поток и ленивы: ничего не вычисляют, пока не вызвана терминальная операция. Терминальные (collect, forEach, reduce, count) запускают конвейер и возвращают результат или побочный эффект, после чего поток использовать нельзя.',
      },
      {
        q: 'Чем map отличается от flatMap?',
        a: 'map преобразует каждый элемент в один новый элемент: Stream<T> → Stream<R>. flatMap преобразует каждый элемент в поток и «расплющивает» их в один: Stream<List<T>> → Stream<T>. Например, из списка заказов получить поток всех товаров — orders.stream().flatMap(o -> o.items().stream()).',
      },
      {
        q: 'Чем orElse отличается от orElseGet?',
        a: 'orElse(value) всегда вычисляет аргумент, даже если Optional не пуст — если там дорогой вызов или побочный эффект, это проблема. orElseGet(supplier) вызывает поставщика только когда значения нет.',
      },
      {
        q: 'Когда parallelStream ухудшает производительность?',
        a: 'На маленьких коллекциях (накладные расходы на разбиение больше выгоды), при блокирующих операциях внутри (занимают потоки общего ForkJoinPool, мешая всему приложению), для источников, которые плохо делятся (LinkedList, итераторы), при операциях, требующих порядка, и при общих изменяемых данных.',
      },
    ],
  },
  {
    id: 'java-concurrency',
    title: 'Многопоточность в Java',
    area: J,
    level: 'core',
    minutes: 120,
    summary: 'synchronized, volatile, JMM, ExecutorService, CompletableFuture, java.util.concurrent.',
    body: `### Основы
Поток — \`Thread\` или задача в пуле. Состояния: NEW, RUNNABLE, BLOCKED, WAITING, TIMED_WAITING, TERMINATED. Создавать потоки вручную на каждую задачу дорого — используют **пулы**.

### synchronized и мониторы
Каждый объект имеет монитор. \`synchronized\` метод или блок захватывает монитор: взаимное исключение + видимость изменений. Реентерабелен (поток может захватить повторно). \`wait/notify\` — ожидание условия внутри synchronized.

### volatile и Java Memory Model
**volatile** гарантирует видимость записи другим потокам и запрет переупорядочивания вокруг неё, но **не атомарность** составных операций (\`count++\`). JMM определяет отношение **happens-before**: освобождение монитора → последующий захват, запись volatile → чтение, \`Thread.start\`, \`join\`.

### java.util.concurrent
- \`ExecutorService\`: \`Executors.newFixedThreadPool\`, \`ThreadPoolExecutor\` (ядро, максимум, очередь, политика отказа). Виртуальные потоки (Java 21): \`Executors.newVirtualThreadPerTaskExecutor()\` — дешёвые потоки для I/O.
- \`Future\`, \`CompletableFuture\` — асинхронные цепочки: \`supplyAsync\`, \`thenApply\`, \`thenCompose\`, \`thenCombine\`, \`exceptionally\`, \`allOf\`.
- Блокировки: \`ReentrantLock\` (tryLock, таймауты, справедливость), \`ReadWriteLock\`, \`StampedLock\`.
- Атомики: \`AtomicInteger\`, \`AtomicReference\`, \`LongAdder\` (для счётчиков под высокой конкуренцией).
- Синхронизаторы: \`CountDownLatch\`, \`CyclicBarrier\`, \`Semaphore\`.
- Коллекции: \`ConcurrentHashMap\`, \`BlockingQueue\`, \`CopyOnWriteArrayList\`.

### Типичная задача с секции
«Исправьте код»: общий \`HashMap\` из нескольких потоков, \`count++\` без синхронизации, двойная проверка без volatile, deadlock из-за разного порядка блокировок, пул, который никто не закрывает (\`shutdown\`).

### Double-checked locking
\`\`\`java
private static volatile Singleton instance;   // volatile обязателен
static Singleton get() {
    if (instance == null) {
        synchronized (Singleton.class) {
            if (instance == null) instance = new Singleton();
        }
    }
    return instance;
}
\`\`\`
Без volatile другой поток может увидеть ссылку на ещё не до конца сконструированный объект.`,
    questions: [
      {
        q: 'Что гарантирует synchronized и чем он отличается от volatile?',
        a: 'synchronized даёт взаимное исключение (один поток в критической секции) и видимость: изменения, сделанные до выхода из блока, видны потоку, который затем захватит тот же монитор. volatile даёт только видимость и упорядочивание для одной переменной, без взаимного исключения — составные операции вроде count++ остаются небезопасными.',
      },
      {
        q: 'Почему в double-checked locking нужен volatile?',
        a: 'Создание объекта — несколько шагов: выделить память, вызвать конструктор, записать ссылку. Без volatile JVM и процессор могут переставить запись ссылки перед завершением конструктора, и другой поток на первой проверке увидит ненулевую, но не до конца инициализированную ссылку. volatile запрещает такое переупорядочивание.',
      },
      {
        q: 'Как устроен ThreadPoolExecutor?',
        a: 'Параметры: corePoolSize, maximumPoolSize, keepAliveTime, очередь задач и политика отказа. Новая задача: если потоков меньше core — создаётся поток; иначе задача кладётся в очередь; если очередь полна и потоков меньше максимума — создаётся ещё поток; иначе срабатывает политика отказа (AbortPolicy, CallerRunsPolicy и др.). Опасность Executors.newFixedThreadPool — неограниченная очередь, которая может съесть память.',
      },
      {
        q: 'Что такое CompletableFuture и чем он лучше Future?',
        a: 'Future позволяет только заблокироваться на get. CompletableFuture поддерживает композицию асинхронных шагов без блокировок: thenApply (преобразовать), thenCompose (последовательная асинхронная операция), thenCombine (объединить две), allOf/anyOf, обработку ошибок exceptionally/handle, выбор пула для выполнения.',
      },
      {
        q: 'Как избежать deadlock?',
        a: 'Захватывать блокировки в едином глобальном порядке, держать их минимальное время, не вызывать чужой код под блокировкой, использовать tryLock с таймаутом у ReentrantLock, предпочитать высокоуровневые конструкции (ConcurrentHashMap, очереди) ручной синхронизации. Диагностика — thread dump (jstack), где видно циклическое ожидание.',
      },
      {
        q: 'Что такое виртуальные потоки в Java 21?',
        a: 'Лёгкие потоки, управляемые JVM, а не ОС: при блокирующем вводе-выводе виртуальный поток «открепляется» от несущего потока ОС, и тот выполняет другие задачи. Позволяют писать простой блокирующий код и держать миллионы одновременных задач. Не ускоряют CPU-bound работу; осторожно с synchronized вокруг блокирующих операций (pinning в ранних версиях).',
      },
    ],
    resources: [{ title: 'Goetz «Java Concurrency in Practice»', url: 'https://jcip.net', kind: 'book' }],
  },
  {
    id: 'java-jvm',
    title: 'Память JVM и сборка мусора',
    area: J,
    level: 'plus',
    minutes: 75,
    summary: 'Heap и stack, поколения, сборщики G1/ZGC, утечки памяти в Java.',
    body: `### Области памяти
- **Heap** — объекты, общая для потоков, управляется GC.
- **Stack** — у каждого потока: кадры методов, локальные примитивы и ссылки. Переполнение — \`StackOverflowError\`.
- **Metaspace** — метаданные классов (вместо PermGen с Java 8).
- Code cache — скомпилированный JIT код.

### Поколения
Гипотеза: большинство объектов умирает молодыми.
- **Young generation**: Eden + два Survivor. Новые объекты — в Eden. Minor GC копирует живых в Survivor, после нескольких переживаний — в Old.
- **Old generation** — долгоживущие объекты, собираются реже (major/full GC).

### Сборщики
- **Serial**, **Parallel** — простые, с паузами stop-the-world.
- **G1** (по умолчанию с Java 9) — делит кучу на регионы, собирает сначала самые «мусорные», целевое время паузы (\`-XX:MaxGCPauseMillis\`).
- **ZGC**, **Shenandoah** — почти вся работа конкурентно, паузы в миллисекунды даже на огромных кучах.

Алгоритм — от корней (GC roots: стеки потоков, статические поля, JNI) помечаются достижимые объекты, остальные — мусор. Циклические ссылки не мешают.

### Утечки памяти в Java
Объект, на который осталась ненужная ссылка, не соберётся: статические коллекции-кэши без ограничения, незакрытые ресурсы, слушатели, которых забыли отписать, внутренние классы, держащие внешний объект, \`ThreadLocal\` в пулах потоков.

### Инструменты
\`-Xms\`/\`-Xmx\` (размер кучи), логи GC, \`jstat\`, \`jmap\` (дамп кучи), \`jstack\` (дамп потоков), VisualVM, Java Flight Recorder, Eclipse MAT для анализа дампов.

### Ссылки
Strong, Soft (очищаются при нехватке памяти — кэши), Weak (очищаются при следующей сборке — \`WeakHashMap\`), Phantom (для действий после сборки).`,
    questions: [
      {
        q: 'Как устроена память JVM?',
        a: 'Куча — общая для всех потоков, в ней объекты; делится на молодое поколение (Eden и два Survivor) и старое. Стек у каждого потока — кадры вызовов, локальные примитивы и ссылки. Metaspace — метаданные классов в нативной памяти. Кроме того, есть область скомпилированного кода и память под потоки и прямые буферы.',
      },
      {
        q: 'Как работает сборщик мусора с поколениями?',
        a: 'Новые объекты создаются в Eden. Когда он заполняется, выполняется быстрая minor GC: живые объекты копируются в Survivor, а пережившие несколько сборок — в старое поколение. Большинство объектов умирает молодыми, поэтому молодое поколение собирается дёшево. Старое поколение собирается реже и дольше. Достижимость определяется от GC roots.',
      },
      {
        q: 'Может ли в Java быть утечка памяти?',
        a: 'Да: если на ненужные объекты остаются достижимые ссылки, GC их не соберёт. Частые причины — растущие статические коллекции и кэши без вытеснения, незакрытые соединения и потоки, неотписанные слушатели, ThreadLocal в пулах потоков, нестатические внутренние классы, держащие внешний объект. Находят анализом дампа кучи.',
      },
      {
        q: 'Чем StackOverflowError отличается от OutOfMemoryError?',
        a: 'StackOverflowError — переполнение стека потока, обычно из-за бесконечной или слишком глубокой рекурсии. OutOfMemoryError — не удалось выделить память: в куче (Java heap space), в metaspace (слишком много классов), нельзя создать поток и т. д. Первое лечится исправлением рекурсии, второе — поиском утечки или увеличением лимитов.',
      },
    ],
  },
  {
    id: 'java-spring',
    title: 'Spring и работа с БД',
    area: J,
    level: 'plus',
    minutes: 90,
    summary: 'IoC и DI, бины и их области, транзакции, Spring Boot, JPA/Hibernate.',
    body: `### IoC и DI
Контейнер Spring создаёт объекты (**бины**) и внедряет зависимости. Способы внедрения: через конструктор (рекомендуется — неизменяемые поля, явные зависимости, удобно тестировать), сеттер, поле.

Аннотации: \`@Component\`, \`@Service\`, \`@Repository\`, \`@Controller\`/\`@RestController\`, \`@Configuration\` + \`@Bean\`, \`@Autowired\`, \`@Qualifier\`, \`@Value\`.

### Области видимости бинов
\`singleton\` (по умолчанию, один на контейнер — должен быть потокобезопасным), \`prototype\` (новый при каждом запросе), \`request\`, \`session\`.

### Жизненный цикл
Создание → внедрение зависимостей → \`@PostConstruct\` → использование → \`@PreDestroy\`. BeanPostProcessor позволяет оборачивать бины — так работают прокси.

### Прокси и AOP
\`@Transactional\`, \`@Cacheable\`, \`@Async\` работают через **прокси** вокруг бина. Ловушка: вызов аннотированного метода из того же класса (\`this.method()\`) идёт мимо прокси — транзакция не откроется.

### Транзакции
\`@Transactional\`: распространение (\`REQUIRED\` — присоединиться или создать, \`REQUIRES_NEW\` — новая, приостановив текущую), изоляция, \`readOnly\`, откат по умолчанию только на **unchecked** исключения.

### Spring Boot
Автоконфигурация, стартеры, встроенный сервер, \`application.yml\`, профили, Actuator (health, metrics).

### JPA и Hibernate
Сущности (\`@Entity\`), связи (\`@OneToMany\`, \`@ManyToOne\`), ленивая (\`LAZY\`) и жадная (\`EAGER\`) загрузка. **N+1**: при обходе списка сущностей ленивые связи подгружаются отдельными запросами — лечится \`JOIN FETCH\`, \`@EntityGraph\`, batch size. \`LazyInitializationException\` — обращение к ленивой связи вне сессии. Кэш первого уровня (в пределах сессии) и второго уровня.`,
    questions: [
      {
        q: 'Что такое IoC и DI в Spring?',
        a: 'Inversion of Control — объекты не создают свои зависимости сами, их создаёт и связывает контейнер. Dependency Injection — способ реализации: зависимости передаются через конструктор, сеттер или поле. Это ослабляет связанность, позволяет подменять реализации (например, в тестах) и централизованно управлять жизненным циклом.',
      },
      {
        q: 'Почему @Transactional не работает при вызове метода из того же класса?',
        a: 'Транзакционность реализована через прокси вокруг бина: транзакцию открывает прокси, когда вызов приходит снаружи. Вызов this.method() внутри класса идёт напрямую к объекту, минуя прокси, и аннотация игнорируется. Решения — вынести метод в другой бин, внедрить себя через прокси или использовать AspectJ.',
      },
      {
        q: 'Какие есть области видимости бинов?',
        a: 'singleton — один экземпляр на контейнер (по умолчанию); prototype — новый экземпляр при каждом получении; в веб-приложениях request, session, application, websocket. Singleton-бины разделяются между потоками, поэтому не должны хранить изменяемое состояние запроса.',
      },
      {
        q: 'Что такое проблема N+1 в Hibernate и как её решить?',
        a: 'Загрузив список из N сущностей одним запросом, при обращении к ленивой связи каждой сущности Hibernate делает ещё по запросу — всего N+1. Решения: JOIN FETCH в JPQL, @EntityGraph, настройка batch-size (подгружать связи пачками через IN), DTO-проекции, если нужны только некоторые поля.',
      },
      {
        q: 'При каких исключениях @Transactional откатывает транзакцию?',
        a: 'По умолчанию только при unchecked-исключениях (RuntimeException и Error). Checked-исключения транзакцию не откатывают, если не указать rollbackFor. Если исключение перехвачено внутри метода и не выброшено наружу, отката тоже не будет.',
      },
    ],
  },
]
