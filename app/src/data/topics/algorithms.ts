import type { Topic } from '../../types'

const A = 'Алгоритмы'

export const algorithms: Topic[] = [
  {
    id: 'algo-complexity',
    title: 'Сложность алгоритмов и O-нотация',
    area: A,
    level: 'base',
    minutes: 60,
    summary: 'Как оценить время и память решения и понять, пройдёт ли оно по ограничениям.',
    body: `### Зачем это спрашивают
Любая алгоритмическая секция заканчивается вопросом «какая сложность?». В Яндексе и Т-Банке это отдельный критерий оценки: решение без оценки сложности считается незаконченным.

### O-нотация
- **O(f(n))** — верхняя граница роста числа операций при росте входа. Константы и младшие слагаемые отбрасываются: \`3n² + 10n\` → \`O(n²)\`.
- Оцениваем **худший случай**, если не сказано иное. Для хеш-таблиц говорим «в среднем O(1)».
- **Амортизированная** сложность — среднее на операцию по длинной серии. Добавление в динамический массив: иногда O(n) на копирование, но амортизированно O(1).

### Шкала на практике
Примерно 10⁸ простых операций в секунду (C++/Go/Java), в Python — в 10–50 раз меньше.

| n | Допустимая сложность |
|---|---|
| ≤ 10–12 | O(n!), перебор перестановок |
| ≤ 20–25 | O(2ⁿ), перебор подмножеств |
| ≤ 500 | O(n³) |
| ≤ 5000 | O(n²) |
| ≤ 10⁶ | O(n log n) |
| ≤ 10⁸ | O(n), O(log n) |

### Типичные сложности
- Проход по массиву — O(n); вложенный цикл по парам — O(n²).
- Сортировка сравнениями — O(n log n), быстрее нельзя.
- Бинарный поиск, операции со сбалансированным деревом и кучей — O(log n).
- Доступ по индексу, хеш-таблица в среднем — O(1).
- Рекурсия без мемоизации вида \`f(n-1) + f(n-2)\` — O(2ⁿ).

### Память
Считается дополнительная память сверх входа. Рекурсия тратит стек: глубина n — O(n) памяти. Копирование строки в цикле в Python или Java создаёт новые объекты: конкатенация строк в цикле — O(n²).

### Как отвечать на интервью
Назовите время и память отдельно, объясните, откуда они берутся («сортировка — n log n, проход — n, итого n log n»), и скажите, можно ли лучше.`,
    questions: [
      {
        q: 'Чем отличаются O, Θ и Ω?',
        a: 'O — верхняя оценка (растёт не быстрее), Ω — нижняя (не медленнее), Θ — точная (и то и другое). На собеседованиях под «O» обычно подразумевают точную оценку худшего случая, но полезно показать, что вы знаете разницу.',
      },
      {
        q: 'Какая сложность у добавления элемента в конец динамического массива (vector, ArrayList, list в Python)?',
        a: 'Амортизированно O(1). Когда ёмкость заканчивается, массив перевыделяется с увеличением в константу раз (обычно ×1,5 или ×2) и копируется за O(n). Но такие копирования происходят всё реже, и суммарная стоимость n вставок — O(n).',
      },
      {
        q: 'Почему сортировка сравнениями не может быть быстрее O(n log n)?',
        a: 'Любой алгоритм сравнений можно представить деревом решений. Листьев должно быть не меньше n! (все перестановки), значит высота дерева ≥ log₂(n!) ≈ n log n. Быстрее сортируют только алгоритмы, использующие структуру ключей: подсчётом, поразрядная — O(n + k).',
      },
      {
        q: 'Какая сложность у рекурсивного вычисления чисел Фибоначчи и как её улучшить?',
        a: 'Наивная рекурсия — O(φⁿ) ≈ O(1,618ⁿ), потому что одни и те же значения пересчитываются. Мемоизация или итеративный проход дают O(n) времени; хранение двух последних значений — O(1) памяти. Быстрое возведение матрицы в степень — O(log n).',
      },
      {
        q: 'n = 2·10⁵. Пройдёт ли решение O(n²)?',
        a: 'Нет: 4·10¹⁰ операций — это десятки секунд даже на C++. Нужно O(n log n) или O(n): сортировка, хеш-таблица, два указателя, префиксные суммы.',
      },
      {
        q: 'Что такое пространственная сложность рекурсивного обхода дерева?',
        a: 'O(h), где h — высота дерева: столько кадров одновременно лежит на стеке вызовов. Для сбалансированного дерева это O(log n), для вырожденного («палки») — O(n). На больших глубинах рекурсия может упасть с переполнением стека, тогда переписывают на явный стек.',
      },
    ],
    resources: [
      { title: 'Хендбук Яндекса «Основы алгоритмов»', url: 'https://education.yandex.ru/handbook/algorithms', kind: 'course' },
      { title: 'Big-O Cheat Sheet', url: 'https://www.bigocheatsheet.com/', kind: 'article' },
    ],
  },
  {
    id: 'algo-arrays',
    title: 'Массивы, строки и два указателя',
    area: A,
    level: 'base',
    minutes: 120,
    summary: 'Самая частая тема контестов: проходы по массиву, два указателя, обработка строк.',
    body: `### Идея двух указателей
Два индекса двигаются по массиву так, что каждый проходит не больше n шагов — итого O(n) вместо O(n²).

**Встречные указатели** (отсортированный массив): \`l = 0, r = n-1\`. Если сумма меньше цели — двигаем \`l\`, больше — \`r\`. Задачи: пара с заданной суммой, палиндром, контейнер с водой.

**Однонаправленные** (быстрый и медленный): \`slow\` указывает, куда писать, \`fast\` — что читать. Задачи: удалить дубликаты на месте, сдвинуть нули в конец, слить два отсортированных массива.

\`\`\`python
def remove_duplicates(a):  # a отсортирован
    if not a:
        return 0
    w = 1
    for r in range(1, len(a)):
        if a[r] != a[w - 1]:
            a[w] = a[r]
            w += 1
    return w
\`\`\`

### Строки
- Строки неизменяемы в Python, Java, Go, JS: сборка через \`''.join\`, \`StringBuilder\`, \`strings.Builder\`, массив + \`join\`.
- Сравнение символов, подсчёт частот через массив на 26/128 элементов или словарь.
- Анаграммы — сравнение частот или отсортированных строк.
- Палиндромы — два указателя, для «почти палиндрома» — одна попытка пропуска символа.

### Приёмы, которые часто спасают
- **Сортировка как предобработка**: после неё работают два указателя и бинпоиск.
- **Обработка групп подряд идущих элементов**: RLE-сжатие «AAAB → A3B», сворачивание чисел в диапазоны «1,2,3,5 → 1-3,5».
- **Разворот на месте**: поворот массива на k — три разворота.

### Краевые случаи
Пустой массив, один элемент, все элементы одинаковые, отрицательные числа, переполнение при сумме (в C++/Java — \`long\`), пробелы и регистр в строках, Unicode.`,
    questions: [
      {
        q: 'Как найти в отсортированном массиве пару чисел с суммой X за O(n) и O(1) памяти?',
        a: 'Два указателя с концов. Если a[l] + a[r] == X — ответ; если меньше — l++, потому что с текущим r сумму можно увеличить только увеличив левое; если больше — r--. Каждый шаг отбрасывает один элемент, поэтому шагов не больше n.',
      },
      {
        q: 'Как сдвинуть все нули в конец массива, сохранив порядок остальных элементов, на месте?',
        a: 'Указатель записи w = 0. Проходим массив: если элемент не ноль — пишем его в a[w] и w++. Затем заполняем нулями позиции с w до конца. O(n) времени, O(1) памяти. Вариант со swap делает это за один проход.',
      },
      {
        q: 'Почему конкатенация строк в цикле может быть O(n²)?',
        a: 'Строки неизменяемы: каждая операция s += c создаёт новую строку и копирует старую. Суммарно копируется 1 + 2 + … + n символов. Правильно — собирать части в список/буфер и соединять один раз. CPython иногда оптимизирует += на месте, но на это нельзя рассчитывать.',
      },
      {
        q: 'Как проверить, что две строки — анаграммы?',
        a: 'Сравнить счётчики символов: массив на 26 элементов для латиницы или словарь для произвольных символов — O(n). Сортировка обеих строк — O(n log n). Сначала стоит сравнить длины.',
      },
      {
        q: 'Как сжать массив [1,4,5,2,3,9,8,11,0] в строку диапазонов «0-5,8-9,11»? (задача с интервью VK)',
        a: 'Отсортировать, затем пройти, группируя подряд идущие числа: запоминаем начало группы, пока следующий = текущий + 1 — продолжаем. Группу из одного числа печатаем как число, иначе «start-end». Не забыть последнюю группу и дубликаты (их стоит пропускать). O(n log n).',
        companies: ['vk'],
      },
      {
        q: 'Как развернуть массив на k позиций вправо за O(n) и O(1) памяти?',
        a: 'k = k mod n. Развернуть весь массив, затем развернуть первые k элементов и оставшиеся n − k. Три разворота на месте.',
      },
    ],
  },
  {
    id: 'algo-hash',
    title: 'Хеш-таблицы и множества',
    area: A,
    level: 'base',
    minutes: 90,
    summary: 'Главный инструмент ускорения с O(n²) до O(n): подсчёт, поиск пары, группировка.',
    body: `### Как устроена хеш-таблица
Ключ → хеш-функция → индекс корзины. Коллизии решаются **цепочками** (в корзине список, в Java 8+ при длине > 8 — дерево) или **открытой адресацией** (ищем следующую свободную ячейку: так устроены dict в Python и map в Go начиная с версии 1.24).

- Средняя сложность вставки, поиска, удаления — **O(1)**, худшая — O(n) при плохом хеше или атаке.
- **Коэффициент заполнения** (load factor): при превышении порога (0,75 в Java HashMap, ~2/3 в Python) таблица расширяется и все элементы перехешируются.
- Ключ должен быть **неизменяемым** и иметь согласованные equals/hash: равные объекты — равные хеши.

### Паттерны
1. **Подсчёт частот**: самые частые элементы, анаграммы, первый уникальный символ.
2. **Поиск дополнения**: «две суммы» — для каждого x проверяем, был ли уже target − x.
3. **Группировка по ключу**: анаграммы по отсортированной строке, события по пользователю.
4. **Множество посещённых**: дубликаты, циклы, BFS.
5. **Префиксные суммы + словарь**: количество подмассивов с суммой k.

\`\`\`python
def two_sum(nums, target):
    seen = {}
    for i, x in enumerate(nums):
        if target - x in seen:
            return [seen[target - x], i]
        seen[x] = i
\`\`\`

### Чего не делает хеш-таблица
Не хранит порядок (Python dict сохраняет порядок вставки с 3.7, Go map — намеренно случайный порядок обхода), не умеет искать «ближайший» ключ — для этого нужно дерево (TreeMap, std::map) или сортировка.`,
    questions: [
      {
        q: 'Почему поиск в хеш-таблице O(1) только в среднем?',
        a: 'Если много ключей попадает в одну корзину (плохая хеш-функция, специально подобранные ключи), поиск превращается в проход по цепочке — O(n). Java с 8-й версии превращает длинные цепочки в красно-чёрное дерево, это даёт O(log n) в худшем случае.',
      },
      {
        q: 'Что будет, если изменить объект, который уже лежит ключом в HashMap/dict?',
        a: 'Его хеш изменится, и таблица будет искать его не в той корзине: get вернёт null, а объект «потеряется», хотя физически останется в таблице. Поэтому ключи должны быть неизменяемыми. В Python list нельзя сделать ключом именно по этой причине — он нехешируемый.',
      },
      {
        q: 'Как найти количество подмассивов с суммой ровно k?',
        a: 'Префиксные суммы и словарь «сколько раз встречалась такая префиксная сумма». Для текущей суммы s прибавляем к ответу count[s − k], затем count[s]++. Начальное значение count[0] = 1. O(n) времени и памяти; работает и с отрицательными числами, в отличие от окна.',
      },
      {
        q: 'Чем отличаются HashMap и TreeMap (или dict и SortedDict)?',
        a: 'HashMap — O(1) в среднем, порядка нет. TreeMap — сбалансированное дерево: O(log n), ключи упорядочены, есть floor/ceiling/диапазоны. Выбирают TreeMap, когда нужен ближайший ключ или обход по порядку.',
      },
      {
        q: 'Как сгруппировать слова-анаграммы?',
        a: 'Ключ группы — отсортированные буквы слова (O(L log L)) или кортеж счётчиков 26 букв (O(L)). Словарь ключ → список слов. Итого O(n·L log L) или O(n·L).',
      },
      {
        q: 'Что такое load factor и rehash?',
        a: 'Load factor — отношение числа элементов к числу корзин. Когда он превышает порог, таблица выделяет массив большего размера (обычно вдвое) и заново раскладывает все элементы — это rehash, O(n), но амортизированно вставка остаётся O(1). Если размер заранее известен, таблицу стоит создать с нужной ёмкостью.',
      },
    ],
  },
  {
    id: 'algo-sorting',
    title: 'Сортировки',
    area: A,
    level: 'base',
    minutes: 90,
    summary: 'Quicksort, mergesort, counting sort и сортировка как шаг решения.',
    body: `### Что знать наизусть
| Алгоритм | Время (среднее / худшее) | Память | Устойчивость |
|---|---|---|---|
| Пузырёк, вставками | O(n²) | O(1) | да |
| Быстрая (quicksort) | O(n log n) / O(n²) | O(log n) стек | нет |
| Слиянием (mergesort) | O(n log n) | O(n) | да |
| Кучей (heapsort) | O(n log n) | O(1) | нет |
| Подсчётом | O(n + k) | O(k) | да |

**Устойчивая** сортировка сохраняет порядок равных элементов — важно при сортировке по нескольким ключам по очереди.

### Встроенные сортировки
- Python \`sorted\`, Java \`Collections.sort\` для объектов — **Timsort**: гибрид слияния и вставок, устойчивый, O(n) на почти отсортированных данных.
- Java \`Arrays.sort\` для примитивов — dual-pivot quicksort.
- C++ \`std::sort\` — introsort (quick + heap + insertion), неустойчивый; \`std::stable_sort\` — устойчивый.
- Go \`sort.Slice\` — pdqsort, неустойчивый; \`sort.SliceStable\` — устойчивый.

### Quicksort
Выбираем опорный элемент, разбиваем на «меньше» и «больше», рекурсивно сортируем части. Худший случай O(n²) — при неудачном опорном (уже отсортированный массив и опорный = первый). Лечится случайным опорным или медианой трёх.

### Сортировка как инструмент
- Слияние пересекающихся отрезков: сортируем по началу и идём слева направо.
- Встречи и комнаты: сортируем события начала и конца.
- Жадные задачи: сортируем по дедлайну или по отношению ценности к весу.
- Кастомный компаратор: «составить наибольшее число из массива» — сравниваем a+b и b+a.

### Quickselect
k-я порядковая статистика за O(n) в среднем: как quicksort, но рекурсия только в одну часть.`,
    questions: [
      {
        q: 'Когда quicksort работает за O(n²) и как этого избежать?',
        a: 'Когда опорный элемент каждый раз оказывается минимальным или максимальным — например, первый элемент в уже отсортированном массиве. Тогда глубина рекурсии n. Помогают случайный опорный, медиана трёх, трёхпутевое разбиение при большом числе равных, переход на heapsort при большой глубине (introsort).',
      },
      {
        q: 'Что такое устойчивая сортировка и когда она нужна?',
        a: 'Устойчивая сохраняет относительный порядок равных элементов. Нужна, если сортируем по второстепенному ключу, а затем по основному: например, сначала по имени, потом по городу — внутри города имена останутся упорядочены. Mergesort и Timsort устойчивы, quicksort и heapsort — нет.',
      },
      {
        q: 'Как слить пересекающиеся отрезки?',
        a: 'Отсортировать по началу. Идти по отрезкам, держа текущий объединённый [s, e]. Если начало следующего ≤ e — расширяем e = max(e, конец). Иначе сохраняем текущий и начинаем новый. O(n log n).',
      },
      {
        q: 'Как найти k-й по величине элемент быстрее, чем сортировкой?',
        a: 'Quickselect — O(n) в среднем, O(n²) в худшем. Или куча из k элементов — O(n log k), что удобно для потока данных. Для маленького диапазона значений — подсчётом за O(n + k).',
      },
      {
        q: 'Почему mergesort хорош для связных списков и внешней сортировки?',
        a: 'Ему нужен только последовательный доступ: слияние двух отсортированных последовательностей идёт одним проходом. Для списка слияние делается без доп. памяти перестановкой ссылок. Для данных больше оперативной памяти сортируют куски и сливают их потоково (k-way merge с кучей).',
      },
    ],
  },
  {
    id: 'algo-binsearch',
    title: 'Бинарный поиск',
    area: A,
    level: 'base',
    minutes: 90,
    summary: 'Поиск в отсортированном массиве и бинпоиск по ответу.',
    body: `### Классика без ошибок
Большинство багов — в границах. Надёжный шаблон — искать **первую позицию, где условие истинно** (левая граница), с полуинтервалом \`[lo, hi)\`:

\`\`\`python
def lower_bound(a, x):
    lo, hi = 0, len(a)
    while lo < hi:
        mid = (lo + hi) // 2
        if a[mid] < x:
            lo = mid + 1
        else:
            hi = mid
    return lo  # первый индекс, где a[i] >= x
\`\`\`
- В C++/Java \`mid = lo + (hi - lo) / 2\`, чтобы не переполнить int.
- Инвариант: всё левее \`lo\` — «ложь», всё правее или равно \`hi\` — «истина».
- \`upper_bound\` — первый элемент > x. Количество вхождений x = upper − lower.

### Бинпоиск по ответу
Если функция «можно ли уложиться в ответ T?» монотонна (при большем T тоже можно), ищем минимальное подходящее T.
- Минимальная скорость, чтобы съесть бананы за h часов.
- Разбить массив на k частей с минимальной максимальной суммой.
- Минимальное время, за которое n принтеров напечатают m страниц.

Схема: границы ответа → функция \`check(T)\` за O(n) → бинпоиск за O(n log(диапазон)).

### Вариации
- Поиск в повёрнутом отсортированном массиве: одна половина всегда отсортирована.
- Пик в массиве: сравниваем a[mid] и a[mid+1].
- Бинпоиск по вещественным числам: фиксированное число итераций (60–100) вместо сравнения с eps.`,
    questions: [
      {
        q: 'Почему mid = (lo + hi) / 2 может быть ошибкой?',
        a: 'В языках с фиксированным int (C++, Java, Go) сумма lo + hi может переполниться при больших индексах. Правильно lo + (hi − lo) / 2. В Python переполнения нет.',
      },
      {
        q: 'Как найти первое и последнее вхождение числа в отсортированном массиве?',
        a: 'Два бинпоиска: lower_bound(x) — первая позиция ≥ x, upper_bound(x) — первая позиция > x. Если lower указывает не на x — элемента нет. Последнее вхождение — upper − 1. O(log n).',
      },
      {
        q: 'Что такое бинарный поиск по ответу? Приведите пример.',
        a: 'Поиск минимального (или максимального) значения параметра, для которого выполняется монотонное условие. Пример: минимальная вместимость грузовика, чтобы перевезти посылки за D дней. check(C) жадно считает дни за O(n); бинпоиск по C от max(вес) до суммы весов. Итого O(n log S).',
      },
      {
        q: 'Как искать в отсортированном, но циклически сдвинутом массиве?',
        a: 'На каждом шаге одна из половин [lo, mid] или [mid, hi] отсортирована — это видно по сравнению концов. Если x попадает в диапазон отсортированной половины — идём туда, иначе в другую. O(log n). С дубликатами худший случай деградирует до O(n).',
      },
      {
        q: 'Когда бинпоиск неприменим?',
        a: 'Когда нет монотонности: массив не отсортирован или условие check не монотонно по параметру. Тогда сначала сортируют (если можно потерять исходный порядок) или используют другую идею.',
      },
    ],
  },
  {
    id: 'algo-prefix-window',
    title: 'Префиксные суммы и скользящее окно',
    area: A,
    level: 'core',
    minutes: 90,
    summary: 'Суммы на отрезках за O(1) и подмассивы с ограничениями за O(n).',
    body: `### Префиксные суммы
\`pref[0] = 0, pref[i+1] = pref[i] + a[i]\`. Сумма на отрезке [l, r] = \`pref[r+1] − pref[l]\` за O(1) после O(n) предобработки.
- Двумерный вариант: сумма в прямоугольнике по формуле включений-исключений.
- **Разностный массив** — обратная операция: прибавить v на отрезке [l, r] = \`d[l] += v, d[r+1] −= v\`, затем префиксные суммы. Много обновлений отрезков офлайн за O(n + q).
- Префиксы + хеш-таблица: подмассивы с суммой k, с равным числом 0 и 1.

### Скользящее окно
Два указателя, задающие окно [l, r]. Правый расширяет окно, левый сужает, пока окно нарушает условие.

\`\`\`python
def longest_unique(s):
    last = {}
    l = best = 0
    for r, ch in enumerate(s):
        if ch in last and last[ch] >= l:
            l = last[ch] + 1
        last[ch] = r
        best = max(best, r - l + 1)
    return best
\`\`\`

**Когда работает окно:** условие монотонно — если окно хорошее, то и любое вложенное хорошее (или наоборот). Сумма положительных чисел ≤ S — да; сумма с отрицательными — нет, тогда нужны префиксы.

**Фиксированное окно размера k**: добавляем правый, убираем левый — среднее, максимум суммы, анаграммы подстроки.

### Задачи-маркеры
«Самая длинная подстрока без повторов», «минимальное окно, содержащее все символы», «максимум в каждом окне» (монотонный дек), «количество подмассивов с суммой k».`,
    questions: [
      {
        q: 'Как отвечать на запросы суммы на отрезке, если массив не меняется?',
        a: 'Посчитать префиксные суммы за O(n) и отвечать pref[r+1] − pref[l] за O(1). Если массив меняется — дерево Фенвика или дерево отрезков: O(log n) на запрос и обновление.',
      },
      {
        q: 'Почему скользящее окно не работает для «подмассив с суммой k» при отрицательных числах?',
        a: 'Окно опирается на монотонность: при расширении сумма растёт, при сужении — падает. С отрицательными числами это не так, и сдвиг левой границы может как уменьшить, так и увеличить сумму. Решение — префиксные суммы и словарь частот, O(n).',
      },
      {
        q: 'Как найти длину самой длинной подстроки без повторяющихся символов?',
        a: 'Окно [l, r] и словарь последней позиции символа. Если символ s[r] уже был внутри окна — сдвигаем l на позицию после его прошлого вхождения. Ответ — максимум r − l + 1. O(n).',
      },
      {
        q: 'Как найти максимум в каждом окне размера k за O(n)?',
        a: 'Монотонный дек индексов с убывающими значениями. Для каждого нового элемента выкидываем с конца все меньшие, добавляем его; с начала удаляем индексы вне окна. Максимум окна — первый элемент дека. Каждый индекс добавляется и удаляется один раз.',
      },
      {
        q: 'Что такое разностный массив и где он полезен?',
        a: 'Массив d, где d[i] = a[i] − a[i−1]. Прибавление v на отрезке [l, r] — это d[l] += v и d[r+1] −= v, O(1). После всех обновлений префиксные суммы восстанавливают a. Пример: сколько человек в каждый момент было в офисе по интервалам присутствия.',
      },
    ],
  },
  {
    id: 'algo-stack-queue',
    title: 'Стек, очередь, дек, монотонный стек',
    area: A,
    level: 'core',
    minutes: 90,
    summary: 'Скобочные последовательности, вычисление выражений, «ближайший больший элемент».',
    body: `### Стек (LIFO)
Операции push/pop/peek — O(1). Реализация: массив/список, \`ArrayDeque\` в Java (не \`Stack\`), срез в Go.

**Где встречается:**
- Проверка скобочной последовательности: открывающие кладём, на закрывающей сверяем с вершиной.
- Вычисление выражений: обратная польская запись, алгоритм сортировочной станции.
- Упрощение пути Unix «/a/./b/../c» → «/a/c».
- Отмена действий (undo), обход в глубину без рекурсии.

### Монотонный стек
Хранит индексы с монотонными значениями. Решает «для каждого элемента найти ближайший больший/меньший справа/слева» за O(n).

\`\`\`python
def next_greater(a):
    res = [-1] * len(a)
    st = []  # индексы, значения убывают
    for i, x in enumerate(a):
        while st and a[st[-1]] < x:
            res[st.pop()] = x
        st.append(i)
    return res
\`\`\`
Задачи: сколько дней до потепления, наибольший прямоугольник в гистограмме, сбор дождевой воды.

### Очередь (FIFO) и дек
- Очередь — BFS, обработка задач по порядку, буферы. В Python \`collections.deque\` (у list \`pop(0)\` — O(n)!).
- Дек — добавление/удаление с обоих концов за O(1): максимум в окне, 0-1 BFS.
- Очередь на двух стеках: амортизированно O(1) на операцию — классический вопрос.

### Стек с минимумом за O(1)
Храним пары (значение, минимум на момент вставки) или второй стек минимумов.`,
    questions: [
      {
        q: 'Как проверить правильность скобочной последовательности с тремя типами скобок?',
        a: 'Проходим строку: открывающую кладём в стек; на закрывающей проверяем, что стек не пуст и на вершине парная открывающая, и снимаем её. В конце стек должен быть пуст. O(n) времени и памяти. Типичные ошибки — забыть проверку пустого стека и проверку в конце.',
      },
      {
        q: 'Как реализовать очередь на двух стеках?',
        a: 'Стек in для push, стек out для pop. При pop, если out пуст, перекладываем всё из in в out — порядок разворачивается. Каждый элемент перекладывается один раз, поэтому амортизированно O(1) на операцию.',
      },
      {
        q: 'Как сделать стек, который возвращает минимум за O(1)?',
        a: 'Хранить вместе с каждым элементом текущий минимум стека: push(x) кладёт (x, min(x, top.min)). Тогда getMin — это top.min. Альтернатива — второй стек, куда кладём x, если x ≤ текущего минимума.',
      },
      {
        q: 'Для каждого дня найти, через сколько дней станет теплее. Как решить за O(n)?',
        a: 'Монотонный стек индексов с невозрастающими температурами. Когда приходит более тёплый день i, снимаем со стека все индексы j с меньшей температурой и записываем ответ i − j. Каждый индекс попадает в стек и снимается один раз.',
      },
      {
        q: 'Почему в Python нельзя использовать list как очередь?',
        a: 'list.pop(0) и insert(0, x) — O(n): элементы сдвигаются. Для очереди нужен collections.deque с popleft за O(1). То же в JS: shift у массива — O(n) в худшем случае.',
      },
    ],
  },
  {
    id: 'algo-linked-list',
    title: 'Связные списки',
    area: A,
    level: 'core',
    minutes: 60,
    summary: 'Разворот, быстрый и медленный указатели, слияние — любимые задачи для лайвкодинга.',
    body: `### Основное
Узел хранит значение и ссылку на следующий (и предыдущий — в двусвязном). Вставка и удаление по известному узлу — O(1), доступ по индексу — O(n). Плохо дружит с кэшем процессора, поэтому на практике массивы почти всегда быстрее.

### Приёмы
- **Фиктивная голова (dummy)**: избавляет от особого случая при удалении первого узла и при слиянии.
- **Разворот**: три указателя prev, cur, next.
\`\`\`python
def reverse(head):
    prev = None
    while head:
        head.next, prev, head = prev, head, head.next
    return prev
\`\`\`
- **Быстрый и медленный указатели** (Флойд): середина списка, наличие цикла, начало цикла, k-й с конца (отставание на k).
- **Слияние двух отсортированных** — как в mergesort, через dummy.

### Где это в жизни
LRU-кэш = хеш-таблица + двусвязный список (\`LinkedHashMap\` в Java, \`OrderedDict\` в Python). Это частая задача на проектирование в рамках кодовой секции.`,
    questions: [
      {
        q: 'Как определить, есть ли цикл в связном списке, за O(1) памяти?',
        a: 'Алгоритм Флойда: медленный указатель шагает на 1, быстрый — на 2. Если есть цикл, они встретятся; если быстрый дошёл до null — цикла нет. Чтобы найти начало цикла, после встречи ставим один указатель в голову и двигаем оба по одному шагу — встретятся в начале цикла.',
      },
      {
        q: 'Как найти середину списка за один проход?',
        a: 'Быстрый и медленный указатели: когда быстрый дошёл до конца, медленный — в середине. Для чётной длины важно договориться, какую из двух середин возвращать, и выбрать условие цикла (fast and fast.next или fast.next and fast.next.next).',
      },
      {
        q: 'Как удалить n-й узел с конца за один проход?',
        a: 'Dummy перед головой, два указателя; первый продвигаем на n + 1 шаг вперёд, затем двигаем оба до конца. Второй окажется перед удаляемым узлом: second.next = second.next.next. Dummy решает случай удаления головы.',
      },
      {
        q: 'Как устроен LRU-кэш с get и put за O(1)?',
        a: 'Хеш-таблица ключ → узел двусвязного списка. Список упорядочен по давности использования: при get узел переносится в голову, при put новый узел добавляется в голову, а при переполнении удаляется хвост и его ключ из таблицы. Все операции — O(1).',
      },
      {
        q: 'Чем массив лучше связного списка на практике, даже для вставок в середину?',
        a: 'Массив лежит в памяти подряд и хорошо кэшируется процессором; сдвиг элементов через memmove очень быстрый. Список требует выделения памяти на каждый узел и прыжков по указателям с промахами кэша. Поэтому ArrayList/vector выигрывают у LinkedList почти всегда, кроме частых вставок по уже найденному узлу.',
      },
    ],
  },
  {
    id: 'algo-recursion',
    title: 'Рекурсия и перебор с возвратом',
    area: A,
    level: 'core',
    minutes: 90,
    summary: 'Генерация подмножеств, перестановок, скобок; отсечения перебора.',
    body: `### Рекурсия
Функция решает задачу через ту же задачу меньшего размера. Нужны **база** (когда остановиться) и **шаг** (как свести к меньшему). Глубина ограничена стеком: в Python ~1000 по умолчанию (\`sys.setrecursionlimit\`), в Java зависит от размера стека потока.

### Backtracking: шаблон
\`\`\`python
def subsets(nums):
    res, cur = [], []
    def go(i):
        if i == len(nums):
            res.append(cur[:])  # копия!
            return
        go(i + 1)              # не берём
        cur.append(nums[i])
        go(i + 1)              # берём
        cur.pop()              # откат
    go(0)
    return res
\`\`\`
Главное — **откатить** изменение после рекурсивного вызова и **копировать** найденное решение.

### Классика
- Все подмножества — 2ⁿ, все перестановки — n!, сочетания из n по k.
- Правильные скобочные последовательности длины 2n: ставим «(», пока open < n, и «)», пока close < open.
- Комбинации цифр телефона, разбиение строки на палиндромы, судоку, N ферзей.

### Отсечения
Сортировка + прерывание, если сумма уже больше цели; пропуск одинаковых элементов на одном уровне для уникальных комбинаций; проверка допустимости до углубления.

### Когда перебор не нужен
Если подзадачи повторяются — это динамическое программирование (мемоизация). Если n ≤ 20 — перебор по битовым маскам тоже вариант.`,
    questions: [
      {
        q: 'Сгенерируйте все правильные скобочные последовательности из n пар скобок.',
        a: 'Рекурсия с параметрами open и close. Добавляем «(», если open < n; добавляем «)», если close < open. Когда длина 2n — сохраняем. Количество ответов — число Каталана C(n), время O(C(n)·n).',
      },
      {
        q: 'Как сгенерировать все перестановки массива без повторов, если в массиве есть одинаковые элементы?',
        a: 'Отсортировать массив и перебирать с массивом used. На каждом уровне пропускать элемент, если он равен предыдущему и предыдущий не использован (or used — в зависимости от выбранного инварианта). Так одинаковые элементы ставятся в фиксированном порядке и дубликатов не возникает.',
      },
      {
        q: 'Почему при сохранении результата нужно делать копию текущего списка?',
        a: 'В res кладётся ссылка на тот же объект cur, который продолжает изменяться при откатах. В итоге все элементы res окажутся одинаковыми (обычно пустыми). Нужно res.append(cur[:]) или list(cur), new ArrayList<>(cur).',
      },
      {
        q: 'Чем отличается перебор с возвратом от динамического программирования?',
        a: 'Перебор строит все варианты решения; ДП использует то, что подзадачи повторяются, и сохраняет их ответы. Если в дереве рекурсии одинаковые вызовы с одинаковыми аргументами встречаются много раз — добавьте мемоизацию, и перебор станет ДП.',
      },
      {
        q: 'Как перебрать все подмножества без рекурсии?',
        a: 'Битовые маски от 0 до 2ⁿ − 1: i-й бит маски говорит, взят ли i-й элемент. Удобно при n ≤ 20. Итеративный вариант: начинаем с [[]], и для каждого элемента добавляем его ко всем уже построенным подмножествам.',
      },
    ],
  },
  {
    id: 'algo-trees',
    title: 'Деревья и BST',
    area: A,
    level: 'core',
    minutes: 120,
    summary: 'Обходы, глубина, LCA, проверка BST, сериализация.',
    body: `### Обходы
- **В глубину (DFS)**: прямой (корень, левое, правое), симметричный (левое, корень, правое — для BST даёт отсортированный порядок), обратный (левое, правое, корень — удобен для вычислений «снизу вверх»).
- **В ширину (BFS)**: по уровням через очередь — вывод по уровням, правый вид дерева, минимальная глубина.

\`\`\`python
def max_depth(root):
    if not root:
        return 0
    return 1 + max(max_depth(root.left), max_depth(root.right))
\`\`\`

### Двоичное дерево поиска
Слева меньше, справа больше. Поиск, вставка, удаление — O(h). В сбалансированных деревьях (красно-чёрное, AVL) h = O(log n), в вырожденном — O(n). Библиотечные: TreeMap в Java, std::map в C++.

**Проверка BST**: передаём вниз допустимый диапазон (low, high); нельзя проверять только прямых детей.

### Популярные задачи
- Диаметр дерева: для каждого узла глубина левого + правого, обход снизу вверх.
- Сбалансированность: разница высот ≤ 1 в каждом узле за один проход.
- Наименьший общий предок (LCA): в BST — по значениям; в обычном дереве — рекурсия «нашли ли в левом/правом».
- Путь с максимальной суммой, симметричность, зеркальное отражение, сериализация.
- k-й наименьший в BST — симметричный обход со счётчиком.

### Префиксное дерево (trie)
Дерево по символам строк: автодополнение, поиск по префиксу, словарь для «Эрудита». Вставка и поиск — O(длины слова).`,
    questions: [
      {
        q: 'Как проверить, что двоичное дерево является деревом поиска?',
        a: 'Рекурсивно с диапазоном: узел валиден, если low < val < high, левое поддерево проверяется с (low, val), правое — с (val, high). Альтернатива — симметричный обход и проверка, что значения строго возрастают. Ошибка новичков — сравнивать узел только с непосредственными детьми.',
      },
      {
        q: 'Как вывести дерево по уровням?',
        a: 'BFS очередью. На каждой итерации запоминаем размер очереди — это количество узлов текущего уровня, обрабатываем ровно их, добавляя детей. O(n).',
      },
      {
        q: 'Как найти наименьшего общего предка двух узлов в обычном двоичном дереве?',
        a: 'Рекурсия: если узел пуст или равен одному из искомых — вернуть его. Иначе искать в левом и правом поддереве. Если оба результата не пусты — текущий узел и есть LCA; иначе возвращаем непустой. O(n).',
      },
      {
        q: 'Какая сложность поиска в BST и от чего она зависит?',
        a: 'O(h), где h — высота. Для сбалансированного дерева O(log n), для вырожденного (вставка отсортированных данных) — O(n). Поэтому в библиотеках используют самобалансирующиеся деревья: красно-чёрные (TreeMap, std::map) или AVL.',
      },
      {
        q: 'Как найти диаметр двоичного дерева?',
        a: 'Обратный обход: функция возвращает высоту поддерева, а по пути обновляем глобальный ответ как высота(левое) + высота(правое). Один проход O(n).',
      },
      {
        q: 'Зачем нужен trie и какая у него сложность?',
        a: 'Префиксное дерево хранит строки по символам, общие префиксы делятся. Вставка и поиск слова — O(L), поиск всех слов с префиксом — O(L + количество результатов). Используется для автодополнения, словарных игр, маршрутизации по префиксам.',
      },
    ],
  },
  {
    id: 'algo-graphs',
    title: 'Графы: BFS, DFS, топосортировка, Дейкстра',
    area: A,
    level: 'core',
    minutes: 150,
    summary: 'Представление графа, поиск путей, компоненты связности, циклы и порядок зависимостей.',
    body: `### Представление
- **Список смежности** — словарь/массив списков, O(V + E) памяти. Стандарт для разреженных графов.
- **Матрица смежности** — O(V²), проверка ребра за O(1), подходит для плотных и маленьких графов.
- **Сетка** (лабиринт, карта) — неявный граф: соседи — клетки сверху, снизу, слева, справа.

### BFS
Кратчайший путь в **невзвешенном** графе, обход по слоям. Очередь + массив расстояний/посещённых. O(V + E).

\`\`\`python
from collections import deque
def bfs(graph, s):
    dist = {s: 0}
    q = deque([s])
    while q:
        v = q.popleft()
        for u in graph[v]:
            if u not in dist:
                dist[u] = dist[v] + 1
                q.append(u)
    return dist
\`\`\`

### DFS
Компоненты связности, число островов, поиск цикла, проверка двудольности. Для поиска цикла в ориентированном графе — три цвета: белый (не были), серый (в стеке), чёрный (обработан). Ребро в серую вершину — цикл.

### Топологическая сортировка
Порядок вершин DAG, где все рёбра идут вперёд: сборка зависимостей, расписание курсов. Алгоритм Кана: считаем входящие степени, кладём в очередь вершины со степенью 0, снимаем и уменьшаем степени соседей. Если обработали не все вершины — есть цикл.

### Взвешенные графы
- **Дейкстра** — кратчайшие пути при неотрицательных весах, куча: O((V + E) log V).
- **0-1 BFS** — веса 0 и 1, дек вместо кучи.
- **Беллман — Форд** — отрицательные веса, O(VE). **Флойд — Уоршелл** — все пары, O(V³).
- **Система непересекающихся множеств (DSU)** — объединение компонент и минимальное остовное дерево (Краскал).`,
    questions: [
      {
        q: 'Почему BFS находит кратчайший путь в невзвешенном графе, а DFS — нет?',
        a: 'BFS обходит вершины в порядке неубывания расстояния: сначала все на расстоянии 1, потом 2 и т. д. Поэтому первое достижение вершины — по кратчайшему пути. DFS уходит вглубь по первому попавшемуся пути, который может быть длинным.',
      },
      {
        q: 'Как посчитать количество островов на карте из 0 и 1?',
        a: 'Проходим все клетки; встретив непосещённую «1», увеличиваем счётчик и запускаем BFS/DFS, помечая все клетки острова. O(n·m). На больших картах DFS рекурсией может переполнить стек — лучше BFS или явный стек.',
      },
      {
        q: 'Как определить, можно ли пройти все курсы, если у них есть пререквизиты?',
        a: 'Это проверка ориентированного графа на цикл. Топологическая сортировка Кана: если удалось обработать все вершины — циклов нет, и порядок прохождения найден. Иначе — есть циклическая зависимость. O(V + E).',
      },
      {
        q: 'Почему алгоритм Дейкстры не работает с отрицательными весами?',
        a: 'Дейкстра фиксирует вершину как окончательно обработанную, когда достаёт её из кучи, предполагая, что более короткого пути уже не будет. Отрицательное ребро может позже уменьшить расстояние до уже зафиксированной вершины. Для таких графов — Беллман — Форд.',
      },
      {
        q: 'Что такое DSU и какие у него оптимизации?',
        a: 'Система непересекающихся множеств: find(x) возвращает представителя множества, union(a, b) объединяет. Оптимизации — сжатие путей и объединение по рангу/размеру; вместе дают почти O(1) амортизированно (обратная функция Аккермана). Применение: компоненты связности онлайн, алгоритм Краскала, группировка аккаунтов.',
      },
    ],
  },
  {
    id: 'algo-heap',
    title: 'Куча и приоритетная очередь',
    area: A,
    level: 'core',
    minutes: 60,
    summary: 'Top-K, слияние k списков, медиана потока, планирование.',
    body: `### Устройство
Двоичная куча — полное двоичное дерево в массиве: дети узла i — 2i+1 и 2i+2. В min-куче родитель ≤ детей.
- Вставка и извлечение минимума — O(log n), просмотр минимума — O(1).
- Построение из массива (heapify) — **O(n)**, а не O(n log n).

**Библиотеки:** Python \`heapq\` (только min-куча: для max кладём \`-x\`), Java \`PriorityQueue\`, C++ \`std::priority_queue\` (max-куча по умолчанию), Go \`container/heap\`.

### Паттерны
- **Top-K**: k наибольших — min-куча размера k; всё, что меньше вершины, отбрасываем. O(n log k).
- **Слияние k отсортированных списков**: куча из голов списков. O(N log k).
- **Медиана потока**: две кучи — max-куча нижней половины и min-куча верхней, размеры отличаются максимум на 1.
- **Планирование**: задачи с приоритетами, переговорные комнаты (куча времён окончания), Дейкстра.

\`\`\`python
import heapq
def top_k_frequent(nums, k):
    from collections import Counter
    return [x for x, _ in heapq.nlargest(k, Counter(nums).items(), key=lambda p: p[1])]
\`\`\``,
    questions: [
      {
        q: 'Как найти k наибольших элементов в массиве из миллиарда чисел, который не помещается в память?',
        a: 'Читать потоком и держать min-кучу размера k. Если новое число больше вершины — заменяем вершину. Память O(k), время O(n log k). Альтернативы — внешняя сортировка или quickselect по частям, но куча проще всего.',
      },
      {
        q: 'Как поддерживать медиану потока чисел?',
        a: 'Две кучи: max-куча для меньшей половины и min-куча для большей. Новое число кладём в нужную кучу и балансируем так, чтобы размеры отличались не больше чем на 1. Медиана — вершина большей кучи или среднее двух вершин. O(log n) на вставку, O(1) на запрос.',
      },
      {
        q: 'Почему heapify работает за O(n)?',
        a: 'Просеивание вниз запускается от середины массива к началу. Большинство узлов находятся у листьев и просеиваются на малую высоту: сумма высот всех узлов полного дерева — O(n).',
      },
      {
        q: 'Сколько переговорных нужно, чтобы провести все встречи?',
        a: 'Сортируем встречи по началу и держим min-кучу времён окончания занятых комнат. Для каждой встречи, если самая ранняя комната освободилась (конец ≤ начала) — снимаем её. Кладём конец текущей. Ответ — максимальный размер кучи. O(n log n). Альтернатива — сортировка событий начала и конца.',
      },
    ],
  },
  {
    id: 'algo-dp',
    title: 'Динамическое программирование',
    area: A,
    level: 'core',
    minutes: 180,
    summary: 'Состояние, переход, база, порядок вычисления. Рюкзак, НВП, редакционное расстояние.',
    body: `### Как придумывать ДП
1. **Состояние**: что означает dp[i] (или dp[i][j]) — сформулируйте словами.
2. **Переход**: как dp[i] выражается через меньшие состояния.
3. **База**: значения для самых маленьких состояний.
4. **Порядок**: вычислять так, чтобы зависимости были готовы.
5. **Ответ**: где он лежит — dp[n], максимум по всем dp[i]…

Сверху вниз — рекурсия с мемоизацией (проще придумать). Снизу вверх — таблица в цикле (нет проблем со стеком, легче оптимизировать память).

### Классические задачи
- **Лестница / Фибоначчи**: dp[i] = dp[i−1] + dp[i−2].
- **Грабитель домов**: dp[i] = max(dp[i−1], dp[i−2] + a[i]).
- **Размен монет** (минимум монет): dp[s] = min(dp[s − c] + 1).
- **Рюкзак 0/1**: dp[w] по весу, цикл по весу **в обратном порядке**, чтобы предмет не брался дважды.
- **Наибольшая возрастающая подпоследовательность**: O(n²) через dp[i] или O(n log n) через массив «минимальных хвостов» и бинпоиск.
- **Наибольшая общая подпоследовательность, редакционное расстояние**: dp[i][j] по префиксам двух строк.
- **Пути в сетке**: dp[i][j] = dp[i−1][j] + dp[i][j−1], с препятствиями.
- **Максимальная сумма подмассива** (Кадане): cur = max(x, cur + x).

\`\`\`python
def edit_distance(a, b):
    n, m = len(a), len(b)
    dp = [[0] * (m + 1) for _ in range(n + 1)]
    for i in range(n + 1): dp[i][0] = i
    for j in range(m + 1): dp[0][j] = j
    for i in range(1, n + 1):
        for j in range(1, m + 1):
            same = a[i-1] == b[j-1]
            dp[i][j] = min(dp[i-1][j] + 1, dp[i][j-1] + 1, dp[i-1][j-1] + (0 if same else 1))
    return dp[n][m]
\`\`\`

### Оптимизация памяти
Если dp[i] зависит только от dp[i−1] — храним две строки или даже две переменные.`,
    questions: [
      {
        q: 'Как понять, что задачу решает динамическое программирование?',
        a: 'Два признака: оптимальная подструктура (оптимальный ответ строится из оптимальных ответов подзадач) и перекрывающиеся подзадачи (одни и те же подзадачи возникают многократно). Маркеры в условии: «количество способов», «минимальная стоимость», «можно ли», последовательности и строки с префиксами.',
      },
      {
        q: 'Почему в рюкзаке 0/1 с одномерным массивом цикл по весу идёт в обратном порядке?',
        a: 'dp[w] = max(dp[w], dp[w − wt] + val). При прямом порядке dp[w − wt] уже обновлён текущим предметом, и предмет может взяться несколько раз — это рюкзак с повторениями. Обратный порядок гарантирует, что используются значения с предыдущей итерации.',
      },
      {
        q: 'Как найти максимальную сумму подмассива за O(n)?',
        a: 'Алгоритм Кадане: cur — лучшая сумма подмассива, заканчивающегося в текущей позиции: cur = max(x, cur + x); ответ — максимум cur. Если все числа отрицательные, ответ — максимальный элемент (важно не инициализировать ответ нулём, если пустой подмассив запрещён).',
      },
      {
        q: 'Как найти наибольшую возрастающую подпоследовательность за O(n log n)?',
        a: 'Массив tails, где tails[k] — минимальный возможный последний элемент возрастающей подпоследовательности длины k + 1. Для каждого x бинпоиском находим первую позицию tails ≥ x и заменяем её (или добавляем в конец). Длина tails — ответ.',
      },
      {
        q: 'Мемоизация сверху вниз или таблица снизу вверх — что выбрать на интервью?',
        a: 'Мемоизацию проще придумать и объяснить: пишется рекурсия, добавляется кеш. Но в Python есть лимит глубины рекурсии, и на больших n таблица надёжнее. Хорошая стратегия: придумать через рекурсию, затем показать, что можно перевести в таблицу и сэкономить память.',
      },
      {
        q: 'Сколько существует путей из левого верхнего угла в правый нижний в сетке n×m, если ходить только вправо и вниз?',
        a: 'dp[i][j] = dp[i−1][j] + dp[i][j−1], база — первая строка и столбец равны 1. Без препятствий ответ — сочетания C(n + m − 2, n − 1). С препятствиями dp[i][j] = 0 в клетках-стенах.',
      },
    ],
  },
  {
    id: 'algo-contest',
    title: 'Как решать контест и лайвкодинг',
    area: A,
    level: 'base',
    minutes: 45,
    summary: 'Тактика на 5 часов контеста и протокол поведения на секции с кодом.',
    body: `### Контест (Яндекс Контест, Т-Банк, Ozon Techpoint)
1. **Прочитайте все задачи** за первые 10–15 минут и отсортируйте по простоте. Решайте по возрастанию сложности.
2. **Проверяйте ограничения**: по n выберите сложность (см. тему про O-нотацию).
3. **Ввод/вывод**: быстрый ввод (\`sys.stdin\` в Python, \`BufferedReader\` в Java, \`bufio\` в Go). Проверьте формат вывода — лишний пробел или отсутствие переноса могут дать WA.
4. **WA на тесте N**: вспомните краевые случаи — пустой ввод, n = 1, одинаковые элементы, максимальные значения, переполнение.
5. **Стресс-тест**: напишите медленное очевидное решение и генератор случайных маленьких тестов, сравните ответы — лучший способ найти ошибку.
6. **TL**: уменьшите асимптотику, а не константу. В Python — меньше вызовов функций, списковые включения, встроенные функции.
7. **Не застревайте**: 40 минут без прогресса — переходите к другой задаче. Частичный результат тоже оценивают.

### Лайвкодинг (секция с интервьюером)
1. **Перескажите задачу своими словами** и уточните: диапазоны, пустой вход, дубликаты, формат ответа.
2. **Разберите пример руками** — лучше свой, маленький.
3. **Озвучьте наивное решение и его сложность**, затем улучшите. Не начинайте писать код, пока интервьюер не согласился с идеей.
4. **Пишите чисто**: понятные имена, небольшие функции, без «магии».
5. **Прогоните код на примере вслух**, как дебаггер: значения переменных по шагам.
6. **Проверьте краевые случаи** и назовите итоговую сложность по времени и памяти.
7. **Слушайте подсказки**: если интервьюер спрашивает «а что будет, если…» — это почти всегда указание на баг.

### Чего не делать
- Молчать дольше минуты. Думайте вслух.
- Спорить, если просят переписать работающий код — проверяют и реакцию на требования.
- Использовать сторонние библиотеки там, где не разрешено.`,
    questions: [
      {
        q: 'Что такое стресс-тестирование решения и как его быстро написать?',
        a: 'Сравнение вашего решения с заведомо правильным медленным (перебором) на множестве случайных маленьких тестов. Нужны три части: генератор, наивное решение, цикл сравнения, который останавливается на первом расхождении и печатает тест. Пишется за 5–10 минут и находит большинство ошибок в краевых случаях.',
      },
      {
        q: 'Решение получает TL. Что делать?',
        a: 'Сначала пересчитать асимптотику по максимальным ограничениям — обычно проблема в ней, а не в константе. Затем проверить ввод-вывод (быстрое чтение, один print в конце), лишние копирования, операции O(n) внутри цикла (list.pop(0), in по списку, конкатенация строк). Для Python — избегать глубокой рекурсии и лишних вызовов функций.',
      },
      {
        q: 'Что делать, если на интервью не приходит идея решения?',
        a: 'Не молчать: проговорить наивное решение и его сложность, разобрать маленький пример руками, поискать паттерн (сортировка, хеш-таблица, два указателя, префиксы). Можно честно сказать, в каком месте застряли — интервьюер часто даёт подсказку, и умение её использовать тоже оценивается.',
      },
      {
        q: 'Какие краевые случаи проверять почти в любой задаче?',
        a: 'Пустой вход, один элемент, два элемента, все элементы одинаковые, отсортированный и обратно отсортированный вход, отрицательные числа и ноль, максимальные значения (переполнение), дубликаты, для строк — пробелы, регистр и Unicode, для графов — несвязный граф и петли.',
      },
    ],
    resources: [
      { title: 'Тренировки по алгоритмам Яндекса', url: 'https://yandex.ru/yaintern/training/algorithm-training', kind: 'course' },
      { title: 'CodeRun — задачи отбора прошлых лет', url: 'https://coderun.yandex.ru', kind: 'practice' },
      { title: 'LeetCode', url: 'https://leetcode.com/problemset/', kind: 'practice' },
      { title: 'Codeforces', url: 'https://codeforces.com', kind: 'practice' },
      { title: 'Алгоритмика — e-maxx', url: 'http://e-maxx.ru/algo/', kind: 'article' },
    ],
  },
]
