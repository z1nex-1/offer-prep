import type { Topic } from '../../types'

const C = 'C++'

export const cpp: Topic[] = [
  {
    id: 'cpp-memory',
    title: 'Память в C++: указатели, ссылки, RAII, умные указатели',
    area: C,
    level: 'core',
    minutes: 90,
    summary: 'Стек и куча, new/delete, RAII, unique_ptr, shared_ptr, weak_ptr.',
    body: `### Указатели и ссылки
- **Указатель** хранит адрес, может быть \`nullptr\`, переназначаться, поддерживает арифметику.
- **Ссылка** — псевдоним существующего объекта: обязательно инициализируется, не может быть «пустой» и не переназначается.
- \`const T*\` — нельзя менять объект через указатель, \`T* const\` — нельзя менять сам указатель.

### Стек и куча
Автоматические объекты живут на стеке и уничтожаются при выходе из области видимости. \`new\` выделяет в куче и требует \`delete\` (\`new[]\` → \`delete[]\`). Ошибки ручного управления: утечки, двойное освобождение, висячие указатели, использование после освобождения.

### RAII
**Resource Acquisition Is Initialization**: ресурс захватывается в конструкторе и освобождается в деструкторе. Деструктор вызывается автоматически при выходе из области видимости — в том числе при исключении. Так работают \`std::vector\`, \`std::string\`, \`std::lock_guard\`, \`std::fstream\`, умные указатели. В современном C++ голые \`new\`/\`delete\` почти не пишут.

### Умные указатели
- \`std::unique_ptr<T>\` — единоличное владение, нельзя копировать, можно перемещать. Нулевые накладные расходы. Создание — \`std::make_unique\`.
- \`std::shared_ptr<T>\` — разделяемое владение со счётчиком ссылок (атомарным). Объект удаляется, когда счётчик падает до нуля. \`std::make_shared\` выделяет объект и блок управления одним куском.
- \`std::weak_ptr<T>\` — наблюдатель без владения; разрывает **циклы** shared_ptr (родитель ↔ ребёнок), проверяется через \`lock()\`.

\`\`\`cpp
struct Node {
    std::vector<std::shared_ptr<Node>> children;
    std::weak_ptr<Node> parent;   // иначе цикл и утечка
};
\`\`\`

### Выравнивание и размер
\`sizeof\` структуры включает выравнивание полей (padding); порядок полей влияет на размер.`,
    questions: [
      {
        q: 'Чем указатель отличается от ссылки?',
        a: 'Указатель — переменная с адресом: может быть nullptr, может быть переназначен, поддерживает арифметику, имеет собственный адрес. Ссылка — псевдоним объекта: должна быть инициализирована, не бывает нулевой (корректно), не переназначается. Ссылки используют для передачи параметров без копирования, указатели — когда объект может отсутствовать или нужно переназначение.',
      },
      {
        q: 'Что такое RAII?',
        a: 'Идиома, при которой время жизни ресурса привязано к времени жизни объекта: захват в конструкторе, освобождение в деструкторе. Поскольку деструкторы автоматических объектов вызываются при выходе из области видимости, в том числе при исключении, ресурсы (память, файлы, мьютексы, сокеты) не утекают. Основа безопасного C++.',
      },
      {
        q: 'Чем unique_ptr отличается от shared_ptr?',
        a: 'unique_ptr — единственный владелец, копирование запрещено, передача владения через std::move, без накладных расходов по сравнению с сырым указателем. shared_ptr — совместное владение со счётчиком ссылок в блоке управления, копирование увеличивает атомарный счётчик, объект удаляется при последнем владельце. По умолчанию выбирают unique_ptr.',
      },
      {
        q: 'Зачем нужен weak_ptr?',
        a: 'Для ссылки на объект, которым владеет shared_ptr, без участия во владении. Главное применение — разрыв циклических ссылок (родитель и ребёнок держат друг друга через shared_ptr — счётчики никогда не обнулятся). Также для кэшей и наблюдателей. Перед использованием weak_ptr превращают в shared_ptr через lock() и проверяют на пустоту.',
      },
      {
        q: 'Почему make_shared лучше, чем shared_ptr<T>(new T)?',
        a: 'make_shared выделяет память под объект и блок управления одним вызовом — меньше аллокаций и лучше локальность. Кроме того, это безопасно при исключениях в выражениях с несколькими аргументами. Минус: память объекта освобождается только когда умрут и все weak_ptr, так как блок общий.',
      },
    ],
  },
  {
    id: 'cpp-oop',
    title: 'ООП в C++: виртуальные функции, конструкторы, правило пяти',
    area: C,
    level: 'core',
    minutes: 90,
    summary: 'vtable, виртуальный деструктор, специальные функции-члены, наследование.',
    body: `### Виртуальные функции
Вызов \`virtual\` метода через указатель или ссылку на базовый класс выбирает реализацию динамического типа. Реализация — **таблица виртуальных функций** (vtable) у каждого полиморфного класса и скрытый указатель vptr в каждом объекте. Чисто виртуальная функция (\`= 0\`) делает класс абстрактным. \`override\` и \`final\` — проверка компилятором.

### Виртуальный деструктор
Если объект наследника удаляется через указатель на базовый класс, у базового класса **обязательно** должен быть виртуальный деструктор — иначе деструктор наследника не вызовется (неопределённое поведение, утечки).

### Специальные функции-члены
Конструктор по умолчанию, деструктор, копирующий конструктор, копирующее присваивание, перемещающий конструктор, перемещающее присваивание.
- **Правило трёх**: если нужен один из (деструктор, копирующий конструктор, копирующее присваивание) — скорее всего нужны все три (класс управляет ресурсом).
- **Правило пяти**: плюс перемещающие операции.
- **Правило нуля**: лучше не управлять ресурсами вручную, а использовать RAII-члены (\`vector\`, \`unique_ptr\`) — тогда специальные функции генерируются корректно сами.

### Конструкторы
Списки инициализации (порядок инициализации — порядок объявления полей, а не списка). \`explicit\` запрещает неявные преобразования. Делегирующие конструкторы. Не вызывайте виртуальные функции в конструкторе и деструкторе — вызовется версия текущего класса.

### Наследование
Публичное («является»), защищённое, приватное («реализовано через»). Срезка (**slicing**) — при копировании наследника в объект базового класса по значению теряется часть наследника. Ромбовидное наследование решается \`virtual\` наследованием.`,
    questions: [
      {
        q: 'Как работают виртуальные функции?',
        a: 'У каждого класса с виртуальными функциями компилятор создаёт таблицу виртуальных функций — массив указателей на реализации. Каждый объект такого класса содержит скрытый указатель на таблицу своего динамического типа. Вызов виртуального метода через указатель или ссылку идёт через эту таблицу: один лишний косвенный переход и невозможность встраивания.',
      },
      {
        q: 'Зачем нужен виртуальный деструктор?',
        a: 'Если удалять объект производного класса через указатель на базовый, а деструктор базового не виртуальный, вызовется только деструктор базового — это неопределённое поведение, на практике ресурсы наследника не освобождаются. Правило: у класса, предназначенного для полиморфного использования, деструктор публичный и виртуальный (или защищённый и невиртуальный).',
      },
      {
        q: 'Что такое правило трёх, пяти и нуля?',
        a: 'Правило трёх: если классу нужен пользовательский деструктор, копирующий конструктор или копирующее присваивание, скорее всего нужны все три — класс владеет ресурсом. Правило пяти добавляет перемещающие конструктор и присваивание. Правило нуля: проектировать классы так, чтобы ресурсы держали RAII-члены, и не писать ни одной из этих функций вручную.',
      },
      {
        q: 'Что такое срезка объекта?',
        a: 'Когда объект производного класса копируется в переменную базового класса по значению, копируется только базовая часть — поля и поведение наследника теряются, виртуальные вызовы идут к базовой реализации. Избегают передачей по ссылке или указателю.',
      },
      {
        q: 'Можно ли вызывать виртуальную функцию в конструкторе?',
        a: 'Можно, но вызовется версия того класса, чей конструктор сейчас выполняется, а не наследника: объект наследника ещё не построен, и vptr указывает на таблицу текущего класса. Вызов чисто виртуальной функции таким образом — неопределённое поведение. Поэтому так не делают.',
      },
    ],
  },
  {
    id: 'cpp-move',
    title: 'Семантика перемещения',
    area: C,
    level: 'core',
    minutes: 60,
    summary: 'lvalue и rvalue, std::move, perfect forwarding, copy elision.',
    body: `### lvalue и rvalue
**lvalue** — выражение с именем и адресом (переменная). **rvalue** — временный объект, который вот-вот исчезнет (\`f()\`, \`a + b\`, литерал). Rvalue-ссылка \`T&&\` связывается с временными объектами.

### Перемещение
Перемещающий конструктор «забирает» ресурсы у временного объекта вместо копирования: у \`std::vector\` — просто переставить указатель на буфер, O(1) вместо O(n).
\`\`\`cpp
class Buffer {
    char* data_ = nullptr; size_t n_ = 0;
public:
    Buffer(Buffer&& other) noexcept : data_(other.data_), n_(other.n_) {
        other.data_ = nullptr; other.n_ = 0;   // оставить источник в валидном состоянии
    }
};
\`\`\`
Перемещающие операции помечают \`noexcept\` — иначе \`std::vector\` при росте будет копировать элементы ради гарантии исключений.

### std::move
Не перемещает сам по себе — это **приведение к rvalue-ссылке**, разрешающее вызвать перемещающую перегрузку. После перемещения объект в валидном, но неопределённом состоянии: его можно уничтожить или присвоить новое значение.

### Perfect forwarding
В шаблонах \`T&&\` — «универсальная» ссылка (forwarding reference), \`std::forward<T>(x)\` передаёт аргумент дальше с сохранением его категории (lvalue остаётся lvalue). Так устроены \`make_unique\`, \`emplace_back\`.

### Copy elision и RVO
Возврат локального объекта по значению обычно не копирует и не перемещает — объект строится сразу в месте назначения (с C++17 для prvalue это гарантировано). Поэтому \`return std::move(local);\` — антипаттерн: мешает оптимизации.`,
    questions: [
      {
        q: 'Что делает std::move?',
        a: 'Ничего не перемещает: это static_cast к rvalue-ссылке. Он лишь разрешает выбрать перемещающий конструктор или оператор присваивания, которые и выполняют перенос ресурсов. После перемещения исходный объект остаётся в валидном, но неопределённом состоянии.',
      },
      {
        q: 'Зачем перемещающий конструктор помечают noexcept?',
        a: 'Контейнеры вроде std::vector при перераспределении памяти должны сохранять строгую гарантию исключений. Если перемещение может бросить исключение, вектор копирует элементы (std::move_if_noexcept), чтобы при ошибке оставить исходные данные нетронутыми. noexcept позволяет перемещать и существенно ускоряет рост вектора.',
      },
      {
        q: 'Что такое RVO и почему return std::move(x) плох?',
        a: 'Return Value Optimization — компилятор строит возвращаемый объект сразу в памяти вызывающего, без копирования и перемещения (для временных объектов с C++17 гарантированно). return std::move(local) превращает выражение в rvalue-ссылку, что запрещает NRVO и заставляет выполнить перемещение — хуже, чем просто return local.',
      },
      {
        q: 'Что такое perfect forwarding?',
        a: 'Передача аргументов из шаблонной функции в другую функцию с сохранением их категории значения и константности. Используются forwarding-ссылки T&& в шаблоне и std::forward<T>(arg): lvalue передаётся как lvalue, rvalue — как rvalue, и вызывается нужная перегрузка. Так работают emplace_back и make_unique.',
      },
    ],
  },
  {
    id: 'cpp-stl',
    title: 'STL: контейнеры, итераторы, алгоритмы',
    area: C,
    level: 'core',
    minutes: 90,
    summary: 'vector, map, unordered_map, set, deque, priority_queue: устройство и сложность.',
    body: `### Контейнеры
| Контейнер | Устройство | Доступ | Вставка | Примечание |
|---|---|---|---|---|
| \`vector\` | динамический массив | O(1) | O(1) аморт. в конец | инвалидация итераторов при росте |
| \`deque\` | блоки фиксированного размера | O(1) | O(1) с обоих концов | ссылки на элементы стабильны при вставке в концы |
| \`list\` | двусвязный список | O(n) | O(1) по итератору | итераторы стабильны |
| \`map\`/\`set\` | красно-чёрное дерево | O(log n) | O(log n) | упорядочены, \`lower_bound\` |
| \`unordered_map\`/\`set\` | хеш-таблица с цепочками | O(1) в среднем | O(1) в среднем | порядок не определён |
| \`priority_queue\` | куча на vector | O(1) top | O(log n) | max-куча по умолчанию |

### vector
\`size\` — число элементов, \`capacity\` — выделенная память. \`reserve(n)\` — заранее выделить, \`emplace_back\` — построить элемент на месте. При росте все итераторы, указатели и ссылки **инвалидируются**. \`vector<bool>\` — особая упакованная специализация, не настоящий контейнер bool.

### Итераторы
Категории: input, output, forward, bidirectional, random access (\`vector\`, \`deque\`), contiguous. Инвалидация — частый источник багов: удаление из \`vector\` в цикле делают через идиому erase–remove или \`std::erase_if\` (C++20).

### Алгоритмы
\`sort\` (introsort, O(n log n), неустойчивая), \`stable_sort\`, \`nth_element\`, \`lower_bound\`/\`upper_bound\`, \`binary_search\`, \`find\`, \`count\`, \`accumulate\`, \`transform\`, \`unique\` (сдвигает дубликаты, но не удаляет — нужен \`erase\`), \`reverse\`, \`next_permutation\`.

### Приёмы для задач
\`\`\`cpp
std::ios::sync_with_stdio(false); std::cin.tie(nullptr);   // быстрый ввод
std::sort(v.begin(), v.end(), [](auto& a, auto& b){ return a.second > b.second; });
auto it = std::lower_bound(v.begin(), v.end(), x);
std::priority_queue<int, std::vector<int>, std::greater<>> minHeap;
\`\`\``,
    questions: [
      {
        q: 'Чем std::map отличается от std::unordered_map?',
        a: 'map — сбалансированное дерево (красно-чёрное): ключи упорядочены, операции O(log n), есть lower_bound и обход по порядку, нужен operator<. unordered_map — хеш-таблица: O(1) в среднем и O(n) в худшем случае, порядок не определён, нужен std::hash и operator==. Для частых поисков без порядка — unordered_map, для диапазонов и упорядоченного обхода — map.',
      },
      {
        q: 'Когда инвалидируются итераторы vector?',
        a: 'При перераспределении памяти (вставка сверх capacity, reserve, shrink_to_fit) — все итераторы, указатели и ссылки. При вставке без перераспределения — итераторы начиная с позиции вставки. При erase — итераторы начиная с удалённой позиции. Поэтому удаление в цикле делают через it = v.erase(it) или erase–remove.',
      },
      {
        q: 'Как удалить из vector все элементы, удовлетворяющие условию?',
        a: 'Идиома erase–remove: v.erase(std::remove_if(v.begin(), v.end(), pred), v.end()). remove_if сдвигает оставляемые элементы в начало и возвращает новый конец, erase удаляет хвост. В C++20 — std::erase_if(v, pred). Сложность O(n) вместо O(n²) при удалении по одному.',
      },
      {
        q: 'Чем emplace_back отличается от push_back?',
        a: 'push_back принимает готовый объект и копирует или перемещает его в вектор. emplace_back принимает аргументы конструктора и строит объект прямо в памяти вектора, избегая временного объекта. Для уже существующих объектов разницы почти нет; emplace_back может неожиданно вызвать explicit-конструктор.',
      },
      {
        q: 'Какая сложность у std::sort и какой алгоритм внутри?',
        a: 'O(n log n) в худшем случае — стандарт это требует с C++11. Обычно реализован introsort: быстрая сортировка, которая переключается на пирамидальную при слишком глубокой рекурсии, и на сортировку вставками для маленьких подмассивов. Сортировка неустойчивая; для устойчивой — std::stable_sort.',
      },
    ],
  },
  {
    id: 'cpp-ub',
    title: 'Неопределённое поведение, const, шаблоны',
    area: C,
    level: 'plus',
    minutes: 60,
    summary: 'Типичные UB, const-корректность, шаблоны и constexpr.',
    body: `### Неопределённое поведение (UB)
Программа с UB может делать что угодно, и компилятор оптимизирует, предполагая, что UB нет. Частые случаи:
- выход за границы массива, разыменование \`nullptr\` и висячих указателей;
- использование после \`delete\`, двойной \`delete\`, \`delete\` вместо \`delete[]\`;
- переполнение **знаковых** целых (беззнаковые — по модулю, это определено);
- неинициализированные переменные;
- гонки данных;
- нарушение strict aliasing;
- модификация объекта дважды без точки следования (\`i = i++\` до C++17).
Инструменты: санитайзеры \`-fsanitize=address,undefined,thread\`, valgrind, предупреждения компилятора \`-Wall -Wextra\`.

### const-корректность
\`const\` методы не меняют наблюдаемое состояние объекта; вызываются у константных объектов. \`mutable\` — поля, которые можно менять и в const-методах (кэши, мьютексы). Передача параметров: \`const T&\` для больших объектов, по значению — для маленьких и когда всё равно копируем.

### Шаблоны
Обобщённый код, инстанцируется при компиляции для каждого набора типов. Специализация (полная и частичная для классов). SFINAE и **concepts** (C++20) — ограничения на параметры шаблонов:
\`\`\`cpp
template <std::integral T>
T gcd(T a, T b) { return b == 0 ? a : gcd(b, a % b); }
\`\`\`
\`constexpr\` — вычисление на этапе компиляции. \`auto\`, \`decltype\`, лямбды, structured bindings (\`auto [k, v] : map\`).

### Компиляция и линковка
Препроцессор → компиляция единиц трансляции → линковка. Правило одного определения (ODR). Заголовочные файлы, \`#pragma once\`, \`inline\` для определений в заголовках. Статическая и динамическая линковка.`,
    questions: [
      {
        q: 'Что такое неопределённое поведение? Приведите примеры.',
        a: 'Ситуация, для которой стандарт не предписывает никакого поведения: программа может упасть, работать «правильно» или выдавать мусор, а компилятор вправе оптимизировать, считая, что UB не происходит. Примеры: выход за границы массива, разыменование нулевого или висячего указателя, переполнение знакового int, чтение неинициализированной переменной, гонка данных, двойное удаление.',
      },
      {
        q: 'Почему переполнение int — UB, а unsigned — нет?',
        a: 'Для беззнаковых типов стандарт определяет арифметику по модулю 2ⁿ. Для знаковых переполнение не определено — исторически из-за разных представлений отрицательных чисел, а теперь это позволяет компилятору оптимизировать (например, считать, что i + 1 > i в цикле всегда истинно). Обнаружить помогает -fsanitize=undefined.',
      },
      {
        q: 'Что такое const-метод и зачем mutable?',
        a: 'Метод, помеченный const, обещает не менять наблюдаемое состояние объекта: внутри this — указатель на const. Только такие методы можно вызвать у константного объекта или через const-ссылку. mutable позволяет менять отдельные поля и в const-методах — для технических деталей, не влияющих на логическое состояние: кэшированных значений, мьютексов, счётчиков обращений.',
      },
      {
        q: 'Как ограничить параметры шаблона?',
        a: 'В C++20 — концептами: template <std::integral T> или requires-выражения. Раньше — SFINAE через std::enable_if и static_assert внутри шаблона. Концепты дают читаемые ошибки компиляции и явно документируют требования к типу.',
      },
    ],
  },
]
